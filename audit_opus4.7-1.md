| Archivo(s) | Error Lógico / Fuga Detectada | Solución Arquitectónica Propuesta |
|---|---|---|
| doc6 §8 (Payment Invariante 4) ↔ doc13 §1 (`payment_status`) | **ENUM desincronizado**. doc6 define explícitamente el estado `in_process` como 6to estado canónico (pagos CBU/transferencia 24-48hs que extienden el timer del booking a 48hs). doc13 lo omite del ENUM y lo comenta como "absorbido en `pending`". Esto rompe: (a) la lógica del timer dual 15min/48hs (no hay forma de discriminar estados en SQL), (b) los reports financieros no podrán separar pagos `in_process` reales de pagos recién iniciados. | Agregar el valor faltante al ENUM: `ALTER TYPE payment_status ADD VALUE 'in_process' AFTER 'pending';`. Actualizar el comentario del ENUM en doc13 §1 eliminando "(incl. in_process CBU 24-48hs)". Reflejar en `booking.state-machine.ts` la lectura de `payment.status = 'in_process'` para extender el timer a 48hs. |
| doc6 §3 (Booking `payment_method`) ↔ doc13 §3.2 (CREATE TABLE bookings) | **Columna faltante en schema**. doc6 declara `payment_method enum? 'mercadopago'\|'cash'\|'transfer'` como atributo propio del Booking (necesario para reservas manuales con seña en efectivo donde no hay fila en `payments`). doc13 omite la columna. Además, si se agregara, el set `{mercadopago, cash, transfer}` no coincide con el ENUM `payment_method` de doc13 que tiene 4 valores (`cash, transfer, mercadopago, other`). | Opción A (recomendada, respeta doc6): agregar columna `payment_method payment_method` (nullable) a `bookings` reutilizando el ENUM existente de 4 valores. Opción B: declarar el atributo en doc6 como *derivado* de `payments.method` vía la FK `payment_id` y eliminarlo de la lista de atributos propios. Decidir una sola fuente de verdad. |
| doc13 §3.2 (bookings INSERT policy) ↔ doc12 §4.1 (player middleware) | **Fuga lógica (fail-closed silencioso)** sobre flujo de reserva del jugador. La policy `tenant_isolation_insert` exige `tenant_id = current_setting('app.current_tenant_id', true)::UUID`, pero doc12 §4.1 explicita que para JWT `type=player` **NO** se setea `app.current_tenant_id` (solo `app.current_player_id`). Resultado: un `POST /api/player/bookings` con el rol `turnogol_app` es **rechazado silenciosamente por RLS** (la comparación con NULL da NULL → rechazo). La doc implica que esto debe funcionar, pero no hay policy que lo permita. | Agregar policy dedicada `player_self_insert` en `bookings`: `CREATE POLICY player_self_insert ON bookings FOR INSERT WITH CHECK (player_id IS NOT NULL AND player_id = current_setting('app.current_player_id', true)::UUID AND tenant_id IS NOT NULL);`. Documentar en doc12 §8.5 que los endpoints de jugador DEBEN popular `tenant_id` explícitamente en el INSERT (derivado de `tenant_slug` validado). Agregar test de integración que cree reserva con contexto de player y verifique éxito. |
| doc13 §3.2 (bookings `realtime_tenant_select`) ↔ doc12 §10.1 (test "Without tenant context = 0 rows") | **Fuga fail-open sobre fail-safe**. La policy Realtime evalúa `tenant_id = (auth.jwt()->'app_metadata'->>'tenant_id')::UUID`. Las múltiples policies se combinan con OR. Si el middleware falla en ejecutar `SET LOCAL app.current_tenant_id` (bug), el staff autenticado por Supabase client sigue viendo sus bookings vía JWT. Esto **rompe la garantía** documentada en doc12 §10.1 de "sin contexto = cero filas" y elimina una capa de defensa declarada en doc12 §12 (Capa 2). | Limitar la policy Realtime al rol `authenticated` de Supabase y NO aplicarla al rol `turnogol_app` del backend: `CREATE POLICY realtime_tenant_select ON bookings FOR SELECT TO authenticated USING (...)`. Asegurar que `turnogol_app` queda sujeto SOLO a las policies basadas en `current_setting`. Agregar test negativo en doc16: con rol `turnogol_app` y sin `SET LOCAL`, `SELECT * FROM bookings` debe devolver 0 filas incluso con JWT válido en contexto. |
| doc12 §2 (players, staff_users globales sin RLS) | **Fuga de PII cross-tenant** (compliance Ley 25.326). Staff del Tenant A puede ejecutar `SELECT email, phone, first_name FROM players` o `SELECT * FROM staff_users` y obtener datos personales de jugadores y staff de otros complejos con los que no tiene relación legítima. Viola el principio de minimización declarado implícitamente en la arquitectura (doc12 regla absoluta: "si puede exponer info de un complejo a otro, debe tener RLS"). | Mantener `players` y `staff_users` como tablas globales pero **agregar RLS basada en relación**. Para `players`: `CREATE POLICY staff_can_see_related_players ON players FOR SELECT USING (EXISTS (SELECT 1 FROM player_tenant_relationships ptr WHERE ptr.player_id = players.id AND ptr.tenant_id = current_setting('app.current_tenant_id', true)::UUID));` + policy para el propio jugador vía `app.current_player_id`. Para `staff_users`: `CREATE POLICY staff_see_same_tenant_staff ON staff_users FOR SELECT USING (EXISTS (SELECT 1 FROM tenant_staff_members tsm WHERE tsm.staff_user_id = staff_users.id AND tsm.tenant_id = current_setting('app.current_tenant_id', true)::UUID));`. Actualizar doc12 §2 reclasificando a "global con RLS relacional". |
| doc6 §17 (TenantPlayerBan Invariante 1) ↔ doc13 §3.11 (`uq_tenant_player_ban`) | **Constraint mal expresada**. doc6 dice "un solo ban **activo** por jugador por complejo" pero doc13 implementa `UNIQUE (tenant_id, player_id)` sin filtrar por vigencia. Consecuencia: cuando un ban expira (`banned_until < NOW()`) el admin **no puede volver a banear con un nuevo INSERT** (viola UNIQUE); debe UPDATE o DELETE previo, perdiendo el historial. El Flujo 4D (3 no-shows) se rompe al segundo ban del mismo jugador. | Reemplazar la constraint por un índice único parcial: `DROP CONSTRAINT uq_tenant_player_ban; CREATE UNIQUE INDEX uq_tenant_player_active_ban ON tenant_player_bans (tenant_id, player_id) WHERE banned_until IS NULL OR banned_until > NOW();`. Esto preserva historial e impide solo solapamiento de bans vigentes. Nota: el índice parcial con `NOW()` requiere estrategia de refresh (job nocturno) o validación a nivel de aplicación; alternativa más limpia es usar columna `is_active BOOLEAN GENERATED ALWAYS AS (banned_until IS NULL OR banned_until > NOW()) STORED` + índice parcial sobre ella. |
| doc13 §3.11 (`tenant_player_bans` RLS) ↔ doc7 Flujo 2 (verificación de ban al reservar) | **Fuga fail-open en verificación de ban**. La tabla tiene solo 4 policies `tenant_isolation_*`. Cuando un jugador autenticado (con `app.current_player_id` seteado, sin `app.current_tenant_id`) intenta reservar, la verificación `SELECT ... FROM tenant_player_bans WHERE player_id = $1 AND tenant_id = $2` ejecutada con rol `turnogol_app` devuelve **0 filas silenciosamente** (RLS filtra todo sin contexto de tenant). Resultado: jugador baneado puede reservar. | Agregar policy de lectura para el propio jugador: `CREATE POLICY player_own_bans_select ON tenant_player_bans FOR SELECT USING (player_id = current_setting('app.current_player_id', true)::UUID);`. En paralelo, el endpoint `POST /api/player/bookings` debe setear `SET LOCAL app.current_tenant_id` temporalmente (derivado del `tenant_slug` validado) para ejecutar la verificación de ban bajo policy de staff, o usar service role para ese check puntual. Documentar explícitamente en doc12 §7.4 el mecanismo elegido. |
| doc13 §3.12 (`player_tenant_relationships` policies) ↔ doc6 §6 (creada en primera reserva) | **Fuga fail-closed sobre INSERT**. doc6 dice "Creada automáticamente en la primera reserva del jugador en un complejo". El INSERT ocurre en el contexto del endpoint del jugador (que solo tiene `app.current_player_id`). doc13 solo define `tenant_isolation_insert` (requiere `app.current_tenant_id`) y un SELECT para el jugador. **No hay policy de INSERT compatible con el contexto del jugador** → el trigger/servicio falla al crear la PTR, rompiendo consentimiento Ley 25.326. | Agregar policy: `CREATE POLICY player_self_ptr_insert ON player_tenant_relationships FOR INSERT WITH CHECK (player_id = current_setting('app.current_player_id', true)::UUID AND tenant_id IS NOT NULL);`. Alternativamente, crear la PTR via trigger `AFTER INSERT ON bookings` ejecutado con `SECURITY DEFINER` para bypasear RLS con lógica controlada. La opción trigger es más robusta porque acopla atómicamente la PTR al booking. |
| doc13 §3.2 (transición bookings `pending_payment`→`expired`/`confirmed`) ↔ doc6 §3 State Machine | **Race condition no enforced a nivel de schema**. doc6 describe la competencia entre el job de expiración (15min) y el webhook MP llegando en el segundo 14:59. doc13 NO enforces la transición con un trigger ni documenta el patrón de UPDATE condicional. Sin esto, dos workers concurrentes pueden ambos transicionar, generando audit logs contradictorios (`booking.expired` seguido de `booking.confirmed` sobre una reserva "resucitada"). | Opción 1 (aplicativa, documentar en doc13 §4): exigir que TODA transición de `pending_payment` use la forma `UPDATE bookings SET status = $new WHERE id = $id AND status = 'pending_payment' RETURNING *;` y chequear `rowCount = 1` antes de disparar efectos secundarios. Opción 2 (schema-level, más robusta): trigger `BEFORE UPDATE ON bookings` que valide transiciones contra una matriz de estados permitidos y rechace reingresos a estados ya terminales (expired/completed/no_show/canceled_*). Esto reemplaza el trigger `prevent_immutable_booking_changes` existente extendiéndolo a todos los estados finales. |
| doc13 §1 (ENUM `payment_method` = 4 valores) ↔ doc6 §8 (Payment attr `method` = 4 valores) ↔ doc6 §3 (Booking attr `payment_method` = 3 valores) | **Inconsistencia de cardinalidad**. El ENUM en doc13 y el atributo `method` de Payment en doc6 definen 4 valores (`cash, transfer, mercadopago, other`). El atributo `payment_method` del Booking en doc6 lista solo 3 (sin `other`). Dado que el Booking referencia el mismo concepto de medio de pago, debe alinearse o declarar explícitamente por qué excluye `other`. | Sincronizar doc6 §3 Booking `payment_method` para incluir los 4 valores (`cash, transfer, mercadopago, other`) igual que Payment, o eliminar el atributo del Booking si se resuelve la finding #2 como "derivado de payments.method". |

Hallazgos menores detectados pero **fuera del scope estricto de Fase 1** (involucran doc5/doc14), diferidos para Fase 2: typo `'pending'` vs `'pending_payment'` en doc5 §9; granularidad excesiva del `SELECT FOR UPDATE` sobre toda la fila de `courts` en doc5/doc14 (la exclusion constraint ya previene el doble booking; el FOR UPDATE serializa innecesariamente reservas en fechas distintas de la misma cancha).

