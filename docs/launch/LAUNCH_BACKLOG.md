# TurnoGol — Launch Backlog

| ID | Prioridad | Tarea | Archivos | Criterio de aceptación | Comando de verificación | Estado |
|---|---|---|---|---|---|---|
| TG-BL-01 | P1 | Cubrir las 4 tablas RLS post-021 en la suite bloqueante de aislamiento: fail-safe cross-tenant para push_subscriptions y player_favorites; fail-safe de escritura para feature_flags y reviews (lectura pública por diseño) | `tests/integration/isolation.test.ts` | El loop fail-safe (bloque H) corre sobre las 4 tablas; test rojo si se dropea cualquier policy | `pnpm test:integration -- isolation` | Pendiente |
| TG-BL-02 | P2 | Migración nueva: `ALTER TABLE {players, staff_users, system_admins, push_subscriptions, feature_flags, reviews, player_favorites} FORCE ROW LEVEL SECURITY` + aserción de schema (toda tabla con relrowsecurity debe tener relforcerowsecurity) | nueva migr. en `src/shared/db/migrations/`, `tests/integration/isolation.test.ts` | Todas las tablas con RLS tienen FORCE; aserción falla ante tabla nueva sin FORCE | `pnpm test:integration -- isolation` | Pendiente |
| TG-BL-03 | P1 | Corregir webhook de suscripción SaaS: usar `getBillingGateway()` (token master) en vez de `resolveTenantGateway` para `getPaymentStatus` en las ramas `subscription_authorized_payment` / `subscription_preapproval`; agregar `assert info.externalReference === job.tenantId` antes de mutar; mover el guard `TenantMpNotConnectedError` para que aplique solo a la rama `payment` (booking/upgrade) | `src/modules/payments/mp-webhook.handler.ts`, `src/modules/billing/billing.gateway.ts` | Renovación SaaS auto-confirma con token master; webhook de suscripción con tenant mismatch se rechaza; test cross-tenant/token de suscripción rojo→verde | `pnpm test:integration -- mp-webhook` | Pendiente |
