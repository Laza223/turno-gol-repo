# TurnoGol — Launch Backlog

| ID | Prioridad | Tarea | Archivos | Criterio de aceptación | Comando de verificación | Estado |
|---|---|---|---|---|---|---|
| TG-BL-01 | P1 | Cubrir las 4 tablas RLS post-021 en la suite bloqueante de aislamiento: fail-safe cross-tenant para push_subscriptions y player_favorites; fail-safe de escritura para feature_flags y reviews (lectura pública por diseño) | `tests/integration/isolation.test.ts` | El loop fail-safe (bloque H) corre sobre las 4 tablas; test rojo si se dropea cualquier policy | `pnpm test:integration -- isolation` | ✅ Hecho (bloques H/L/M, commit 1067806; re-verificado 2026-07-08, 111/111 verde) |
| TG-BL-02 | P2 | Migración nueva: `ALTER TABLE {players, staff_users, system_admins, push_subscriptions, feature_flags, reviews, player_favorites} FORCE ROW LEVEL SECURITY` + aserción de schema (toda tabla con relrowsecurity debe tener relforcerowsecurity) | nueva migr. en `src/shared/db/migrations/`, `tests/integration/isolation.test.ts` | Todas las tablas con RLS tienen FORCE; aserción falla ante tabla nueva sin FORCE | `pnpm test:integration -- isolation` | ✅ Hecho (migr. 036, commit 1067806, espejo supabase/ en 92b54e2; re-verificado 2026-07-08, 111/111 verde) |
| TG-BL-03 | P1 | Corregir webhook de suscripción SaaS: usar `getBillingGateway()` (token master) en vez de `resolveTenantGateway` para `getPaymentStatus` en las ramas `subscription_authorized_payment` / `subscription_preapproval`; agregar `assert info.externalReference === job.tenantId` antes de mutar; mover el guard `TenantMpNotConnectedError` para que aplique solo a la rama `payment` (booking/upgrade) | `src/modules/payments/mp-webhook.handler.ts`, `src/modules/billing/billing.gateway.ts` | Renovación SaaS auto-confirma con token master; webhook de suscripción con tenant mismatch se rechaza; test cross-tenant/token de suscripción rojo→verde | `pnpm test:integration -- mp-webhook` | ✅ Hecho (2 tests nuevos, gate 567/567 verde) |
| TG-BL-04 | P1 | Arreglar 4 tests de integración rojos que bloquean el gate verde (encontrados corriendo `pnpm test:integration` en TG-BL-01; fallan también standalone → pre-existentes, ajenos a RLS). (a) `admin-jobs-endpoint`: fixture usa `'sa-1'` como system_admin id → `22P02 string_to_uuid`, usar UUID real. (b) `push-test-endpoint` (2 tests): mock de `extractAuthUser` desalineado con `withTenant` → 403 en vez de 200. (c) `mp-callback-app-url` + `mp-callback-happy-path`: fallo a nivel archivo (beforeAll/env MP), verificar config/mocks. | `tests/integration/admin-jobs-endpoint.test.ts`, `tests/integration/push-test-endpoint.test.ts`, `tests/integration/mp-callback-app-url.test.ts`, `tests/integration/mp-callback-happy-path.test.ts` | `pnpm test:integration` verde (0 fallas); cada archivo pasa standalone | `pnpm test:integration` | ✅ Hecho (gate 565/565 verde) |
| TG-BL-05 | P2 | Día 3 3.1 (`pnpm test:e2e`) reveló ~19 tests e2e desactualizados vs UI/lógica actual (detalle completo en RISK_REGISTER TG-INFO-E2E-01, deuda aceptada — no bloquea deploy: el job e2e de CI corre solo en PRs a `main`, no en push directo). Reescribir selectors/copy en: `admin-login.spec.ts` + `cross-browser/login-smoke.spec.ts` (botón "Ingresar", no "iniciar sesión"), `caja-crud.spec.ts:266` (heading `/Caja/i` ambiguo), `abonados-crud.spec.ts` (PhoneInput ahora hidden input, name real no es `contactPhone` visible), `onboarding.spec.ts` (placeholder de teléfono cambió), `reservas-crud.spec.ts` + `critical-flows/admin-cancel-mp-refund.spec.ts` (radios "Sin/Con reembolso" ELIMINADOS — Tarea #3 ya decide el reembolso por motivo, reescribir para aserción contra el texto informativo nuevo), `admin-create-booking-ui.spec.ts:56` (botón "60 min" no existe más — cambio #14 eliminó el picker de duración), `push.spec.ts:72` (grilla ya no usa `<table>` literal tras el rediseño), `canchas-crud.spec.ts` (3 tests, causa raíz no confirmada — posible efecto de los refactors recientes de PricingGrid/StepCourts, Ticket 3), `touch-targets.spec.ts` (mobile-chrome: 4 elementos nuevos de la grilla bajo 44px — revisar si es regresión real de a11y o objetivo de contraste a redefinir) | 8 archivos de `tests/e2e/` (ver detalle arriba) | Los ~19 tests pasan de nuevo reflejando la UI actual; `touch-targets` confirma si hace falta un fix de a11y en la grilla o solo ajustar el test | `pnpm test:e2e` | ✅ Hecho (2026-07-08). `abonados-crud` ya pasaba (la cifra "~19" incluía ruido de contención en paralelo, no bugs). `touch-targets` ya pasaba sin cambios — resuelto por el trabajo previo de touch targets a 44px (mobile-refactor). Hallazgo no listado acá: `canchas-crud` (los 3 tests) fallaba por un 500 real en `POST /canchas` — `src/app/(admin)/canchas/actions.ts` importaba `@/shared/storage/r2` (→ `@aws-sdk/client-s3`) a nivel de módulo, y ese import roto (dependencia `@aws-sdk/checksums` desalineada, ver nota abajo) tumbaba TODAS las acciones del archivo, no solo las de fotos. Fix: import dinámico de r2 solo dentro de `uploadCourtPhotoAction`/`removeCourtPhotoAction`. 39/39 verde (`pnpm typecheck`/`lint`/`test` también verdes). | 

## Tickets del Addendum Día 0 (ex-`LAUNCH_BACKLOG.md` raíz, fusionado 2026-07-07)

### 🟢 Completados (Día 0 y Día 1/2)
- **MP-WEBHOOK-001 (Diseño + Implementación)**: harness de replay de webhooks de MercadoPago.
- **INV-ABUSE-001**: rate limiting mínimo para holds (anti denial-of-inventory en portal público).
- **PRIVACY-001**: `/privacidad` y `/terminos` publicados.
- **OPS-48-001**: protocolo primeras 48 horas post-launch.

### 🟡 Pendientes
- **STAGING-001**: ambiente de staging real (project ref/credenciales) NO provisionado. Ver `docs/operations/LAUNCH.md` y `RISK_REGISTER.md` TG-P0-RESTORE-01.
- ~~**RESTORE-001**~~: **EJECUTADO el 2026-08-24** con "Restore to new project" a un proyecto efímero (`docs/audit/backup-drills/2026-08-24-drill.md`). RTO de la base: menos de 5 minutos. RPO real: hasta 24 h — **PITR no está habilitado**, es un add-on sin contratar, contra lo que afirmaba el registro de riesgos. No necesitó STAGING-001 para correrse.
- **test:concurrency**: script de test de concurrencia (no existe en `package.json`).
