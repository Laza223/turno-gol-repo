[effort: high]

## Estado de la Auditoría Fase 2 — Post-correcciones

Los 9 hallazgos originales fueron atendidos correctamente. Sin embargo, la introducción de nuevas decisiones (Super Admin, rol único admin, court_status binario) y algunos descuidos en la propagación de los fixes generaron nuevos hallazgos. Reporto solo los defectos:

| Archivo(s) | Error Lógico / Fuga Detectada | Solución Arquitectónica Propuesta |
|---|---|---|
| doc13 §3.7 (`tenant_staff_members`) ↔ doc13 §1 (`staff_role ENUM('admin')`) | **Default imposible**. La columna declara `role staff_role NOT NULL DEFAULT 'readonly'`, pero el ENUM `staff_role` fue reducido a un solo valor `'admin'`. Todo INSERT sin rol explícito fallará con `invalid input value for enum staff_role: "readonly"`. Bloquea el onboarding y la invitación de staff. | Corregir el default: `role staff_role NOT NULL DEFAULT 'admin'`. Además depurar el comentario obsoleto encima del CREATE TABLE que dice *"Marcelo es admin de Complejo A y recepcionista de Complejo B"* porque `receptionist` ya no existe como rol. |
| doc13 §3.2 (`bookings`) ↔ doc6 §3 (Booking State Machine) | **Trigger `enforce_booking_immutability` no aplica el Fix #9**. El trigger sigue siendo el original: solo bloquea UPDATE cuando `OLD.status IN ('completed','no_show','expired')`. Deja desprotegidos los estados finales `canceled_refunded` y `canceled_no_refund`, permitiendo resucitar una reserva cancelada a `confirmed`. El Fix #9 se documentó como patrón aplicativo (UPDATE condicional) pero el schema debería dar al menos esa segunda línea de defensa. | Extender el trigger a todos los estados finales y a todas las transiciones inválidas: `IF OLD.status IN ('completed','no_show','expired','canceled_refunded','canceled_no_refund') AND NEW.status != OLD.status THEN RAISE EXCEPTION ...`. Mantener la excepción actual que permite editar solo `notes_internal`. Nombre sugerido: `enforce_booking_terminal_states`. |
| doc13 §3.5 (`cash_flows`) ↔ doc13 §1 (`cashflow_type`) ↔ doc6 §9 | **Constraint ausente** e inconsistencia con la nueva decisión "sin gastos". doc6 §9 y el comentario del ENUM dicen "TurnoGol solo registra ingresos". El ENUM quedó en `('income','adjustment')` correctamente. Pero la `method payment_method NOT NULL` acepta `'other'` para `cash_flows` (heredado del ENUM global), y además **no hay ningún CHECK que impida insertar un `cash_flow` con `type = 'adjustment' AND category = 'booking'`** u otras combinaciones sin sentido (ej: `type='income' + category='no_show_correction'` que semánticamente es un ajuste). Esto rompe los reports (`GET /api/cash-flows/summary` separa income de adjustments por tipo). | Agregar constraint: `CONSTRAINT chk_cashflow_type_category CHECK ((type = 'income' AND category IN ('booking','product_sale','other')) OR (type = 'adjustment' AND category IN ('other','no_show_correction')))`. Alinear el comentario `'Movimientos de caja. income/expense por categoría.'` removiendo la mención a `expense`. |
| doc13 §3.13 (`daily_cash_closes`) ↔ doc13 §1 (`cashflow_type` sin `expense`) | **Columna `total_expense` huérfana**. La tabla define `total_expense INTEGER NOT NULL DEFAULT 0` y `balance = total_income - total_expense`, pero ya NO existen egresos en el sistema (el ENUM `cashflow_type` fue reducido a `income|adjustment`). Esa columna nunca se poblará con valores > 0, generando código muerto, reports engañosos y el riesgo de que un desarrollador agregue un `type='expense'` en el futuro rompiendo el ENUM. | Dos opciones: (A) dropear la columna y simplificar: `balance = total_income + total_adjustments`. Renombrar a `total_adjustments INTEGER NOT NULL DEFAULT 0` para reflejar la semántica real. (B) si se prefiere preservar compatibilidad futura, mantener la columna con `CHECK (total_expense = 0)` como documentación activa. Recomendada la opción A por claridad. |
| doc6 §2 (Court state machine diagram) ↔ doc13 §1 (`court_status ENUM('online','offline')`) | **Diagrama y texto desincronizados** tras la nueva decisión 2-estados. El ENUM y la tabla de estados muestran `online|offline` correctamente, pero el ASCII diagram justo arriba sigue mostrando `ACTIVE → INACTIVE → MAINTENANCE`. Además `doc6 §1` (atributos derivados del Tenant) dice `active_courts_count = COUNT de canchas con status='active'` — nunca va a matchear. También `doc8` US-ADM-001 sigue diciendo "status=`active`", "pongo una cancha en `maintenance`", "active/maintenance/inactive". | Reemplazar el ASCII diagram por una versión de 2 estados: `ONLINE ⇄ OFFLINE`. Corregir `active_courts_count` por `online_courts_count = COUNT(status='online')`. Actualizar las aserciones en doc8 US-ADM-001 para usar `online/offline`. Asegurar que doc7 Flujo 2 (precondición "cancha tiene status `active`") diga `'online'`. |
| doc13 §3.9 (`tenant_staff_members` comment) ↔ CLAUDE.md | **Comentarios del schema mencionan roles eliminados**. Los comentarios dentro del bloque SQL contradicen la decisión de rol único: *"Tabla de unión: un staff puede tener roles en N complejos. Marcelo es admin de Complejo A y recepcionista de Complejo B"*. Aunque el ENUM ya está bien, estos comentarios se copian literalmente a Drizzle y generan confusión en code review y en el IDE de cualquier dev. | Reemplazar comentarios: *"Un staff user puede ser admin de múltiples complejos. Solo existe el rol 'admin' en v1. Las zonas sensibles se protegen con tenant.settings.admin_pin."*. Propagar a doc8 US-ADM-003 (cambiar "contrato un nuevo recepcionista" por "invito un empleado como admin con PIN para zonas sensibles"). |
| CLAUDE.md ("Multi-tenancy") ↔ doc5 §5 | **doc5 sigue listando 12 tablas aisladas sin `tenant_staff_members`** y repitiendo que son 12. CLAUDE.md ya se actualizó a 12 aisladas correctas (incluye `tenant_staff_members` pero omite `daily_cash_closes` en algunas listas), pero doc5 §5 mantiene el listado viejo y NO menciona `tenant_staff_members` ni la RLS relacional en `players`/`staff_users`. Esto es la causa más probable de que un dev implemente un endpoint sin RLS nueva. | Sincronizar doc5 §5: lista única de 12 aisladas = {courts, bookings, abonados, payments, cash_flows, daily_cash_closes, products, tenant_staff_members, tenant_subscriptions, notifications, audit_logs, tenant_player_bans}. Agregar un párrafo explícito: "Además, `players` y `staff_users` son globales pero con **RLS relacional** (Fix #5). Ver doc12 §2." Garantizar que la nota *"CLAUDE.md y doc12 ganan en caso de discrepancia"* siga visible. |
| doc6 §8 (Payment `method`) ↔ doc6 §9 (CashFlow `method`) ↔ doc13 §3.5 | **Cardinalidad heredada del bug original, no del todo corregida**. CashFlow entidad en doc6 §9 declara `method enum 'cash' \| 'transfer' \| 'mercadopago'` (3 valores), pero doc13 usa el ENUM global `payment_method` (4 valores, incluye `'other'`). Al igual que el hallazgo original del Booking, hay dos fuentes de verdad. | Alinear doc6 §9 CashFlow.method = `'cash' \| 'transfer' \| 'mercadopago' \| 'other'` (o lo contrario: un ENUM `cashflow_method` de 3 valores separado). Recomendado opción A (reutilizar ENUM global) porque evita proliferación y porque `method='other'` puede usarse para ingresos atípicos (ajustes, regalos). |
| doc13 §4.3 (`enforce_price_snapshot_immutability`) ↔ §4.2 (`enforce_booking_immutability`) | **Dos triggers BEFORE UPDATE independientes sobre la misma tabla con lógica superpuesta**. Ambos disparan en cada UPDATE de `bookings`. Si se extiende `enforce_booking_immutability` para cubrir estados cancelados (hallazgo #2 de esta auditoría), parte de la comparación de `price_snapshot` queda duplicada. Además, el orden de ejecución entre triggers del mismo evento (BEFORE UPDATE) es alfabético por nombre — un refactor de naming puede cambiar semántica silenciosamente. | Consolidar ambos en un único trigger `enforce_booking_invariants` que valide: (1) transiciones de estado contra matriz permitida, (2) inmutabilidad de `price_snapshot`, (3) inmutabilidad de estados finales excepto `notes_internal`. Un solo trigger, una sola fuente de verdad, orden de ejecución determinista. |
| doc13 §3.11 `tenant_player_bans` (uq_tenant_player_active_ban) | **Índice parcial con `NOW()` es inválido en PostgreSQL**. El predicado `WHERE (banned_until IS NULL OR banned_until > NOW())` **no se puede crear**: PostgreSQL rechaza `NOW()` en un índice parcial porque no es IMMUTABLE. El error esperado es `ERROR: functions in index predicate must be marked IMMUTABLE`. Este es el hallazgo más crítico de esta auditoría porque la migration del Fix #6 NO correrá en Supabase. | Reemplazar por columna generada + índice parcial sobre valor estático: `ALTER TABLE tenant_player_bans ADD COLUMN is_active BOOLEAN GENERATED ALWAYS AS (banned_until IS NULL OR banned_until > '2099-12-31'::timestamptz) STORED;`. Esta opción NO resuelve el caso vencido. **Opción robusta**: (A) eliminar la constraint como DB-level y enforced-en-aplicación (verificar ban activo antes de INSERT en un SELECT ... FOR UPDATE + job nocturno que borra/archiva bans vencidos viejos). (B) usar trigger `BEFORE INSERT` que rechace si ya hay un ban vigente para ese `(tenant_id, player_id)` con `banned_until IS NULL OR banned_until > NOW()`. Recomendada la opción B: reemplaza el índice parcial por un trigger que valida contra NOW() dinámicamente. |
| doc13 §3.13 (`daily_cash_closes`) policy UPDATE/DELETE ausente ↔ §3.10 (`audit_logs`) | **Inconsistencia en estrategia de inmutabilidad**. Ambas tablas son declaradas inmutables, pero `audit_logs` usa `REVOKE UPDATE, DELETE ON audit_logs FROM turnogol_app` + ausencia de policies, mientras que `daily_cash_closes` dice `-- NO UPDATE ni DELETE` pero NO aplica el REVOKE. Con RLS habilitado y sin policy de UPDATE/DELETE, el rol `turnogol_app` NO podrá ejecutarlas (correcto), pero el service role sí → riesgo de corrupción silenciosa en soporte/runbook. Además falta el CHECK/constraint que impida re-insertar otra fila para el mismo (tenant_id, date) en el mismo segundo (ya existe `uq_daily_close_per_tenant`), OK. | Agregar `REVOKE UPDATE, DELETE ON daily_cash_closes FROM turnogol_app;` explícito como en audit_logs, con comentario: "Correcciones post-cierre se hacen solo vía service role + nuevo cash_flow compensatorio documentado en runbook". Unificar el patrón en doc12 como "tablas INSERT-ONLY": {audit_logs, daily_cash_closes, price_versions, processed_webhooks}. |
| doc13 §3 — `system_admins` referenciada pero NO implementada | **Tabla fantasma**. CLAUDE.md, doc6 §Mapa-de-Relaciones y doc13 §Resumen-de-Tablas listan `system_admins` como tabla del sistema v1. NO existe `CREATE TABLE system_admins (...)` en doc13, ni está en el Migration Order (§10), ni tiene policies, ni tiene comentarios definiendo campos. Una feature declarada como "MUY importante" (P2.2 DECISIONES) sin schema es un gap bloqueante para Claude Code. | Crear sección §3.14 `system_admins`: `CREATE TABLE system_admins (id UUID PK, email TEXT UNIQUE NOT NULL, first_name TEXT, last_name TEXT, status staff_status DEFAULT 'active', mfa_secret TEXT, last_login_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW());`. Agregar `ALTER TABLE system_admins ENABLE ROW LEVEL SECURITY;` + policy restrictiva `CREATE POLICY system_admin_self ON system_admins USING (id = current_setting('app.current_system_admin_id', true)::UUID);`. Documentar en doc12 que existe una 3ª variable de contexto (`app.current_system_admin_id`) y que el service role es quien lo setea. Agregar al Migration Order §10 y al Resumen §7. |
| doc13 §3.2 (`bookings.payment_method`) ↔ doc6 §3 | **Ambigüedad semántica pendiente de resolución**. Se agregó la columna (Fix #2 opción A) pero quedó el campo `payment_id` en paralelo sin documentar la regla de consistencia: ¿puede haber `payment_method='mercadopago' AND payment_id IS NULL`? ¿O `payment_method='cash' AND payment_id IS NOT NULL`? El schema no lo impide. | Agregar CHECK constraint: `CONSTRAINT chk_booking_payment_consistency CHECK ((payment_method = 'mercadopago' AND payment_id IS NOT NULL) OR (payment_method IN ('cash','transfer','other') AND payment_id IS NULL) OR (payment_method IS NULL AND deposit_status = 'not_required'))`. Documentar en doc6 §3 Invariantes: "Si `payment_method='mercadopago'`, debe existir `payment_id`. Para cash/transfer/other (reservas manuales), `payment_id IS NULL` y el cobro se refleja solo en `cash_flows`." |

**Findings resueltos correctamente**: los 9 originales (ENUM in_process, policy player_self_insert, Realtime TO authenticated, RLS relacional en players/staff_users, índice parcial concepto correcto, policy player_own_bans_select, policy player_self_ptr_insert, patrón UPDATE condicional en §4.4). Felicitaciones por ese trabajo.

**Findings críticos nuevos a resolver antes de cualquier migration**: #1 (default 'readonly'), #10 (NOW() en índice parcial), #12 (tabla system_admins inexistente). Estos tres bloquean deploy.