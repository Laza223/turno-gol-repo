---
name: convenciones-stack
description: Usar cuando se escribe o modifica código que toca la DB (Drizzle, schema, migraciones, jsonb), aislamiento multi-tenant/RLS, background jobs de pg-boss, pagos MercadoPago (mock mode, webhooks, OAuth), Server Actions o Route Handlers en TurnoGol.
---

# Convenciones del stack

## Invariantes globales

- Montos en **centavos ARS, integer**. Nunca decimal ni float.
- Timestamps **UTC en DB**; conversión a ART solo en frontend.
- UUIDs como PK. TypeScript strict, jamás `any`.
- Vocabulario de enums: **`canceled` con una L** (`canceled_refunded`, `canceled_no_refund`). `cancelled` no existe en este repo.
- Turnos de 60 min fijos: `SLOT_DURATION_MINUTES` en `src/shared/constants.ts:8`. El slot 23:00→medianoche se guarda con `time_end='24:00'`. Cierres post-medianoche: usar los helpers de `src/shared/time/operating-day.ts` — nunca reimplementar esa aritmética.
- **Reimplementar el cierre falla en SILENCIO y hacia un solo lado: descarta el día entero.** Cualquier `close <= open` calculado sin `tenants.closes_next_day` da una ventana vacía, y una ventana vacía no tira error: la UI no dibuja el día, el guardado no emite reglas para ese día y el validador de cobertura no exige nada. Un complejo abierto de 08:00 a 01:00 quedó con lunes a viernes sin precio y sus canchas irreservables 5 de 6 días, sin un solo error en ningún lado. Antes de escribir cualquier ventana horaria, `effectiveCloseMins`; auditá las que ya existen con `grep -rn "close.*<=.*open\|=== '00:00' ? 24" src/`. (pasó: 2026-09-07, `pricing-grid.ts` y `validatePricingRulesCoverage`.)
- **Las franjas de precio se etiquetan por día OPERATIVO, no calendario.** El turno de la 01:00 del sábado pertenece al viernes si el complejo cerró pasada la medianoche, y así lo buscan `calculatePrice`, `priceForSlot` y `priceForDateSlot`. Una regla etiquetada con el día calendario siguiente no la encuentra nadie: tarifa mal o directamente deja la franja sin precio. Es la misma regla que ya vale para `bookings.date`, y vale igual para todo lo que se guarde particionado por día. (pasó: 2026-09-07, `uniformRulesFromOpeningHours` era el único lugar del repo con el criterio invertido.)
- Mutaciones de UI = **Server Actions**. Route Handlers SOLO para webhooks de MP, `/api/public/*` y auth callbacks.
- **El día NUNCA se deriva en UTC.** `new Date().toISOString().slice(0,10)` devuelve el día siguiente entre las 21:00 y medianoche ART: un default de listado o un bucketing de métricas que pase por ahí salta de día 3 horas por noche. Usar `artTodayStr()` (`@/shared/dates/art`) para "hoy", `artDateOf` (`@/shared/time/art-date`) para el día ART de un instante, u `operatingDateOf` (`@/shared/time/operating-day`) cuando el día es el OPERATIVO del complejo. Bloqueado por ESLint (`no-restricted-syntax`).

## Plata en la UI

- **Todo campo de monto es `MoneyInput`** (`@/components/ui/money-input`), nunca un `type="number"` suelto: pesos enteros, separador de miles mientras se tipea y relectura en palabras arriba de `MONEY_WORDS_THRESHOLD_CENTS`. El parseo vive en `@/lib/money` y es la única fuente.
- **El campo se reformatea en CADA tecla, así que el parser tiene que sobrevivir a strings a medio tipear** — no alcanza con que parsee bien el string final. (pasó: `parsePesosToCents` borraba todo lo no-dígito, así que la coma se comía en el acto y los dígitos de los centavos se pegaban al entero en la tecla siguiente: `1500,50` terminaba valiendo $150.050. Afectaba los 7 campos de plata del admin, incluido el precio de cancha, que publicaba el error 100× en el portal público. El test que lo agarra tipea tecla por tecla; los que parseaban el string entero pasaban en verde.)
- **Desambiguación es-AR**: el último separador es decimal solo si lo siguen 0, 1 o 2 dígitos; con 3 es grupo de miles (`35,000` son treinta y cinco mil). El caso de CERO dígitos es el instante en que se acaba de tipear la coma y es tan importante como los otros.
- **Escribir en un día de caja ya cerrado exige `type='adjustment'`** vía `allowClosedDay` en `CreateCashFlowInput`, y ninguna Server Action lo expone. Ver `docs/decisions/2026-08-28-sena-cobrada-con-la-caja-cerrada.md`. (pasó: el `catch` de `DayAlreadyClosedError` avisaba al dueño pero no escribía nada, así que la reserva decía "pagada" y la plata no figuraba en Caja.)
- **Saltear el chequeo de cierre no es saltear `assertDayOpen`**: ahí adentro está el `pg_advisory_xact_lock` que serializa contra un `closeDailyRegister` concurrente. Pasarle el flag, no evitar la llamada.
- **El cash_flow de la seña tiene TRES emisores, no uno**: `recordManualBookingDepositCashFlow` (booking.service.ts), `recordManualDepositCashFlow` y `recordDepositCashFlow` (payment.service.ts). Están duplicados a propósito, así que un arreglo en uno NO llega a los otros — listarlos con `grep -rn "depositCashFlowDescription" src/` antes de dar por cerrado cualquier bug de señas. (pasó: el fix de la caja cerrada tapó una sola de las tres puertas y se dio por cerrado; las otras dos siguieron tirando la plata al vacío hasta que un barrido de la clase las encontró.)
- **Un `mockRejectedValue` pelado sobre el colaborador que falló también hace fallar el camino de recuperación**, y el test rojo parece un bug del fix. El mock tiene que imitar la REGLA, no el resultado: `mockImplementation` que rechaza sólo mientras no venga el flag de escape. (pasó: `createCashFlow` mockeado con `mockRejectedValue(new DayAlreadyClosedError())` rechazaba también la escritura del ajuste, que es justo lo que el caso prueba.)

## Rutas públicas y status HTTP

- **Un `loading.tsx` cubre su segmento Y todo lo que cuelga debajo.** Abre un `<Suspense>`, Next arranca a streamear con el 200 ya en los headers, y cualquier `notFound()` posterior cambia el cuerpo pero no el status: soft-404 indexable. Un `layout.tsx` queda fuera del boundary de SU segmento, **no del de un ancestro**. (pasó dos veces: primero con las pages de `/{slug}/*`, arreglado moviendo el gate a `[slug]/layout.tsx`; después con un gate en `[slug]/torneos/layout.tsx`, que seguía adentro del boundary del `loading.tsx` del perfil. La salida fue encerrar la page del perfil y su skeleton en un grupo `(perfil)`, que no cambia la URL y deja a cada hermano decidiendo su status.)
- **El status se mide, no se deduce**: `fetch(url, {redirect:'manual'})` repetido, y con control negativo (la misma URL en la condición contraria). Un cuerpo que dice "no encontrado" no prueba que el status sea 404, y una sola medición puede caer del lado bueno por timing de compilación.

## Server Actions y forms

- **Retorno**: `ActionResult` de `@/shared/types/action-result` — `{ success: true } & TExtra | { success: false; error: string }`. Nada de `{ success: boolean; error?: string }`: esa forma no discrimina, deja compilar un fallo sin motivo y termina mostrándole al usuario un error genérico donde había uno real.
- **La action llega por PROP tipada** (`import type`), no por import de valor: importar un módulo `'use server'` desde un componente cliente arrastra drizzle y `node:async_hooks` al bundle del browser y rompe Storybook.
- **Nada de wrapper `ActionForm`**: el patrón del repo es `useActionState` + action por prop + `SubmitButton` de `@/components/ui/submit-button` (`useFormStatus` → deshabilitado + `aria-busy`, evita el doble submit). Los forms de auth tienen botón propio a propósito (sistema visual distinto).
- **`z.coerce.number()` NUNCA a pelo sobre un campo opcional de FormData**: un input vacío llega como `''` y `Number('')` es `0`, así que el campo "sin cargar" se guarda como cero **válido** — pasa el rango, pasa el tipo, no rompe ningún test que solo mire `success`. Patrón correcto: `z.string().trim().optional().transform((v) => (!v ? null : Number(v)))` y recién ahí validar, como `optionalCoordinate` en `modules/tenants/tenant.schema.ts`. Si el par de valores solo tiene sentido junto, sumar un `.refine()` de "ambos o ninguno" **y** el CHECK equivalente en la base: todo el lado lector suele asumir el par completo y nadie sabe renderizar la mitad. (pasó: 2026-09-07, carga de la ubicación del complejo — sin eso, un complejo sin punto marcado quedaba en la latitud y longitud cero, o sea en el Golfo de Guinea, apareciendo a diez mil kilómetros del jugador con distancia calculada y todo.)
- **El error SIEMPRE se muestra**: un form que descarta el `{ success: false }` deja al usuario apretando Guardar sin feedback. `ConfirmDialog` ya lo hace solo si el handler devuelve el `ActionResult`.
- **Un `return { success: false }` es INVISIBLE**: no es una excepción, así que no llega a Sentry, no queda en `analytics_events`, y `logger.warn` termina en un `console.log` que nadie lee. Cuando la rama que rechaza puede dejar a un complejo sin poder operar, emitir señal antes del return: `captureMessage` de `@/lib/sentry` en el server, `track.*` de `@/shared/observability/breadcrumbs` en el cliente (`@/lib/sentry` importa `after` de `next/server`, es server-only y rompe el bundle del browser). Si hay un gate espejo del lado cliente, ése es el que más importa: corta ANTES de llegar a la action, así que es donde el usuario se atasca de verdad. (pasó: 2026-09-07 — un complejo real quedó trabado sin poder cargar canchas y nos enteramos por WhatsApp; el servidor sabía exactamente por qué lo bloqueaba y no lo dijo en ningún lado.)
- **`supabase.auth.updateUser` actúa sobre la sesión REAL de cookies, no sobre el tenant que resolvió el guard.** Bajo impersonación de super admin eso escribe sobre la cuenta del super admin. El bug no se ve leyendo la action de arriba hacia abajo: el pre-check de unicidad usa el `staffUserId` correcto y recién la escritura final se va a la otra cuenta. Toda action que mute credenciales (email, contraseña) se bloquea con `getImpersonationSession()`; no se "redirige" la escritura al tenant, porque esa API no permite actuar como otro usuario sin service role y habilitarlo dejaría a un super admin cambiándole el acceso a un cliente sin su consentimiento. Las actions que escriben con `updateTenant(tenant.id, ...)` sí son seguras: ésas son la impersonación funcionando como corresponde. Buscá los casos con `grep -rn "auth.updateUser" src/`. (pasó: 2026-09-07, Configuración > Perfil y /reset-password.)

## Efectos externos y transacciones

- Una llamada a MercadoPago, Resend o R2 **NUNCA va adentro de una transacción SQL**. Si la tx aborta después de la llamada, el reintento duplica el efecto externo (pasó: doble reembolso) y encima la conexión queda tomada durante toda la latencia de red. Patrón: preparar en la tx → commitear → llamar afuera → registrar el resultado en una segunda tx (Saga).
- Al revés también: no commitear una escritura local dando por hecho que el efecto externo salió bien (pasó: alta de staff commiteada con el email de invitación fallado).
- **Un `try/catch` agregado a un middleware que compone DENTRO de `withTenantContext`/`withPlayerContext` convierte un throw en un valor resuelto, y `db.transaction` hace COMMIT en vez de ROLLBACK** aunque el handler haya fallado a mitad de una escritura multi-paso — sin ningún cambio visible en la respuesta al cliente, así que no lo delata ningún test que solo mire el body HTTP. El catch de "loguear + devolver `internal()`" solo es seguro en la capa que envuelve la llamada a `withTenantContext(...)` DESDE AFUERA (ahí el rollback de Drizzle ya corrió antes de que la excepción llegue al catch) — nunca en una capa intermedia que corre adentro de esa tx (`with-role.ts` es justo eso: compone siempre dentro de `withTenant`). Antes de agregar un catch a cualquier wrapper de `src/server/middleware/`, verificar con `grep -n "withTenantContext\|withPlayerContext" src/server/middleware/*.ts` cuál de los dos lados de la tx es. (pasó: 2026-09-02, ver `docs/gtm/ejecucion/10-aprendizajes.md` — un fix de mensajes de error le agregó try/catch a los 4 wrappers por igual; un revisor adversarial de contexto fresco lo agarró antes de mergear, corrigiéndolo solo en `with-role.ts`.)

## Drizzle

- **jsonb**: importar `jsonb` desde `src/shared/db/jsonb.ts`, NUNCA desde `drizzle-orm/pg-core`. El de pg-core doble-codifica con postgres-js y corrompe el dato at-rest (queda string escalar; `columna->>'campo'` devuelve NULL).
- Merge de jsonb en `` sql`` ``: pasar el objeto crudo, NUNCA `JSON.stringify` (mismo doble-encode). Ojo con `||` sobre un array: concatena en vez de reemplazar.
- Subquery correlacionado dentro de `` sql`` `` en `.select()` no califica columnas de la tabla externa → usar LEFT JOIN con tabla derivada.
- **Migraciones**: fuente de verdad `src/shared/db/migrations/NNN_nombre.sql` (numeración secuencial; la última se lee con `ls src/shared/db/migrations | tail -1` — no la fijes acá, un número que se incrementa solo queda viejo y se cita como vigente). Después de crear una: `pnpm db:sync-supabase` genera el espejo en `supabase/migrations/`, y `pnpm supabase:reset` la aplica localmente. NUNCA editar una migración ya existente. Detalle en `docs/operations/MIGRATIONS.md`.
- **`numeric` sin `mode` entra y sale como STRING**, mientras el dominio habla números. Las dos puntas fallan calladas: en lectura, `Number(null)` es `0` (una columna vacía se vuelve cero en vez de seguir vacía); en escritura, un `.set({ ...data })` con spread ciego manda el tipo equivocado. Y ojo con el spread aunque el tipo cierre: un `update` parcial que incluya la clave con `?? null` **borra el valor guardado en cada guardado de cualquier otra pantalla** que llame al mismo service. Patrón: destructurar la columna fuera del spread y agregarla solo `if (valor !== undefined)`, con la conversión (`.toFixed(escala)`, no `String(n)` — evita la notación exponencial) concentrada en el service. Al agregar la columna al tipo de entrada, TypeScript rompe en el spread y te obliga a resolverlo: no lo silencies con un `as`. (pasó: 2026-09-07, coordenadas del complejo — el test que lo agarra es el que afirma que un `update` de OTRO campo no incluye la clave.)
- **Drizzle 0.45 envuelve todo error de postgres-js en `DrizzleQueryError`: `code`/`constraint_name` viajan en `err.cause`, nunca en `err`.** Un `catch` que mira `err.code` en el nivel superior nunca ve un 23505/23503 si el INSERT que falló vino de `.insert(tabla).values().returning()` (query builder) — sí lo ve si vino de `tx.execute(sql\`...\`)` crudo, porque ahí no hay wrapping. Helper único que camina la cadena de `cause`: `isUniqueViolation`/`isForeignKeyViolation` en `src/shared/db/pg-errors.ts` (movido de `modules/tournaments/` acá porque ya lo usan 2+ módulos). (pasó dos veces: primero en tournaments, cazado por un test de integración y no por el typecheck; después en `cashflow/daily-close.service.ts` — el mismo patrón naive sobrevivió sin que nadie lo mirara hasta la campaña de mutación de 2026-09-03, porque el test que lo debía agarrar solo afirmaba "4 de 5 fallaron", no CON QUÉ error.)
- **`DrizzleQueryError.message` incluye los parámetros enlazados** (`Failed query: <sql>
params: <params>`, ver `node_modules/drizzle-orm/errors.js`). Loguear `err.message` de una query fallida escupe los VALORES: credenciales cifradas, datos personales, montos. El tapado de datos sensibles previo a Sentry compara nombres de clave, no contenido, y la clave suele ser `error` → pasa entero. Ya no hace falta acordarse en cada sitio: `logger` recorta los `params:` de TODA cadena que loguea (`redactQueryParams`, `shared/lib/`), y el tapado previo a Sentry mira contenido además de nombres de clave. Lo que sigue siendo tuyo: no armar el mensaje a mano por fuera del logger. (pasó: 2026-09-05, auditoría de aislamiento — el cron de renovación de credenciales de MercadoPago mandaba los dos textos cifrados a Sentry si su UPDATE fallaba; el arreglo fue en el logger y no en los 49 sitios que escriben `error: err.message`.)

## Multi-tenant / RLS

- Contexto por request (helpers en `src/shared/db/client.ts`, hacen el `SET LOCAL` correcto): staff → `withTenantContext(tenantId, tx => ...)`; jugador → `withPlayerContext(playerId, ...)` (cross-tenant: NO setear tenant_id). Jamás `SET` sin LOCAL.
- **Defensa en profundidad**: además de RLS, SIEMPRE filtro explícito `WHERE tenant_id = ...` / `WHERE player_id = ...`. En el entorno de TESTS la base apunta a un rol con BYPASSRLS → RLS no aplica y el filtro explícito es la única barrera (así se cerraron leaks reales de revenue cross-tenant). En dev la app sí entra con el rol restringido; verificá con qué rol estás midiendo antes de creerle a un verde: `docker exec supabase_db_TurnoGol psql -U postgres -d postgres -Atc "SELECT rolname, rolbypassrls FROM pg_roles WHERE rolname IN ('postgres','turnogol_app','turnogol_worker')"`. (pasó: 2026-09-03, campaña de mutación — el patrón "INSERT ... ON CONFLICT (client_idempotency_key) DO NOTHING" + SELECT de fallback para recuperar la fila apareció 6 veces sin `AND tenant_id = ...` — `cashflow.service.ts` y sus dos gemelos en `canteen-tab.service.ts`/`canteen-sale.service.ts` — porque el índice único de `client_idempotency_key` es GLOBAL, no por tenant: al copiar el patrón de un módulo a otro, el WHERE se copió incompleto las tres veces.)
- **Workers/jobs**: nunca `getDb()` — usar `getWorkerDb()`/`getWorkerSql()` (pool con `WORKER_DATABASE_URL`, rol BYPASSRLS) para barridos cross-tenant. Una mutación sobre UN tenant conocido vuelve por `withTenantContext` en el pool normal. **Bloqueado por ESLint** en `src/shared/jobs/**` (bloque `turnogol/jobs-worker-pool`), porque el modo de falla es silencioso: el pool de la app no tira error, devuelve cero filas y el job reporta éxito.
- **Un test de RLS que asume el rol con `SET LOCAL ROLE` no es el camino de producción.** `withTenantContext` sólo setea el GUC de contexto: nunca cambia de rol, así que hereda el rol de la conexión. Si la conexión es superusuario o BYPASSRLS, todo pasa en verde sin haber probado nada. El arnés que entra con LOGIN real del rol restringido está en `tests/integration/isolation-app-role.test.ts` (`pnpm test:isolation:rol-real`, bloqueante en CI); usa el gancho `TEST_ADMIN_DATABASE_URL` para que la siembra siga por el pool superusuario. (pasó: 2026-09-05, auditoría de aislamiento — ninguna suite pasaba por `withTenantContext`; el mismo SELECT cross-tenant devolvía 1 fila como `postgres` y 0 como `turnogol_app`.)
- **`reviews` tiene permisos POR COLUMNA: `player_id` no se puede leer desde el rol web** (migr. 084). Es la única tabla del esquema con recorte por columna, y existe porque su policy de lectura es `USING (true)` a propósito —el portal público muestra reseñas sin sesión— y RLS es por FILA: no puede esconder una columna. Consecuencias prácticas: un `.returning()` pelado sobre `reviews` rompe en runtime (usar lista explícita de columnas), y **una columna nueva en esa tabla no queda legible hasta que una migración la agregue al GRANT**. (pasó: 2026-09-05, hallazgo H-2 — sin contexto se leían las reseñas de todos los complejos con el identificador del jugador, y la única barrera era la proyección del código.)
- **El rol web no puede BORRAR ninguna tabla global ni escribir el catálogo comercial** (migr. 085: `tenants`, `players`, `staff_users`, `processed_webhooks`, `plans`, `price_versions`). La baja de un complejo es un estado, no un DELETE. Lo que SÍ sigue abierto y es un límite conocido: la app puede MODIFICAR la fila de complejo de otro, porque edita la propia y esa tabla no tiene RLS que las distinga — el filtro explícito por `tenant_id` es la única barrera ahí.
- **Una revocación nueva va en UN solo lugar: la migración.** `ensureRoles` (`tests/helpers/tenant.ts`) ya no le otorga nada a `turnogol_app` — los permisos del rol salen únicamente de las migraciones. Hasta el 2026-09-07 re-otorgaba las cuatro operaciones sobre todas las tablas y después re-aplicaba a mano la lista acumulada de revocaciones, así que una que llegara a una migración y no al helper dejaba al rol con permisos que producción no da (hallazgo H-6). Consecuencia práctica: **la base local tiene que tener las migraciones aplicadas**; si faltan, el caso 0.2 del arnés del rol real se pone rojo con la diferencia exacta.
- **Un arnés que deriva su criterio del sistema que audita se adapta a la rotura y da verde.** Lo que se afirma tiene que estar declarado en el test y contrastado contra lo leído, no leído y usado como expectativa. (pasó: 2026-09-05 — la primera versión clasificaba "lectura abierta" leyendo `pg_policy`; al abrir a mano la policy de SELECT de `payments` para probar el arnés, la tabla se reclasificó sola como excepción de diseño y los 140 casos siguieron en verde. Corolario: toda suite de seguridad necesita su prueba de mutación, y la prueba se corre ANTES de creerle al verde.)
- **Un `player_id` que llega del cliente se valida contra `playerBelongsToTenant` (`modules/relationships/ptr.service.ts`) antes de escribirlo.** La policy de alta sobre `bookings` (y sobre casi todo) sólo mira `tenant_id`, no `player_id`, y `ensurePTR`/`applyNoShowStrike` crean la fila de `player_tenant_relationships` sin precondición — que es exactamente la condición de `staff_can_see_related_players` sobre `players`. O sea: aceptar un identificador ajeno le regala al complejo la llave para leerle nombre, correo y teléfono a alguien que nunca fue su cliente. (pasó: 2026-09-05, hallazgo H-1 — el guard existía privado en `contact-link.service.ts` con un comentario que describía el ataque, y ni la carga manual de reservas ni el alta de abonado lo llamaban. Se movió a `ptr.service.ts` para que haya UNA copia. El baneo manual tenía el mismo agujero sin crear la relación, y se cerró igual: el guard vive en el service, no en la Server Action, justamente porque el hallazgo original nació de un guard que existía en un solo llamador.)
- **Roles staff**: el rol NUNCA sale del JWT. Guards de `src/modules/staff/guards.ts`: `requireOperatorStaff()` (admin+manager: grilla, reservas, caja, jugadores) y `requireAdminStaff()`/`requireAdminStaffAction()` (solo admin: configuración, equipo, MP/facturación).
- **Checklist tabla tenant-aislada nueva**: columna `tenant_id` + policy RLS + `FORCE ROW LEVEL SECURITY` + DELETE manual en `src/shared/jobs/workers/data-retention-cleanup.worker.ts` (el wipe NO cascadea: el tenant se soft-anonimiza, no se borra la fila) + caso en `tests/integration/isolation.test.ts` + entrada en la lista declarada de `tests/integration/isolation-app-role.test.ts` (si no, su caso 1.0 se pone rojo: contrasta la lista contra las tablas con `tenant_id` que hay en la base).
- Nunca exportar helpers de servidor desde un archivo `'use client'`: vitest pasa igual y revienta solo en runtime real.

## pg-boss

- Workers en `src/shared/jobs/workers/`, nombres de cola en `src/shared/jobs/queue-names.ts`, proceso con `pnpm jobs:start`.
- Todo job debe ser idempotente (webhooks deduplican vía `processed_webhooks` / idempotency keys en cash_flows).
- Efectos con horario (push en madrugada 00–08): patrón `startAfter` de `src/modules/notifications/push-quiet-hours.ts`, nunca sleep.
- **Un estado intermedio que sólo se sale dentro de la misma invocación del worker es una fila que se puede clavar para siempre.** Si el proceso muere en el medio (deploy de Railway, OOM), el barrido —que filtra por el estado de entrada— no la vuelve a ver nunca: falla en silencio, sin reintento ni alerta. Todo estado así necesita su marca de tiempo propia y un rescate al inicio del barrido. (pasó: 2026-09-07, AUD-15 — `notifications` en `'sending'`; el rescate está en `reclaimStalledSendingNotifications`.)
- **Para medir "hace cuánto está en este estado" hace falta una columna que se escriba AL ENTRAR al estado.** `created_at` y la marca del alta no sirven: no se mueven en la transición, así que una fila vieja en la cola cuyo trabajo arrancó recién se reclamaría en vuelo — y para un envío de mail eso es mandarlo dos veces. Verificá que la columna exista antes de construir sobre un hallazgo que la nombra: el informe de AUD-15 proponía `updated_at` y `notifications` no la tiene. Racional: `docs/decisions/2026-09-07-lote3-auditoria-integral.md` D2.

## MercadoPago

- Dev/E2E: `MP_MOCK_MODE=1` en `.env.local` activa `LocalMockGateway` (`src/modules/payments/mock-mp.ts`) → checkout local en `/mock-mp/checkout`, webhook procesado inline sin pg-boss. Hard-gated a no-producción: no intentar "arreglar" ese gate.
- NUNCA llamar la API real de MP desde tests ni dev. Replay manual de webhooks: `pnpm webhook:replay`.
- Webhooks entrantes verifican firma HMAC en `src/modules/payments/webhook-auth.ts` (gotcha: `data.id` va en minúsculas al construir el manifest).
- **El manifiesto tiene ventana de antigüedad, así que ningún fixture puede firmar con un `ts` congelado.** Cada archivo que firma un webhook tiene que usar el instante actual. Los encontrás todos con `grep -rln ';ts:' tests/ scripts/`. (pasó: 2026-09-07, hardening de la auditoría integral — al agregar la ventana, cinco archivos de test que firmaban con un `ts` fijo de 2021/2023 pasaron a dar 401; dos aparecieron recién al correr la suite completa, porque correr "los tests de webhook" no alcanza para encontrarlos.)
- **El `ts` del manifiesto se normaliza a milisegundos antes de compararlo.** MercadoPago lo documenta en milisegundos, pero `scripts/replay-mp-webhook.ts` y `scripts/probe-mp-webhook-signature.ts` firman con `Math.floor(Date.now()/1000)` — segundos. Restar sin normalizar daría una antigüedad de décadas y rechazaría TODOS los avisos a la vez, incluida la sonda con la que se diagnostica producción. Toda comparación de antigüedad sobre un `ts` externo asume unidad: normalizala.
- **La ventana falla ABIERTA a propósito**: `ts` ilegible o futuro pasa; sólo rechaza con certeza de vejez, y cuando rechaza loguea `error` (no `warn`) para que llegue a Sentry. Rechazar un aviso legítimo cuesta un pago no registrado; repetir uno cuyas ramas ya son idempotentes no cuesta nada. Si ese log aparece en producción, la ventana quedó corta: ensancharla, no investigar al emisor. Racional: `docs/decisions/2026-09-07-lote3-auditoria-integral.md` D3.
- **El preapproval se crea en el momento en que el complejo toca "Activar plan", y sin `auto_recurring.start_date` MercadoPago puede cobrar YA** — aunque al complejo le queden meses de prueba. `subscribe()` manda `tenants.trial_ends_at` como `start_date`, y lo omite si esa fecha ya pasó o es nula (ahí no hay nada que diferir). Se usa `start_date` y no `free_trial` porque este último se define por duración fija y no puede alinearse con un trial que soporte extendió a una fecha arbitraria; además el SDK sólo tipa `free_trial` en las respuestas, no en el body de creación. La fecha correcta es SIEMPRE `tenants.trial_ends_at`, nunca `tenant_subscriptions.current_period_end`: esa segunda columna no se mueve cuando soporte extiende el trial. (pasó: 2026-09-07 — un complejo con 90 días de prueba quedó con una suscripción viva que podía cobrarle antes; ver `docs/decisions/2026-09-07-vagon-deportivo-horarios-y-cobro.md` D1.)
- Llamadas salientes pasan por `withCircuitBreaker` (`src/modules/payments/mp-breaker.gateway.ts`) — no bypassearlo con un fetch directo.
- Tokens OAuth por tenant (`mp_access_token`/`mp_refresh_token`) están cifrados at-rest y el cifrado es la ÚNICA barrera (tabla `tenants` es global, sin RLS). Jamás loguearlos ni devolverlos en payloads.
