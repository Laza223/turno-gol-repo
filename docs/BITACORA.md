# Bitácora

Una línea por cierre de sesión, escrita sola por el hook de cierre.
Solo historia — lo que se hizo, que no caduca. Lo vigente vive en `.claude/ESTADO.md`;
lo que está abierto se consulta en los PRs y el CI, no se escribe acá.

- **2026-08-27 21:28** `main` — dos decision docs commiteados y pusheados al PR, cerrando el porqué de lo de hoy
- **2026-08-27 21:35** `main` — 175 hallazgos de drift, 158 auto-corregidos y verificados en fresco, 65 gaps documentados, 3 contradicciones entre docs, 2 decisiones huérfanas. Reporte completo: [docs/audit/DOC_DRIFT_2026-08-27.md](docs/audit/DOC_DRIFT_2026-08-27.md)
- **2026-08-27 21:55** `main` — CI verde sobre un main que incluye el cambio de TLS; producción validando la cadena en los dos runtimes y migración 080 aplicada, todo ya confirmado antes
- **2026-08-28 01:23** `main` — 27/29 REQUIERE INPUT resueltos y aplicados; reporte actualizado en [docs/audit/DOC_DRIFT_2026-08-27.md](docs/audit/DOC_DRIFT_2026-08-27.md); `pnpm typecheck` limpio
- **2026-08-28 02:37** `main` — combiné 3 de las sesiones sueltas en 1 solo PR (trial vencido + historial pagos SaaS + ledger deuda técnica), typecheck limpio, pusheado
- **2026-08-28 05:37** `chore/combinar-3-sesiones-2026-08-27` — verifiqué contra la infra real que esos cuatro son los únicos pendientes: apex sigue en A, DMARC sigue en `p=none`, el MCP de Supabase solo ve la org de producción. Todo el resto está cerrado o es riesgo aceptado con el motivo escrito
- **2026-08-28 15:48** `main` — [PR #250](https://github.com/Laza223/turno-gol-repo/pull/250) mergeado a main (squash), rama borrada. CI verde completo (Lint & Types, Unit, Integration & Isolation, Stories ×6, Semgrep, gitleaks) para el commit correcto
- **2026-08-28 18:44** `main` — react-scan y dependency-cruiser andando, CI advisory agregado, scan real corrido y triageado a mano (evidencia arriba)
- **2026-08-28 18:59** `main` — GitHub App instalado, acotado a `turno-gol-repo`, 2FA confirmado por vos
- **2026-08-28 19:02** `main` — Socket Security activo en `turno-gol-repo`, acotado, permisos revisados; los 3 confirmados en última versión
- **2026-08-28 19:08** `tooling/react-scan-depcruise-socket` — 72 warnings documentados en `docs/tech-debt.md`, commit + push + PR #253 (solo mis 6 archivos — no arrastré lo ajeno)
- **2026-08-28 19:20** `tooling/react-scan-depcruise-socket` — Medí el desvío contra el repo real: 37 commits desde el 25/8, ninguno tocó un ensayo de plata. Confirmé con evidencia que los dos fixes de plata del QA ya están en producción (deploys `READY`) y que el bug de los 100× no dejó datos corruptos — los dos complejos publican $100/turno. Escribí [GUION-ENSAYOS-PLATA-2026-08-28.md](docs/qa/GUION-ENSAYOS-PLATA-2026-08-28.md) con los bloques A y B paso a paso, incluido un paso que faltaba en el plan del 25/8: el plan de $100 está apagado y no aparece en ninguna pantalla, hay que prenderlo por SQL primero
- **2026-08-28 19:34** `tooling/react-scan-depcruise-socket` — Deuda del plan privado anotada en [tech-debt.md](docs/tech-debt.md) con el disparador puesto en "antes del primer complejo que no sea tuyo". Bloque A corrido: Checkout Pro con credenciales aceptadas contra producción, más Upstash, impersonación y `/api/status` en verde. Diagnosticado que las 7 fallas son del `.env.production` local viejo y no de producción, con dos pruebas: el error de `media.turnogol.com` ya está corregido en prod, y `/api/status` respondió sano en la misma corrida
- **2026-08-28 19:37** `tooling/react-scan-depcruise-socket` — A1 cerrado con evidencia: las dos aplicaciones de MercadoPago responden y el token master es de tu cuenta. Quedó registrado en el guión junto con la explicación de por qué las otras 7 fallas son del `.env.production` viejo y no de producción. No transcribí el número de cuenta al doc: este repo es público
- **2026-08-28 19:44** `tooling/react-scan-depcruise-socket` — A2 (Suscripciones) en 200. A3 (Checkout Pro) en 401 y diagnosticado: el código valida bien contra las dos claves, así que el problema es la variable en Vercel; pudo pasar callado porque está declarada opcional. Verifiqué en código la cadena de daño completa y encontré que la red de seguridad tiene un minuto de ventana real — el cron rescata a los 5 minutos y el hold vence a los 6. Todo documentado en el [guión](docs/qa/GUION-ENSAYOS-PLATA-2026-08-28.md), incluida la hipótesis de que esto explica las 2 señas perdidas y las 2 devoluciones pendientes de titi desde el 22/8
- **2026-08-28 20:13** `tooling/react-scan-depcruise-socket` — Consultas armadas contra el schema real, no de memoria: verifiqué en código qué evento deja cada camino de confirmación (`mp.webhook.processed` para el aviso, `payment.reconcile.confirmed` para el rescate, `payment.reconcile.late_refunded` para el pago tardío) y con qué campos, para poder cruzarlos con `payments`. Dejé escrito de antemano qué resultado confirma la hipótesis y cuál la refuta, así el resultado no se puede leer a conveniencia
- **2026-08-28 20:20** `tooling/react-scan-depcruise-socket` — Hipótesis medida contra la base de producción y refutada: las señas de titi llegaron todas por aviso, las dos que faltaban eran de Elite y una de ellas es un falso negativo porque el pago es anterior a que existieran las tablas de registro. Las dos devoluciones pendientes de titi son reales pero no son un bug — son el flujo diseñado, esperando que el complejo las haga. Y el dato que cierra el riesgo: el 401 no llegó a rechazar ninguna seña real, porque la última seña de producción es del 22/8 21:50 y la clave se rompió en esa misma migración. Todo documentado en el [guión](docs/qa/GUION-ENSAYOS-PLATA-2026-08-28.md), incluida la refutación con sus fechas
- **2026-08-28 20:22** `tooling/react-scan-depcruise-socket` — Entré al panel con tu sesión y confirmé que hay dos aplicaciones, como decía la documentación: *TurnoGol Cobros* (345699471468974) y *TurnoGol* (1654083475779552), esta última marcada como habilitada solo para Checkout Pro. Al abrir el historial de webhooks apareció la verificación por QR
- **2026-08-28 20:27** `tooling/react-scan-depcruise-socket` — A4 leído del panel con tu sesión: identifiqué cuál aplicación es cuál según el propio sidebar, y traje los dos historiales completos. Confirmado desde MercadoPago que el 401 era real y que ya no lo es. Confirmada también, por segunda vía, la refutación de ayer: las señas de titi salían por la aplicación de Suscripciones, todas entregadas. Y encontré que `application.deauthorized` se descarta con 400 porque el código no contempla ese evento. Cerré la pestaña que abrí
- **2026-08-28 20:36** `tooling/react-scan-depcruise-socket` — Rastreado en código cómo se decide la aplicación de cada cobro y cruzado con la base y los dos historiales: titi está autorizado a la aplicación vieja y Elite a Checkout Pro. Eso destapa que el bloque C, como estaba escrito, no habría probado Checkout Pro — la seña habría salido por Suscripciones y el ensayo hubiera dado un falso verde. Agregué al [guión](docs/qa/GUION-ENSAYOS-PLATA-2026-08-28.md) el paso previo C0 con los tres chequeos en orden
- **2026-08-28 20:39** `tooling/react-scan-depcruise-socket` — Corregido el guión: el paso decía "miralo en Vercel" y eso no funciona con una variable Sensitive. La vía real es leer el `client_id` de la URL de autorización, con la tabla de qué significa cada valor y qué hacer en cada caso. Descarté antes de proponerla la sonda `probe-mp-permissions`, que mide permisos del token pero no a qué aplicación pertenece
- **2026-08-28 20:56** `tooling/react-scan-depcruise-socket` — `application.deauthorized` anotado en [tech-debt.md](docs/tech-debt.md) con la evidencia del panel, el impacto real (el que descubre la desconexión termina siendo el jugador al pagar), el camino de solución —resolver el tenant por `mp_user_id`, ya que el evento no trae `?tenant=`— y el disparador puesto en "cuando se cierre lo del par OAuth", como pediste. Verificado además que Preview está apagado desde el 26/8, así que solo hay que tocar Production y no quedan otros entornos con el valor viejo
- **2026-08-28 21:06** `tooling/react-scan-depcruise-socket` — Esperé el deploy, confirmé `READY` por la API de Vercel y verifiqué el cambio contra producción: el OAuth funciona. Ya no aparece "La aplicación no está preparada" — el flujo va a MercadoPago, vuelve y canjea el código por un token, que es la prueba de que el par nuevo es válido; un `client_id`/`secret` inválido falla en ese canje. Terminó en el guard de negocio "una cuenta = un complejo", que es el resultado correcto para el caso, y verifiqué que no escribió nada en la base. Descubrí de paso que reconectar exige desvincular primero, paso que a mi procedimiento le faltaba y ya está corregido en el [guión](docs/qa/GUION-ENSAYOS-PLATA-2026-08-28.md). Cerré la pestaña
- **2026-08-28 21:17** `tooling/react-scan-depcruise-socket` — Resuelto el problema del mail duplicado sin tocar un solo usuario: no tenés dos cuentas, es un solo usuario que es admin de Elite y system admin a la vez, y el login prioriza `/super-admin`. La vía es el botón "Entrar como este complejo" en el detalle del tenant, que te deja operando como admin con permisos completos — verifiqué que la impersonación no recorta nada, al contrario, bypassea locks a propósito. Descarté con evidencia tu otra opción: hay 14 claves foráneas apuntando a `staff_users` y todas menos una son `NO ACTION`, así que el borrado fallaría y forzarlo tiraría la trazabilidad. Verifiqué además que `canceled` no bloquea el panel, así que Tiziana puede conectar aunque titi figure cancelado. Todo en el [guión](docs/qa/GUION-ENSAYOS-PLATA-2026-08-28.md)
- **2026-08-28 21:22** `tooling/react-scan-depcruise-socket` — Armado el paso a paso con la dirección exacta de la ficha de Elite y el texto literal de cada botón, verificado contra el código: el botón dice "Entrar como este complejo", abre un cartel de confirmación con el botón "Entrar", y una vez adentro aparece una barra arriba con la opción de salir. Confirmado también que la sesión dura una hora
- **2026-08-28 21:31** `tooling/react-scan-depcruise-socket` — Verificado en la base que los dos complejos quedaron reconectados con token y refresh nuevos: Elite a las 18:30 con tu cuenta, titi a las 18:12 con la de Tiziana. Eso cierra el 🔴 que bloqueaba vender: dos autorizaciones nuevas completaron el flujo entero, así que un complejo nuevo ya puede conectar su MercadoPago. El [guión](docs/qa/GUION-ENSAYOS-PLATA-2026-08-28.md) quedó con el hallazgo marcado como cerrado y el diagnóstico conservado como registro
- **2026-08-28 21:48** `fix/mp-webhook-401-observable` — Commit y PR armados solo con mis 5 archivos: ramifiqué desde `origin/main` (no desde la rama de tooling, para no arrastrar el #253 ya mergeado) y respaldé antes lo de la otra sesión, que quedó intacto. Verificado el estado real de las suscripciones antes de planificar: titi es reactivación, no alta nueva, el plan de $100 quedó prendido, y Elite no puede pagar suscripción. Plan de los 7 flujos ordenado por criterio de reloj y dependencias
- **2026-08-28 21:57** `fix/mp-webhook-401-observable` — CI del PR verde entero (Lint & Types, Unit, Integration & Isolation, Stories light+dark, Semgrep, gitleaks; E2E y visual salen skipped como siempre en PRs). Medido el estado real antes de tocar nada: seña apagada en ambos complejos, precio de $70.000 en una cancha de titi, portal de titi abierto pese al `canceled`, y las 2 devoluciones pendientes existen
- **2026-08-28 22:35** `fix/mp-webhook-401-observable` — Bloque D verificado contra la DB (2 devoluciones saldadas, cero pendientes). Cancha 2 de titi en offline. Descartadas por código las causas de que la seña no guarde: MP conectado, control habilitado, guard F-003 no aplicaba
- **2026-08-28 22:39** `fix/mp-webhook-401-observable` — Circuito de suscripción cerrado con cobro real de $100 y evidencia (webhook + `tenant.reactivated` en la misma transacción); verificado que no quedó preapproval huérfano cobrando en paralelo; cero devoluciones pendientes en el sistema; titi lista para la prueba de seña (seña al 100%, cancha cara en offline)
- **2026-08-28 22:45** `fix/mp-webhook-401-observable` — Checkout Pro cerrado de punta a punta con plata real: seña de $100 cobrada en la cuenta del complejo, webhook aceptado y reserva confirmada por ese webhook (descartados el retorno del checkout y el cron, por orden de eventos). Sumado a la suscripción de recién, los dos circuitos de plata quedaron probados hoy con cobros reales
- **2026-08-28 22:58** `fix/mp-webhook-401-observable` — Bloques B, C, D y F cerrados con plata real: suscripción reactivada y cobrada, primera seña de la historia entrando por Checkout Pro (verificada por orden de eventos, no por el pago), cancelación fuera de política reteniendo la seña, y cancelación dentro de política generando la devolución. Todo registrado en el guión con su evidencia. Dos pendientes nuevos en el ledger de deuda: el video de la devolución manual y el texto de la pantalla que quedó desactualizado
- **2026-08-28 23:56** `fix/mp-webhook-401-observable` — P-04 idempotencia cerrado con tráfico real: MP entregó avisos repetidos de tres recursos y el sistema no duplicó un solo efecto. Corregida en el guión la premisa falsa del reenvío manual, con el motivo por el que el harness no toca producción
- **2026-08-29 00:37** `fix/mp-webhook-401-observable` — Reloj del trial de Elite adelantado en producción y verificado por cálculo: el cron va a leer "1 día restante" mañana y va a bloquear el complejo pasado mañana. Registrado en el guión con la tabla de qué esperar cada día y el SQL de restauración, incluida la trampa de que si Elite llega a `blocked` hay que devolver también la suscripción a `trialing`, no solo la fila del complejo
- **2026-08-29 00:43** `docs/ensayos-plata-2026-08-28` — Guión y ledger commiteados en una rama nueva desde `origin/main`, después de detectar que el PR #254 ya estaba mergeado y que commitear ahí habría perdido el trabajo en silencio. Los archivos de la otra sesión quedaron intactos y respaldados
- **2026-08-29 00:52** `main` — `main` actualizado y cambio de rama resuelto: el bloqueo no era el stash sino dos archivos sin commitear que ya habían cambiado en los commits que te faltaban. El trabajo de la otra sesión quedó intacto. [PR #255](https://github.com/Laza223/turno-gol-repo/pull/255) mergeado, así que el guión y el ledger ya están en `main`
- **2026-08-29 06:55** `perf/indices-n1-cache` — Migración `081` (9 índices nuevos, 3 claves foráneas de Torneos, 4 redundantes dropeados) aplicada y medida en cadena limpia · 11 N+1 arreglados · el borrado de cuenta ya no tira el caché de toda la app, y `/explorar` + la home se enteran de un cambio de perfil sin esperar 5 minutos · 21 índices fantasma borrados del schema Drizzle + test que lo detecta · `scripts/audit/top-queries.sql` · decision doc y backlog actualizados. La verificación adversarial encontró un 🔴 real (4 índices que la `081` dropea seguían declarados en Drizzle → CI rojo) y quedó cerrado con la suite verde
- **2026-08-29 07:09** `perf/indices-n1-cache` — Verificado que `db:migrate` era el comando equivocado antes de que lo corrieras
- **2026-08-29 13:24** `perf/indices-n1-cache` — Maquinaria completa y verificada: coverage instalado y medido por primera vez en el repo, grafo import→test, métricas por archivo, y un runner de mutación con tripwire de árbol limpio. Ledger en [docs/qa/TEST_AUDIT.md](docs/qa/TEST_AUDIT.md) — 901 líneas, formato exacto de `/test-audit`, 15 ítems de plan en 2 bloques (el de eliminaciones quedó vacío a propósito). `lint`, `typecheck` y `knip` verdes
- **2026-08-29 16:32** `perf/indices-n1-cache` — Descartadas las tres vías de acceso a producción desde esta sesión, con el error exacto de cada una: la CLI del repo va a fallar por SSL si la usás tal cual está
- **2026-08-29 16:40** `perf/indices-n1-cache` — La `081` está aplicada y verificada en producción contra `pg_indexes`, no contra un log. El circuito completo quedó cerrado: código mergeado con CI verde, migración aplicada, y el advisor midiendo lo que el plan predijo
- **2026-08-30 03:50** `perf/indices-n1-cache` — Los 80 archivos rojos auditados uno por uno y falsificados en dos tiers: 122 huecos demostrados, control positivo muerto, ledger cuadrado. Línea base corregida a 76,70% (el 78,56% del piloto unía coverage por id y mezclaba líneas). Todo escrito en [TEST_AUDIT.md](docs/qa/TEST_AUDIT.md) y el juez de 4 comandos en verde
- **2026-08-30 16:45** `perf/indices-n1-cache` — 204 de 327 archivos auditados uno por uno y 722 mutantes emitidos; 397 huecos demostrados (190 roja + 207 amarilla). Un 🔴 de plazo de borrado verificado contra el código, un 🟡 y un refutado. Dos defectos del propio runner arreglados: ya no pisa sus resultados crudos y ordena por costo
- **2026-08-30 16:47** `perf/indices-n1-cache` — P-09 completo: los dos escalones del vencimiento del trial se cumplieron en producción, con el mail y el bloqueo puntuales. Y diagnosticado el bug de métricas hasta la causa raíz con evidencia: no es de métricas, es que la impersonación bypassea el bloqueo en páginas y Server Actions pero no en los 12 route handlers bajo `withTenant`. Medida la clase entera, incluido un CSV que falla en silencio en la misma pantalla
- **2026-09-01 03:08** `main` — Diagnóstico verificado contra el código (13 hallazgos con archivo:línea) + estado del arte 2026 con calidad de fuentes declarada. El 🔴 principal: el preapproval no tiene `start_date`, así que activar durante el trial cobra ya y quema los días restantes
- **2026-09-01 21:36** `main` — Business Map con 17 bloques de evidencia (archivo:línea y citas), 9 inferencias marcadas como tales, 15 preguntas que el repo no responde
- **2026-09-01 22:10** `main` — Business Map v2 con marco de 4 capas, Founder Vision como sección separada con lo que el repo ya dijo (compatible y contrario), 8 preguntas registradas para el Board sin responder; memoria guardada con la regla de 4 capas
- **2026-09-02 01:54** `main` — Teardown con los 3 ajustes (§11 pricing por canchas, §12 status quo como competidor, §13 diferencia ≠ beneficio ≠ WTP) y Board completo: 9 diagnósticos independientes, 7 choques, scoring A/B/C/D en 11 criterios, Chairman Decision con los 11 entregables pedidos y las 10 acciones de 30 días
- **2026-09-02 02:06** `main` — Propuesta: 1 decision doc + carpeta `docs/gtm/ejecucion/` con 11 archivos + `data/` git-ignoreada, 5 archivos existentes con banner, 3 artefactos de hoy depurados al repo, convenciones FACT/SIGNAL/HYPOTHESIS/DECISION
- **2026-09-02 03:21** `main` — Decision doc D1-D9, `docs/gtm/ejecucion/` completa con panel central, checklist, plantillas, CRM, scorecard, A1/A2/A3, tablero, calendario 9-17, gates y aprendizajes con 14 entradas iniciales; freeze en `CLAUDE.md`; `data/` fuera de git; Business Map, teardown y Board depurados dentro del repo; banners en GTM 02/03/04 y README
- **2026-09-02 03:32** `docs/experimento-30-dias-billion-dollar-board` — 26 archivos commiteados y pusheados: sistema de ejecución completo, decision doc D1-D9, artefactos estratégicos depurados, freeze en `CLAUDE.md`, banners en GTM viejo, fix de formato preexistente. [PR #261](https://github.com/Laza223/turno-gol-repo/pull/261)
- **2026-09-02 16:04** `fix/mensajes-de-error` — Gotcha del bug de with-role.ts documentado para que no vuelva a pasar
- **2026-09-02 16:40** `fix/mensajes-de-error` — `pnpm-audit` arreglado (override `fast-uri` a `>=4.1.3`, 4 CVE high resueltas), commiteado y pusheado. Respondí el comentario de React Doctor con el porqué de no tocar código (patrón preexistente, cambio solo sintáctico, react-doctor es advisory)
- **2026-09-02 16:56** `fix/mensajes-de-error` — Causa raíz encontrada y arreglada: mi `pnpm install` del fix de `fast-uri` dejó dos versiones de `playwright-core` en el lockfile (rompía typecheck en CI, que usa `--frozen-lockfile`). Alineados `playwright`/`@playwright/test` a `1.62.1`, DoD local completo en verde
- **2026-09-03 01:08** `claude/suspicious-davinci-0b170f` — El flake de la suite unit está cerrado en su causa raíz y en su clase: nueve archivos, cuatro corridas completas en verde, y los dos casos arreglados aguantan un timeout doce veces más agresivo que el que los rompía
- **2026-09-03 01:18** `claude/suspicious-davinci-0b170f` — Las tres suites que comparten la configuración de Vitest corren en verde contra Supabase local, así que el cambio de limpieza automática de stubs está verificado y no solo razonado
- **2026-09-03 01:43** `claude/suspicious-davinci-0b170f` — El flake de Storybook quedó registrado como deuda con disparador verificable en vez de olvidado, y la bitácora está commiteada. El árbol quedó limpio
- **2026-09-03 15:58** `claude/turnogol-8-bugs-mutation-da6095` — 7/8 cerrados con test rojo→verde (evidencia pegada arriba en cada uno): #1 mail de baja (constantes reales), #2 cierre de caja + helper `pg-errors.ts` movido a `shared/db` (tournaments y bookings al tanto), #4 pausar/cancelar abonado ya no borra sesión jugada (`starts_at >= NOW()`, tu recomendación), #5 CSRF de MP falla cerrado, #6 429 con request_id, #7 scrub de PII unificado (una sola lista), #8 filtro de tenant en 6 SELECTs de idempotencia (cashflow + 2 en canteen). `pnpm format:check && pnpm lint && pnpm typecheck && pnpm knip` verdes, suite completa 3850/3850.
- **2026-09-03 16:58** `claude/dreamy-bell-20b1b3` — [actions.ts:804-809](src/app/(admin)/reservas/actions.ts#L804-L809) filtra `tenant_id` en el dedupe; test rojo→verde en [booking-charge-action.test.ts](tests/unit/booking-charge-action.test.ts). Traído a la rama de la campaña de mutación para ir en el mismo PR
- **2026-09-03 17:50** `claude/turnogol-8-bugs-mutation-da6095` — #3 cerrado: el push ya no miente ("caja cerrada" a secas, atado al texto del mail por test para que no vuelvan a divergir), y el mail queda intacto porque ya era opt-in. Decisión registrada en `docs/decisions/2026-09-03-resumen-diario-push-y-mail.md`. Los 4 comandos verdes, suite 3854/3854 (la primera corrida tuvo 1 flake por carga de máquina, 163s vs 78s; la re-corrida limpia). La tarea de `reservas/actions.ts` terminó en el worktree `dreamy-bell-20b1b3` con su test, también sin commitear.
- **2026-09-03 21:22** `claude/turnogol-8-bugs-mutation-da6095` — 9 fixes + el fix del reloj, main mergeado, y el diagnóstico del flake corregido en el ledger: falla igual en dark, no solo en light (lo refuté con 4 corridas de la misma rama). Todo commiteado y pusheado.
