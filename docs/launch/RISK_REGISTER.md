# TurnoGol — Risk Register Launch

| ID | Área | Riesgo | Severidad | Evidencia | Estado | Decisión |
|---|---|---|---|---|---|---|
| TG-P0-RLS-01 | RLS / tenant isolation | Suite bloqueante de aislamiento NO cubre 4 tablas RLS post-021 (push_subscriptions, feature_flags, reviews, player_favorites). Si se dropea una policy, ningún test bloqueante lo delata. push_subscriptions guarda endpoint+p256dh+auth (targeting de push). | 🟡 P1 (registrado con ID P0 a pedido) | `tests/integration/isolation.test.ts:70-100` cataloga solo 13+players+staff_users; las 4 tablas tienen tests propios NO bloqueantes | **CERRADO** (contradicción resuelta 2026-09-01) | El fix se hizo: `LAUNCH_BACKLOG.md` TG-BL-01 lo da hecho (bloques H/L/M, commit 1067806) y re-verificado el 2026-07-08 con 111/111 verde. **Esta fila decía "Abierto" contra su propio ticket** — se corrigió al auditar el registro |
| TG-P2-RLS-02 | RLS / defense-in-depth | **REFUTADO por un gate real** — la premisa "no explotable con el rol de app actual" era falsa: tras `036_force_rls_remaining_tables.sql` (FORCE RLS) + PR #30 (4ad5630, `getDb()`/`getSql()` corren como `turnogol_app`, rol restringido sin BYPASSRLS), cualquier escritura desde el pool de app a una tabla FORCE-RLS'd **sin policy de escritura para ese rol** viola RLS en runtime real (no solo en teoría). `tests/e2e/staff-crud.spec.ts` #1 (invitar) y #2 (desactivar) quedaron rojos determinísticamente con `PostgresError: new row violates row-level security policy for table staff_users` — `staff_users` (006_rls_policies.sql) solo tiene policy SELECT ("gestión vía service role"), y `inviteStaffAction` insertaba ahí vía `withTenantContext` (rol app). Sweep de las 7 tablas que forzó 036 (players, staff_users, system_admins, push_subscriptions, feature_flags, reviews, player_favorites) encontró 2 bugs más de la misma clase, ya arreglados: (a) `player.service.ts` `getOrCreatePlayer` — INSERT/UPDATE a `players` (sin policy INSERT; UPDATE solo self-scoped) vía `getSql()` sin contexto, rompía **todo login/signup de jugador** (magic link callback), no solo un flujo admin; (b) `src/app/api/admin/push/{subscribe,unsubscribe}/route.ts` — `push_subscriptions` sí tiene policy de escritura (`push_subs_modify`) pero exige `app.current_tenant_id` seteado, y estas rutas usaban `getSql()` plano sin `withTenantContext`, así que la policy nunca matcheaba. Hallazgo colateral NO arreglado (fuera del alcance de este fix, ver TG-P0-RETENTION-02): `data-retention-cleanup.worker.ts` también escribe a `feature_flags` (sin policy de escritura) y a `audit_logs`/`daily_cash_closes` (GRANT UPDATE/DELETE revocado a `turnogol_app` en `037_turnogol_app_grants.sql`) vía `withTenantContext` (pool de app) — el catch silencioso del worker (`catch (err) { logger.error(...) }`, sin rethrow) hace que el wipe semanal de retención (Ley 25.326 §16) falle y quede logueado sin alertar. | 🔴 P1 (para staff_users/players/push_subscriptions: **cerrado con fix**; el hallazgo de retention worker también cerrado, ver TG-P0-RETENTION-02 resuelta 2026-07-09) | `src/shared/db/migrations/006_rls_policies.sql` (staff_users solo SELECT); `036_force_rls_remaining_tables.sql`; `037_turnogol_app_grants.sql` (REVOKE audit_logs/daily_cash_closes); `tests/e2e/staff-crud.spec.ts` (gate rojo original); `launch-check.ts:52 bypassRlsCheck` confirma que el rol de app NO tiene BYPASSRLS — la premisa de "gate CI protege" era sobre otra cosa (que el rol no sea superusuario), no sobre que las policies de escritura existan | **Cerrado (staff_users/players/push_subscriptions)** | Fix aplicado: `inviteStaffAction` (`src/app/(admin)/staff/actions.ts`) separado en 3 fases — preflight+autorización bajo `withTenantContext` (RLS aplica, sin escrituras), upsert de `staff_users` vía pool worker (`upsertStaffUser` en `staff.service.ts`, `getWorkerDb`), y vínculo a `tenant_staff_members` + invite de vuelta bajo `withTenantContext`. `getOrCreatePlayer` migrado a `getWorkerSql()`. Rutas de push migradas a `withTenantContext` + Drizzle. Sin test e2e para push subscribe/unsubscribe (deuda, no bloqueante — anotado en skipped) |
| TG-CONC-INV | Concurrencia / doble reserva | Invariante no-doble-reserva GARANTIZADA (no es riesgo abierto). A lo sumo 1 booking con status ∈ {pending_payment, confirmed} puede solapar en (court_id, día operativo, rango horario). Enforced por constraint DB + FOR UPDATE de courts + UPDATE condicional race-safe. Auditoría Fable 2: sin P0/P1. | ℹ️ Informativa | `004_isolated_tables.sql:288` `no_overlapping_bookings` (EXCLUDE USING gist); `booking.service.ts:150` `lockCourtOrThrow` FOR UPDATE; `booking.concurrency.ts` `transitionFromPendingPayment`; tests `race-double-booking` / `race-admin-vs-online` / `race-expiry-vs-confirm` + `scripts/stress-test.ts` (50 paralelas) | Cerrado | Sin acción. Documentado como invariante de referencia |
| TG-INFO-CONC-01 | Concurrencia / abonados | Generación batch de abonados vs reserva individual: no se verificó a fondo que el catch de `23P01` sea por-instancia (saltear la semana en conflicto y seguir vs abortar el lote). El constraint DB igual impide doble reserva; riesgo solo de robustez del lote, no de invariante. | ℹ️ Informativa | `tests/integration/race-abonado-vs-individual.test.ts` existe; `abonado.service.ts` no auditado en detalle | Abierto (info) | Confirmar catch por-instancia post-launch. No bloquea |
| TG-INFO-CONC-02 | Concurrencia / testing | `scripts/stress-test.ts` usa backdoor E2E `/api/e2e/create-booking` gateado por `NODE_ENV` → no apuntable a prod (correcto para no-prod). | ℹ️ Informativa | `scripts/stress-test.ts` | Cerrado | Aceptable. Sin acción |
| TG-INFO-E2E-01 | Testing / e2e suite | Día 3 checklist (`pnpm test:e2e`): full run dio 57 failed / 63 passed. Investigación (rerun serial/aislado por spec, sin el vecino ruidoso `capture-screenshots` de 5min) separó ruido de bugs reales: (1) ~38 de las 57 eran contención de recursos (fullyParallel default workers vs `next dev` en modo dev) — booking-flow S1-S4, player-bookings, player-delete-account, portal-search, canchas-crud parcialmente: TODOS verdes al aislar. (2) 2 eran browsers de Playwright no instalados (webkit/firefox) — resuelto con `pnpm exec playwright install`. (3) ~19 tests fallaban DETERMINÍSTICAMENTE incluso aislados — mayoría test desactualizado vs código actual (selectors/copy), sin indicio de regresión de producto, EXCEPTO `canchas-crud.spec.ts` que sí escondía un bug real (ver TG-P2-DEP-01). (4) `abonados-crud.spec.ts` #4 + `first-booking-aha.spec.ts` fallan por `exclusion constraint` — flakiness YA documentada (ver memoria `e2e-abonados-racy-pin-residual`), no nueva. Ningún flujo de pago/booking real resultó afectado — `booking-flow.spec.ts` (S1-S4, deposit + no-deposit + webhook + reintentar) 4/4 verde aislado. | 🟡 P2 (deuda de testing, no bloqueante de pagos) | Ver `docs/launch/LAUNCH_BACKLOG.md` TG-BL-05 para el detalle completo archivo por línea | **Cerrado** (2026-07-08) | Fix aplicado (TG-BL-05): selectors/copy reescritos en los 8 archivos + fix real de `canchas-crud` (TG-P2-DEP-01). 39/39 verde, `pnpm typecheck`/`lint`/`test` (1510) también verdes. `touch-targets.spec.ts` ya pasaba sin cambios (resuelto por el trabajo previo de mobile touch-targets a 44px) |
| TG-P2-DEP-01 | Dependencias / storage | `src/shared/storage/r2.ts` importa `@aws-sdk/client-s3@3.1079.0`, cuya resolución de `@aws-sdk/checksums` (pineado en `pnpm-lock.yaml` a `3.1000.12`, la última versión publicada en el registro es `3.1000.14`) NO tiene el layout de archivos que ese client espera (`dist-cjs/submodules/flexible-checksums/index.js` no existe en 3.1000.x). El `require()` de `@aws-sdk/client-s3` tira `MODULE_NOT_FOUND` en runtime. Como `src/app/(admin)/canchas/actions.ts` importaba r2.ts a nivel de módulo, ESE bug tumbaba con 500 TODAS las acciones del archivo (crear/editar/activar/desactivar cancha), no solo las de fotos — así se manifestó como los 3 tests rojos de `canchas-crud.spec.ts`. Mitigado (import dinámico solo en `uploadCourtPhotoAction`/`removeCourtPhotoAction`, ver TG-BL-05), pero **la feature de fotos de cancha (subir/borrar) sigue rota** en cualquier entorno sin re-resolver esta dependencia — no se puede fijar con un fix de test. | 🔴 P1 (feature de fotos de cancha inutilizable; no bloquea booking/pagos) | `node -e "require('@aws-sdk/client-s3')"` → `Cannot find module '.../@aws-sdk/checksums/dist-cjs/submodules/flexible-checksums/index.js'`; `pnpm view @aws-sdk/client-s3 dist-tags.latest` → `3.1081.0` (el pin actual es real y reciente); `pnpm view @aws-sdk/checksums dist-tags.latest` → `3.1000.14` (muy por detrás, posible reestructuración del paquete río arriba) | **Cerrado (2026-07-26)** — resuelto río arriba, sin cambio de código. `@aws-sdk/checksums` publicó `3.1000.16` (el layout `dist-cjs/submodules/flexible-checksums/index.js` que faltaba) y `client-s3` quedó en `3.1086.0`. Evidencia: `node -e "require(&apos;@aws-sdk/client-s3&apos;)"` carga OK; `new S3Client(...)` + `PutObjectCommand`/`DeleteObjectCommand` se instancian; `tests/unit/r2-storage.test.ts` + `canchas-photo-actions.test.ts` 16/16 verdes. PENDIENTE menor: prueba end-to-end real (subir una foto desde el admin contra el bucket de prod) — el bloqueante técnico ya no existe. El import dinámico en `canchas/actions.ts` se conserva a propósito (lazy-load del SDK pesado, ya no es mitigación). | ~~REQUIERE INPUT~~: la decisión quedó sin objeto (podría no existir ninguna versión publicada compatible con este client-s3, sugiriendo que el paquete completo de S3 necesita bajar de versión, o migrar a otro cliente/SDK). Decisión de negocio/arquitectura: aceptar temporalmente sin fotos de cancha, fijar una versión de `@aws-sdk/client-s3` más vieja y compatible, o resolver el paquete manualmente |
| TG-INFO-DBT-01 | Testing / infra | `scripts/bootstrap-test-db.mjs` NO es idempotente: re-correrlo sobre un DB ya poblado revienta en `002_enums.sql` (`42710 duplicate_object`, `CREATE TYPE` sin guard `IF NOT EXISTS`/`DO`). Sirve solo para DB fresca (CI). Sobre un DB local ya poblado hay que aplicar migraciones nuevas de a una (ej. 036 se aplicó suelto). | ℹ️ Informativa (deuda técnica) | `scripts/bootstrap-test-db.mjs:27-32` (aplica todos los `.sql` en orden vía `sql.unsafe`); `002_enums.sql` `CREATE TYPE` sin guard | Abierto | Fix diferido (fuera de scope): envolver enums en `DO $$ ... IF NOT EXISTS` o hacer el runner idempotente. No bloquea launch |
| TG-P1-MP-01 | Pagos / suscripción SaaS | Webhook `subscription_authorized_payment` usa el gateway del complejo (`resolveTenantGateway`, token OAuth de señas) en vez del master (`MP_TURNOGOL_ACCESS_TOKEN`) para `getPaymentStatus` → el token de collector no puede leer un pago de otra cuenta → 404 → dead-letter → la renovación nunca auto-confirma (tenant desliza active→past_due→suspended). Además `onPaymentApproved(job.tenantId,…)` sin cross-check de `external_reference`. Fail-closed (no corrompe reservas, no doble-cobra). | 🟡 P1 | `mp-webhook.handler.ts:61,71,74` (token del tenant) vs `billing.gateway.ts:20` (master); sin cross-check, contraste `:119`/`:142`; sin test (IDOR solo cubre `payment`) | **Cerrado** | Fix aplicado (TG-BL-03): ramas `subscription_authorized_payment`/`subscription_preapproval` ahora usan `getBillingGateway()` (master); guard `TenantMpNotConnectedError` movido a solo la rama `payment`; cross-check `info.externalReference === job.tenantId` agregado antes de mutar. Tests nuevos en `mp-webhook.test.ts` (activación trialing→active sin token de tenant + rechazo IDOR de suscripción). `pnpm test:integration` 567/567 verde |
| TG-P1-MP-02 | Pagos / upgrade SaaS | Hallazgo colateral al cerrar TG-BL-01: rama `payment`→upgrade de `mp-webhook.handler.ts` pasa el gateway del TENANT (`resolveTenantGateway`) a `handleUpgradeApproved`, que llama `gateway.updatePreapprovalAmount(sub.mp_subscription_id, …)` — pero `mp_subscription_id` es un preapproval de la cuenta MASTER (creado por `subscribe()` vía `getBillingGateway()`). Un token OAuth de complejo no puede operar un preapproval de otra cuenta MP → probable 404/403 → el upgrade completo revienta (la transacción hace rollback, `pending_plan_change` nunca se limpia). NO verificado con test real contra API MP (sin sandbox en este audit); es lectura de código, no reproducido. **Actualización (revisión adversarial pre-launch)**: la aceptación original asumía el endpoint inalcanzable en prod (sin UI de upgrade); se verificó que `POST /api/billing/upgrade` estaba montado, funcional y alcanzable por cualquier admin autenticado (fetch/curl), sin feature flag — cualquiera podía disparar el bug de arriba. | 🟡 P1 (no confirmado) | `mp-webhook.handler.ts` rama `payment`→`handleUpgradeApproved(upgrade.tenantId, upgrade.targetPlanId, gateway, tx)` usa el `gateway` tenant construido arriba; `billing.service.ts:298-300` `gateway.updatePreapprovalAmount(sub.mp_subscription_id, newAmount)`; `sub.mp_subscription_id` creado en `billing.service.ts:160` vía `getBillingGateway()` (master) | **CERRADO — arreglado y el gate ABIERTO** | **Fixeado, y el 501 ya no aplica.** La migración [`067_enable_saas_upgrade_flag.sql`](src/shared/db/migrations/067_enable_saas_upgrade_flag.sql) prende `saas_upgrade` con una fila global y documenta el fix: la `notification_url` de todo lo que cobra la cuenta master lleva `&source=saas` (`billing.service.computeNotificationUrl`) y el handler elige la cuenta con ese discriminador, verificando además que el `external_reference` del pago concuerde. El flag se conserva en el código a propósito, para poder apagarlo sin esperar un deploy. **El registro seguía diciendo que el endpoint devuelve 501** — verificado el 2026-09-01. ⚠️ **Lo que sí queda abierto es otra cosa**: ese circuito cobra plata real y **no está entre los cuatro circuitos probados** de PRODUCTION-READY §A1. Se registró como ítem B11 ahí |
| TG-P0-REFUND-01 | Pagos / cancelación | Hallazgo huérfano fuera del register original (audit_report.md 2-53): si la llamada HTTP a MercadoPago para reembolsar la seña falla (timeout, 5xx, circuit breaker abierto) DURANTE una cancelación con seña, la transacción entera hace rollback — el booking queda `confirmed`, el slot no se libera, y el jugador cree que canceló cuando en realidad no pasó nada. El refund va DENTRO de la misma transacción que la cancelación, sin retry ni desacople. Coincide con el riesgo que el fundador identificó como su mayor miedo (PROGRESS.md:10). | 🔴 P0 (huérfano, sin ticket previo) | `booking.cancellation.ts:133` (cancelación jugador) y `:229` (cancelación admin): `await createRefund(b.payment_id, b.deposit_amount, gateway, tx)` — llamada HTTP síncrona dentro de la tx de cancelación | **CERRADO 2026-09-01 — el riesgo dejó de existir** | **No hizo falta el fix: la causa se fue sola con otro cambio.** Este riesgo describía una llamada HTTP a MercadoPago (`createRefund`) DENTRO de la transacción de cancelación. Esa llamada ya no existe: al eliminarse el reembolso automático por API (PR #212, porque MP lo rechaza siempre con la cuenta de un tercero), el camino pasó a `prepareRefund` ([booking.cancellation.ts:120](src/modules/bookings/booking.cancellation.ts:120)), que es **sólo DB** — no hay red adentro de la tx, así que no hay timeout ni 5xx que pueda hacer rollback y dejar al jugador creyendo que canceló. Verificado por lectura del código el 2026-09-01: `grep createRefund src/modules/bookings/booking.cancellation.ts` no devuelve nada. La aceptación del fundador y la mitigación operativa quedan sin objeto. **El registro lo seguía cobrando como 🔴 P0 abierto** |
| TG-P1-RETENTION-01 | Legal / privacidad | Hallazgo huérfano (audit_report.md 2-76): `data-retention-cleanup.worker.ts` solo anonimiza tenants `churned`; no hay anonimización de jugadores inactivos >12 meses, ni purga de `audit_logs`, ni anonimización de reservas viejas — pese a que doc18 y la política de privacidad publicada prometen ese comportamiento (Ley 25.326, derecho ARCO). | 🟡 P2 (alcance real MUCHO más chico que lo escrito) | `data-retention-cleanup.worker.ts` — verificado función por función el 2026-09-01 | **Mayormente CERRADO; queda un solo hueco** | **Lo que este registro daba por faltante ya existe**: el worker tiene `purgeOldAuditLogs`, `purgeOldNotifications`, `purgeOldAnalyticsEvents`, `purgeProcessedWebhooks` y `purgePushSendLog`, más el wipe por tenant (`wipeTenant`). El candado `tests/unit/retention-matches-legal-promise.test.ts` ata además el plazo de borrado del código al que promete `/terminos` y `/privacidad`, en las dos direcciones. **Lo único que sigue sin existir es la anonimización de jugadores inactivos >12 meses**: cero referencias a `anonymized`/`inactiv` en el worker. Sigue siendo la única exposición ante un reclamo ARCO por ese eje, y baja de P1 a P2 porque el resto de la promesa ya se cumple |
| TG-P0-RETENTION-02 | Legal / privacidad / RLS | Hallazgo colateral del fix de TG-P2-RLS-02 (sweep de escrituras vía rol de app a tablas FORCE-RLS'd): dentro de la MISMA transacción de wipe (`withTenantContext`, rol `turnogol_app`), `runDataRetentionCleanup` ejecuta `DELETE FROM feature_flags WHERE tenant_id=...` (tabla sin policy de escritura para el rol app, `015_feature_flags.sql`: "los roles de app quedan read-only") y `DELETE FROM audit_logs` / `DELETE FROM daily_cash_closes` (privilegio DELETE explícitamente revocado a `turnogol_app` en `037_turnogol_app_grants.sql`, reaplicando `008_revokes.sql`). Cualquiera de los tres statements tira error y aborta la transacción completa (rollback: NINGUNA tabla del tenant se borra, ni siquiera el soft-anonimize de `tenants`). El `catch` del loop por-tenant solo loguea (`logger.error`, sin rethrow, sin alerta) — el wipe semanal (Ley 25.326 §16, derecho al olvido) lleva silenciosamente sin ejecutarse desde que el rol de app se restringió (PR #30) sin que ningún dashboard lo muestre. No confirmado en runtime real (hallazgo de lectura de código durante el sweep, mismo patrón que sí se confirmó vía gate e2e para `staff_users`); no se intentó un fix porque requiere una decisión de diseño (cambiar el worker completo al pool de servicio `getWorkerDb`/`getWorkerSql`, que bypasea RLS por diseño para jobs cross-tenant, vs. agregar policies de escritura ad-hoc para el rol de app en tablas pensadas como read-only/inmutables — cualquiera de las dos es un cambio estructural fuera del alcance de este fix puntual). | 🔴 P0 (compliance; silencioso — sin instrumentación que lo delate) | `src/shared/jobs/workers/data-retention-cleanup.worker.ts:49,53,63,95-97` (DELETEs + catch mudo); `src/shared/db/migrations/015_feature_flags.sql:14-18` (sin policy de escritura, deliberado); `src/shared/db/migrations/037_turnogol_app_grants.sql:32-33` (REVOKE UPDATE/DELETE audit_logs/daily_cash_closes) | **Resuelta (2026-07-09)** | Dueño aprobó la opción (a): migrar `runDataRetentionCleanup` completo al pool de servicio (`getWorkerDb`/`getWorkerSql`, `src/shared/db/client.ts`), consistente con CLAUDE.md ("Background jobs usan rol de servicio separado") y con el patrón de 45a9806/48a472e/f95e9ee. El SELECT de targets pasó a `getWorkerSql()` y la transacción por-tenant a `getWorkerDb().transaction(...)` (mismo patrón que `auto-complete-bookings.worker.ts`); cada DELETE/UPDATE conserva exactamente el mismo `WHERE tenant_id = ...` (o el scoping por `id` del UPDATE de `tenants`) que tenía bajo `withTenantContext` — ningún statement quedó más ancho, es defensa en profundidad ahora que el pool bypasea RLS por diseño. El catch por-tenant deja de ser silencioso: loguea a nivel error con `tenantId`+`stage: 'tenant-wipe-transaction'` (capturable por el pipeline de DLQ/Sentry, `attachFailureHandlers`) y acumula las fallas; si hubo alguna, el job lanza al final con un resumen y queda en failed visible vía DLQ/Sentry (boss.schedule sin options → retryLimit=0 por defecto: SIN reintento automático esa semana, se re-dispara a mano o espera el próximo cron dominical), sin abortar el resto de los tenants a mitad del loop. Ningún DELETE manual existente (reviews, push_subscriptions, player_favorites, feature_flags, etc.) se quitó. Regresión: `tests/unit/data-retention-cleanup-worker-pool.test.ts` (nuevo) prueba que el worker usa `getWorkerDb`/`getWorkerSql` y nunca `getSql`/`getDb`/`withTenantContext`, más el caso de fallas acumuladas → throw final. `tests/integration/data-retention-cleanup.test.ts` (sin modificar, corre como superusuario) sigue verde: 7/7. Ref.: `src/shared/jobs/workers/data-retention-cleanup.worker.ts` |
| TG-P1-SAAS-SELFSERVICE-01 | Pagos / suscripción SaaS | Hallazgo huérfano (audit_report.md 2-43): el motor de suscripción SaaS no tiene UI de autogestión para el tenant (upgrade/downgrade/cancelación); la decisión de que "soporte lo maneja a mano durante el piloto" nunca se registró formalmente — solo se derivaba indirectamente de la aceptación de TG-P1-MP-02. | 🟢 P2 (huérfano, decisión de proceso) | `src/app/(admin)/settings/facturacion/page.tsx:72-85` (solo CTA "Conectar MP", sin cambio de plan); confirmado además que la API subyacente (`POST /api/billing/upgrade`) queda gateada por TG-P1-MP-02 | **Aceptado para post-launch por el Fundador** | Gestión de plan 100% manual vía soporte durante el piloto (mismo alcance ya cubierto por el gate de TG-P1-MP-02). Construir autogestión cuando el volumen de tenants lo justifique |
| TG-P0-RESTORE-01 | Operaciones / backups | RESTORE-001 (simulacro de restore point-in-time) nunca se ejecutó contra datos reales — bloqueado porque STAGING-001 (proyecto Supabase de staging dedicado) no está provisionado. El criterio MASTER_PLAN B11 ("backup restaurado exitosamente al menos 1 vez") queda sin evidencia real de RPO/RTO al momento del lanzamiento. El `LAUNCH_BACKLOG.md` de la raíz del repo marcaba ambos como "Completados" — era incorrecto, contradecía `docs/audit/backup-drills/2026-07-02-drill.md:3` ("NO EJECUTADO") y ya fue corregido ahí. | ✅ Cerrado (era 🔴 P0 aceptado) | `docs/audit/backup-drills/2026-08-24-drill.md` (drill ejecutado, RTO/RPO medidos); `docs/audit/backup-drills/2026-07-02-drill.md:3,21-22` (el drill en papel que esto reemplaza); `docs/operations/LAUNCH.md` (STAGING-001, sigue abierto) | **CERRADO (2026-08-24) — RESTORE-001 ejecutado** | Simulacro real hecho con "Restore to new project" sobre el backup del 24/08 03:45 UTC, a un proyecto efímero borrado al terminar (`docs/audit/backup-drills/2026-08-24-drill.md`). **RTO medido: menos de 5 minutos** para la base (35 MB); conteos, 101 policies RLS, 2 roles, 7 extensiones y 5 `auth.users` idénticos a la predicción. **CORRECCIÓN IMPORTANTE: la afirmación de que se salía "confiando en el PITR automático de Supabase Pro (7 días)" era FALSA** — PITR es un add-on y no está contratado. Lo que hay es un backup físico diario (~03:45 UTC / 00:45 ART) con 8 días de retención, así que el **RPO real es de hasta 24 horas**: el ensayo P-07 de ese mismo mediodía (2 reservas, 5 movimientos de caja, catálogo de cantina y el cierre del día) no existía en la base restaurada. Decisión del dueño (2026-08-24): se deja el RPO de 24 h documentado como hecho medido y no se contrata PITR — tolerable con complejos de prueba, se reabre cuando entre el primer cliente que cobre de verdad. **Queda abierto STAGING-001** (ambiente permanente, USD 10/mes) y sin cronometrar el tramo de repuntar Vercel/Railway al proyecto restaurado, que es el RTO del servicio y no el de la base |
| TG-P0-KILLSWITCH-01 | Operaciones / incident response | El campo de aceptación de riesgo del kill-switch global (deshabilitar reservas online / pagos MP / forzar confirmación manual a nivel plataforma) estaba vacío en `48h-post-launch-protocol.md:17` ("Riesgo aceptado por: _[completar]_"); no existe un kill-switch global en código, solo el per-tenant (`kill-switch.ts:29`). | 🟡 P1 (firma pendiente) | `48h-post-launch-protocol.md:17`; `src/shared/kill-switch.ts:29` (implementación per-tenant, sin equivalente global) | **Firmado — Aceptado por el Fundador** | El fundador acepta operar sin kill-switch global para el piloto: ante un incidente que requiera cortar toda la plataforma, la respuesta es per-tenant (`kill-switch.ts`) o rollback completo (ver Runbook). Actualizar `48h-post-launch-protocol.md:17` con esta firma |
