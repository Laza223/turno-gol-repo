# Triage de Fixes — TurnoGol

> Generado a partir del triage de `docs/qa/vistas_qa_exhaustivo.md`. Cada hallazgo accionable (bug, gap o riesgo) fue verificado contra el codigo fuente actual. Los hallazgos marcados como ya resueltos se listan aparte para trazabilidad. Ordenado por severidad (BLOCKER -> HIGH -> LOW), luego por categoria.

## Resumen

| Metrica | Cantidad |
|---|---|
| Hallazgos totales detectados | 183 |
| **Vigentes (a accionar)** | **176** |
| Ya resueltos / no vigentes | 7 |
| 🔴 BLOCKER | 8 |
| 🟡 HIGH | 61 |
| 🟢 LOW | 107 |
| Esfuerzo S / M / L | 115 / 58 / 3 |

## Hallazgos vigentes (priorizados)

| # | Vista | Categoria | Severidad | Descripcion | Archivo afectado | Esfuerzo |
|---|---|---|---|---|---|---|
| 1 | Transversal: Reservas/concurrencia + MercadoPago/webhooks + WebSockets/realtime | 🔒 Seguridad | 🔴 BLOCKER | MP_MOCK_ENABLED no tiene guard de NODE_ENV. Si MP_MOCK_MODE=1 se filtra a produccion, todos los webhooks se procesan inline con LocalMockGateway (sin gateway real, sin pg-boss), bypass completo del flujo de pagos. | `src/modules/payments/mock-mp.ts` | S |
| 2 | Transversal: Seguridad + Autenticacion/sesiones | 🔒 Seguridad | 🔴 BLOCKER | El campo `balance` (saldo deudor en centavos) especificado en CLAUDE.md para `player_tenant_relationships` no existe en el schema Drizzle ni en la migracion SQL. La funcion `createOnlineBooking` no ejecuta ningun chequeo de balance antes de crear la reserva, por lo que un jugador con deuda puede reservar online en el mismo complejo sin restriccion. | `src/shared/db/schema/player-tenant-relationships.ts` | M |
| 3 | Abonados (listado) | 🐛 Bug funcional | 🔴 BLOCKER | La ruta /suspended no existe: redirectIfTenantSuspended() redirige ahí pero no hay page.tsx, resultando en 404 cuando el tenant está suspendido por kill-switch. | `src/shared/kill-switch.ts` | S |
| 4 | Configuracion: Horarios | 🐛 Bug funcional | 🔴 BLOCKER | No hay validacion de que la hora de cierre sea posterior a la de apertura. El usuario puede guardar apertura=18:00/cierre=14:00 (o 22:00/06:00) sin error. En generateSlots, lastStart = closeMins - durationMins queda por debajo de openMins, por lo que el for no itera y ese dia genera CERO slots: la cancha aparece silenciosamente como no disponible para reservar online, sin ningun aviso al admin. Agregar validacion en updateHorariosAction (y opcionalmente en cliente) que rechace close <= open por dia. | `src/app/(admin)/settings/horarios/actions.ts` | M |
| 5 | Configuracion: PIN | 🐛 Bug funcional | 🔴 BLOCKER | La pagina /settings/pin descarta el resultado de setPinAction: el form castea la action a Promise<void> (action={setPinAction as unknown as (f: FormData) => Promise<void>}) y no usa useActionState ni ningun wrapper cliente. El backend devuelve {success,error} con mensajes correctos ('PIN actual incorrecto.', 'Los PINes no coinciden.', error Zod numerico, rate limit), pero la UI NUNCA los muestra. El usuario hace submit y no sabe si el PIN se guardo o si fallo. Cubre los casos del doc Error 400/429/500 y A11y (falta role='alert' en mensaje de error inexistente). Falta: wrapper con useActionState, UI de error (toast/alert inline), y confirmacion visual de exito. | `src/app/(admin)/settings/pin/page.tsx` | M |
| 6 | Settings (redirect) | 🐛 Bug funcional | 🔴 BLOCKER | La ruta /suspended referenciada en kill-switch.ts no existe: redirect('/suspended') termina en 404 en lugar de mostrar una pantalla de bloqueo operacional. | `src/shared/kill-switch.ts` | S |
| 7 | Transversal: Reservas/concurrencia + MercadoPago/webhooks + WebSockets/realtime | 🐛 Bug funcional | 🔴 BLOCKER | createOnlineBooking no verifica player_tenant_relationships.balance > 0 antes de permitir la reserva. Un jugador con deuda pendiente en un complejo puede seguir reservando online sin restriccion. | `src/modules/bookings/booking.service.ts` | M |
| 8 | Verificacion de magic link | 🐛 Bug funcional | 🔴 BLOCKER | Staff con N>1 tenants es redirigido a /select-tenant, pero esa ruta no existe en src/app. El usuario multi-tenant cae en un 404 y no puede elegir complejo ni entrar al panel. Tambien afecta el test [Multi-tenant - Staff N tenants] (linea 535) y el happy path de seleccion de tenant (linea 520). | `src/app/api/auth/callback/route.ts` | L |
| 9 | Configuracion: Facturacion (SaaS + MP) | 🔒 Seguridad | 🟡 HIGH ✅ | PinGate es un Client Component que recibe como children el <section> de Suscripcion ya renderizado con datos sensibles (plan, estado, proximo cobro) y el estado de conexion MP. La pagina (Server Component) los fetchea con getSubscriptionState ANTES de cualquier verificacion de PIN, por lo que la info de facturacion viaja en el payload RSC/HTML al navegador aunque el usuario nunca ingrese el PIN. El gate solo oculta visualmente: la data es leible inspeccionando la red. Fetchear sub/mpConnected solo despues de verificar la sesion PIN en el servidor. | `src/app/(admin)/settings/facturacion/page.tsx` | M |
| 10 | Configuracion: Facturacion (SaaS + MP) | 🔒 Seguridad | 🟡 HIGH ✅ | El state OAuth de MercadoPago codifica `${tenant.id}:${Date.now()}` y firma con HMAC, pero el callback nunca valida el timestamp: no hay ventana de expiracion ni binding a la sesion del usuario. Un state filtrado/copiado es reutilizable indefinidamente (replay). Validar que el ts del payload este dentro de una ventana corta (ej. 10 min) y rechazar lo expirado. | `src/app/api/mp/callback/route.ts` | S |
| 11 | Privacidad | 🔒 Seguridad | 🟡 HIGH ✅ | sentry.client.config.ts no aplica scrubObject/scrubQueryString en beforeSend: event.extra, event.contexts y query strings del cliente pueden filtrar PII (email, phone, tokens MP) a Sentry en errores del browser. El server config si scrubea, el client no. | `sentry.client.config.ts` | S |
| 12 | Staff | 🔒 Seguridad | 🟡 HIGH ✅ | resendInviteAction no verifica que el email pertenezca a un miembro del tenant actual antes de llamar inviteUserByEmail — un admin podría reenviar invitación a cualquier email arbitrario (jugador global, otro tenant) simplemente enviando el email como parámetro. | `src/app/(admin)/staff/actions.ts` | S |
| 13 | Staff | 🔒 Seguridad | 🟡 HIGH ✅ | inviteStaffAction y deactivateStaffAction no re-validan la sesión de PIN — si la cookie de PIN expiró (TTL 30 min) mientras el admin tenía la página abierta, las acciones se ejecutan igualmente sin exigir re-autenticación con PIN. | `src/app/(admin)/staff/actions.ts` | M |
| 14 | Staff | 🔒 Seguridad | 🟡 HIGH ✅ | Las Server Actions inviteStaffAction y deactivateStaffAction no verifican el estado del tenant (suspended/blocked/canceled) — getStaffTenant no filtra por status y las acciones no llaman al kill-switch, por lo que un admin de un tenant bloqueado puede ejecutarlas si llama la acción directamente. | `src/app/(admin)/staff/actions.ts` | S |
| 15 | Abonados (listado) | 🐛 Bug funcional | 🟡 HIGH ✅ | El preview de reactivación no pasa endsOn al llamar a previewAbonadoSlotsAction, por lo que muestra slots más allá de la fecha de fin del abonado. La acción real de reactivateAbonado sí respeta endsOn desde la DB, generando menos slots que los mostrados en el preview. | `src/app/(admin)/abonados/AbonadosList.tsx` | S |
| 16 | Caja diaria | 🐛 Bug funcional | 🟡 HIGH ✅ | El parametro ?date no se valida en page.tsx antes de usarse: se pasa crudo a getDaySummary/getCashFlows (cast SQL ${date}::date) y a addDays (que hace new Date(date+'T00:00:00Z').toISOString()). Un deep-link con ?date=2026-13-45 o texto basura provoca un error no controlado (Postgres rechaza el cast / Invalid Date -> toISOString throws) que escala a error.tsx en vez de mostrar el dia o un mensaje claro. El schema dateStr solo se aplica dentro de closeDayAction, no en el render. | `src/app/(admin)/caja/page.tsx` | S |
| 17 | Caja diaria | 🐛 Bug funcional | 🟡 HIGH ✅ | Si closeDayAction lanza un error inesperado mid-flight (ej. error de DB despues de validar, re-throw en daily-close.service), ConfirmDialog.handleConfirm hace await onConfirm() sin try/catch: la transicion queda rechazada, el boton se queda en 'Procesando...' e isPending bloquea el cierre del dialogo (handleOpenChange bail), dejando el modal colgado sin Sentry capture. RegisterMovementModal si captura y se recupera; CloseDayButton/ConfirmDialog no. | `src/components/ui/confirm-dialog.tsx` | M |
| 18 | Configuracion del jugador | 🐛 Bug funcional | 🟡 HIGH ✅ | DataExportButton descarga silenciosamente un archivo con contenido 'undefined' si el servidor retorna 200 con JSON malformado o sin el campo 'data'. `JSON.stringify(undefined)` no lanza excepcion; el Blob se crea con la cadena literal 'undefined' y el usuario recibe un JSON corrupto sin ningun mensaje de error. | `src/app/(player)/configuracion/DataExportButton.tsx` | S |
| 19 | Configuracion: Horarios | 🐛 Bug funcional | 🟡 HIGH ✅ | Las Server Actions retornan HorariosActionResult con {success,error} pero el cliente nunca lo consume: los tres forms usan action={accion as unknown as (f)=>Promise<void>} en un form HTML plano sin useFormState ni toast. Errores reales (Zod 'Formato HH:MM', 'PIN requerido.', rate limit, 'Tenant no encontrado.') se descartan: el usuario hace click en Guardar/Agregar/Quitar y no recibe ningun feedback de exito ni de error. Conectar el resultado via useFormState/useActionState + toast aria-live. | `src/app/(admin)/settings/horarios/page.tsx` | M |
| 20 | Configuracion: PIN | 🐛 Bug funcional | 🟡 HIGH ✅ | El boton de submit ('Configurar PIN'/'Cambiar PIN') no tiene estado de carga ni disabled durante la Server Action (no usa useFormStatus). Permite doble submit: si el usuario hace doble click, la action se ejecuta de nuevo; solo el rate limit adminCrud frena el segundo intento. Sin feedback visual de procesamiento, la UX queda ambigua. | `src/app/(admin)/settings/pin/page.tsx` | S |
| 21 | Configuracion: Politicas de reserva | 🐛 Bug funcional | 🟡 HIGH ✅ | El formulario nunca muestra feedback al usuario: ni exito ni error. La action updateReservasPolicyAction devuelve PolicyActionResult (success/error), pero el form la castea a Promise<void> (action={updateReservasPolicyAction as unknown as (f: FormData) => Promise<void>}) y descarta el resultado. Al guardar bien no hay confirmacion visible (solo revalidatePath silencioso); y peor, cuando la action devuelve {success:false,error:'PIN requerido.'} / 'Tenant no encontrado.' / rate-limit / 'Datos invalidos', el usuario no ve nada y cree que guardo. Resolver con un client component wrapper que use useActionState/useTransition y renderice el error/success. | `src/app/(admin)/settings/reservas/page.tsx` | M |
| 22 | Confirmacion de reserva (checkout jugador) | 🐛 Bug funcional | 🟡 HIGH ✅ | Los errores 'rate_limited' y 'unavailable' se redirigen pero la pagina no los muestra: el jugador vuelve a la pantalla de confirmacion sin ninguna alerta y no entiende por que no se proceso su reserva (doble-click bloqueado por rate limit, o cancha offline / precio no calculable). Agregar bloques de alerta para error=rate_limited y error=unavailable, analogos a los de slot_taken/banned. | `src/app/(public)/[slug]/reservar/page.tsx` | S |
| 23 | Confirmacion de reserva (checkout jugador) | 🐛 Bug funcional | 🟡 HIGH ⏭️ (diferido: ver decisiones_pendientes.md) | El bloqueo por saldo deudor (player_tenant_relationships.balance > 0 = jugador bloqueado para reservar online) NO esta implementado en el flujo de reserva online. createOnlineBooking solo verifica bans via checkPlayerBanned, que consulta players.status y tenant_player_bans, pero nunca el balance del PTR. Un jugador con deuda puede reservar online igual, violando la regla de negocio documentada. | `src/modules/bans/ban.service.ts` | M |
| 24 | Dashboard del admin | 🐛 Bug funcional | 🟡 HIGH ✅ | Si NEXT_PUBLIC_APP_URL no esta configurada, el link publico copiado queda como ruta relativa '/c/{slug}' (no URL absoluta). El boton igual muestra 'Copiado!' y marca public_link_shared=true, asi que el admin comparte un enlace inservible durante el paso critico del onboarding (Aha moment). No hay validacion ni fallback de host. | `src/app/(admin)/dashboard/page.tsx` | S |
| 25 | Dashboard del admin | 🐛 Bug funcional | 🟡 HIGH ✅ | Rollback silencioso al copiar el link bajo rate limit: markPublicLinkSharedAction hace `if (limited) return` sin propagar error, pero la UI ya seteo copied=true ('Copiado!') de forma optimista. El usuario cree que el link se compartio y que el checklist avanzo, pero al refrescar el dashboard el item sigue pendiente. Sin feedback de que la operacion no persistio. | `src/app/(admin)/dashboard/actions.ts` | M |
| 26 | Disponibilidad semanal | 🐛 Bug funcional | 🟡 HIGH ✅ | GET /api/public/availability sirve disponibilidad de tenants en estado no disponible (suspended/blocked/canceled/churned/deleted). La ruta solo valida que el tenant exista (getPublicTenant no filtra por status) y nunca chequea el set UNAVAILABLE que sí aplica la page. El test de QA (linea 1144) asume que devuelve not_found, pero el codigo devuelve la disponibilidad normalmente. Un complejo suspendido/dado de baja sigue exponiendo turnos via API publica. | `src/app/api/public/availability/route.ts` | S |
| 27 | Eliminar cuenta (jugador) | 🐛 Bug funcional | 🟡 HIGH ✅ | La página /login no maneja el query param ?deleted=1: no se muestra ningún mensaje de despedida ('Tu cuenta fue eliminada') al jugador redirigido tras la anonimización, dejando al usuario en la pantalla de login sin contexto. | `src/app/(auth)/login/page.tsx` | S |
| 28 | Grilla de canchas | 🐛 Bug funcional | 🟡 HIGH ✅ | La fecha del deep link /grilla?date=YYYY-MM-DD no se valida en servidor. dateStr se inyecta directo como ${dateStr}::date en la query SSR (page.tsx) y tambien en el GET /api/bookings. Una fecha inexistente/mal formada (ej. 2024-13-32 o '2024-99-99') provoca un error de cast de Postgres -> 500 que cae en error.tsx (Sentry + ErrorState 'No pudimos cargar la grilla'), en vez de degradar a 'hoy' o mostrar un mensaje claro. Falta validar formato y validez real de fecha antes de la query. | `src/app/(admin)/grilla/page.tsx` | S |
| 29 | Grilla de canchas | 🐛 Bug funcional | 🟡 HIGH ✅ | El calculo de 'slot pasado' (isPast) usa artNow capturado una sola vez en useEffect con deps [] y nunca se refresca. En una grilla del dia abierta sin recargar, un slot que ya paso (ej. hace 50 min) sigue mostrandose como futuro/clickeable (fondo blanco, cursor-pointer, abre BookingFormModal), permitiendo al admin intentar crear una reserva en un horario que ya transcurrio. No hay setInterval que recalcule artNow ni invalidacion por minuto. | `src/components/booking/BookingGrid.tsx` | S |
| 30 | Listado de reservas (admin) | 🐛 Bug funcional | 🟡 HIGH ✅ | El param de URL ?status= se pasa crudo al cast SQL ::booking_status sin validar contra un allowlist. Un valor invalido (ej. /reservas?status=foo o un typo) provoca 'invalid input value for enum booking_status' en Postgres, la query tira excepcion y se muestra error.tsx ('No pudimos cargar las reservas') en lugar de un listado vacio o de 'Todas'. Validar status contra FILTERS antes de filtrar (fallback a sin filtro). | `src/app/(admin)/reservas/page.tsx` | S |
| 31 | Mis reservas (jugador) | 🐛 Bug funcional | 🟡 HIGH ✅ | Cancelar una reserva cuyo complejo esta 'blocked' o 'deleted' rompe el dialog: cancelByPlayer lanza TenantInactiveError, pero el try/catch de cancelMyBookingAction solo captura BookingNotOwnedByPlayerError y BookingNotInConfirmedError y hace `throw err` para el resto. La excepcion se propaga como error no controlado de la Server Action; handleConfirm en CancelBookingButton no la captura y ConfirmDialog.handleConfirm tampoco envuelve `await onConfirm()` en try/catch, asi que no se muestra error inline y el usuario queda con el dialog abierto y un error generico de Next. Falta un catch de TenantInactiveError que retorne {success:false, error:'El complejo no esta disponible para cancelar online.'} | `src/app/(player)/mis-reservas/actions.ts` | S |
| 32 | Mock MercadoPago (solo testing) | 🐛 Bug funcional | 🟡 HIGH ✅ | page.tsx no valida que ?booking= sea un UUID antes de pasarlo al SQL. Un string no-UUID (ej: 'invalid-uuid') llega directamente a WHERE b.id = $1 en Postgres, que rechaza el tipo causando un error 500 en lugar del 404 esperado segun los test cases del documento. | `src/app/mock-mp/checkout/page.tsx` | S |
| 33 | Nuevo abonado | 🐛 Bug funcional | 🟡 HIGH ✅ | No hay validacion de timeEnd > timeStart en cliente ni en Zod. Si el admin ingresa hora fin <= hora inicio (ej 10:00 a 10:00), el preview se muestra OK y, al confirmar, la unica barrera es el CHECK constraint chk_abonado_time_valid de la DB. createAbonadoAction solo captura AbonadoConflictError en el catch y re-lanza el resto, por lo que la violacion del CHECK explota como error no manejado del Server Action (crash/error boundary) en lugar de mostrar 'Horario invalido' en la zona de alerta roja. Agregar un .refine() en previewSchema/schema que compare ambos tiempos. | `src/app/(admin)/abonados/nuevo/actions.ts` | S |
| 34 | Nuevo abonado | 🐛 Bug funcional | 🟡 HIGH ✅ | El preview NO detecta conflicto con un abonado activo existente en el mismo dia/horario/cancha: getAbonadoSlotConflicts solo consulta la tabla bookings, no abonados. El usuario ve todas las fechas como 'OK', confirma, y recien ahi createAbonado ejecuta checkAbonadoSlotConflict y lanza AbonadoConflictError ('Ya existe un turno fijo activo en ese horario'). El test del doc (linea 1519) espera que ese error aparezca en el preview, pero no lo hace. Incluir checkAbonadoSlotConflict en previewAbonadoSlotsAction. | `src/modules/abonados/abonado.service.ts` | M |
| 35 | Onboarding (wizard 4 pasos) | 🐛 Bug funcional | 🟡 HIGH ✅ | createTenantAction no verifica si el staff ya tiene un tenant antes de llamar a createTenantWithTrial. Si el usuario hace 'atras' del navegador y reenvia el Paso 1 (o re-renderiza Paso 1), se crea un tenant DUPLICADO + una segunda fila en tenant_staff_members, dejando datos inconsistentes. Agregar guard de idempotencia (getStaffTenant -> si existe, redirigir/abortar). | `src/app/onboarding/actions.ts` | S |
| 36 | Onboarding (wizard 4 pasos) | 🐛 Bug funcional | 🟡 HIGH ✅ | updateScheduleAction guarda openingHours sin ninguna validacion: no aplica openingHoursSchema (Zod) ni valida coherencia logica (cierre > apertura). Permite guardar horarios invertidos (apertura 22:00 / cierre 08:00) y, si se manipula el payload del cliente, formatos invalidos. Validar con openingHoursSchema y agregar regla cierre>apertura por dia abierto. | `src/app/onboarding/actions.ts` | M |
| 37 | Perfil del jugador | 🐛 Bug funcional | 🟡 HIGH ✅ | No try-catch en updateProfileAction alrededor de withPlayerContext: si la DB falla (timeout, conexion perdida), la exception se propaga como uncaught server error al cliente sin mensaje amigable ni reporte a Sentry. | `src/app/(player)/perfil/actions.ts` | S |
| 38 | Perfil del jugador | 🐛 Bug funcional | 🟡 HIGH ✅ | No se verifica affected rows despues del UPDATE: si el playerId no existe en DB, el UPDATE no afecta ninguna fila pero la action devuelve success:true, dando feedback falso de exito al usuario. | `src/app/(player)/perfil/actions.ts` | S |
| 39 | Perfil publico del complejo | 🐛 Bug funcional | 🟡 HIGH ✅ | AvailabilityGrid: al fallar el fetch de '/api/public/availability' (Dia siguiente/anterior), el catch silencia el error y solo deja loading=false, pero la fecha (date) ya se actualizo a la nueva mientras availability conserva los slots del dia anterior. Resultado: la etiqueta de fecha muestra el dia nuevo y la grilla muestra slots de otro dia, desincronizada y sin ningun aviso al usuario. Mostrar un toast/estado de error y/o revertir 'date' al dia cuyos datos siguen visibles. | `src/app/(public)/[slug]/components/AvailabilityGrid.tsx` | S |
| 40 | Perfil publico del complejo | 🐛 Bug funcional | 🟡 HIGH ✅ | FavoriteButton en el perfil publico siempre arranca sin favorito: TenantHeader lo renderiza con tenantId pero sin initialFavorited, y page.tsx nunca consulta el estado de favorito del jugador logueado. Para un jugador que ya marco el complejo como favorito, el boton muestra 'Guardar' (no 'Guardado') en cada carga/refresh. Esto contradice los casos de QA de Persistencia y Edge que esperan initialFavorited desde el servidor. Pasar initialFavorited consultando favorite.service para la sesion actual. | `src/app/(public)/[slug]/components/TenantHeader.tsx` | M |
| 41 | Registro de nuevo admin | 🐛 Bug funcional | 🟡 HIGH ✅ | registerAction envía magic link sin verificar si el email ya existe en staff_users. El usuario recibe el enlace pero nunca ve el mensaje 'Ya tenés una cuenta. ¿Querés agregar otro complejo?' especificado en US-ONB-001 y Flujo 1. | `src/app/(auth)/register/actions.ts` | M |
| 42 | Reportes mensuales | 🐛 Bug funcional | 🟡 HIGH ✅ | isEmpty ignora adjustments: si un período tiene solo cash_flows de tipo 'adjustment' (income=0, bookingCount=0), la página muestra 'Sin movimientos en este período.' aunque existan datos de ajustes reales con montos y métodos de pago. | `src/app/(admin)/reportes/page.tsx` | S |
| 43 | Reportes mensuales | 🐛 Bug funcional | 🟡 HIGH ✅ | byMethod en 'Por método de pago' mezcla income Y adjustment: el methodMap en fetchPeriodAgg itera sobre todos los typeRows sin filtrar por type='income', inflando los totales por método cuando existen adjustments. | `src/modules/reports/report.service.ts` | S |
| 44 | Reserva con error de pago | 🐛 Bug funcional | 🟡 HIGH ✅ | La pagina /error no tiene fallback `if (!booking)`. Cuando loadBooking devuelve null (booking inexistente, purgado por RGPD, o IDOR de otro jugador via RLS) la pagina igual renderiza el heading 'El pago no se proceso.' y el Link 'Reservar de nuevo' a '/'. Tergiversa el estado real al jugador en lugar de mostrar un 'No encontramos tu reserva.' como hace la pagina hermana de exito. No hay crash (la guarda `booking !== null` en linea 33 evita el TypeError que sugiere el doc QA), pero la UX es enganosa. | `src/app/reserva/[bookingId]/error/page.tsx` | S |
| 45 | Reserva exitosa (post-pago) | 🐛 Bug funcional | 🟡 HIGH ✅ | El cliente nunca transiciona a 'expired' por su cuenta: cuando ExpiryCountdown llega a 0:00, PaymentStatusWatcher sigue en 'Confirmando tu pago…' indefinidamente porque el estado 'expired' solo llega si el webhook/job lo actualiza en DB. Si el job de expiración se retrasa o falla, el jugador queda colgado sin feedback. Debería forzar status='expired' (o mostrar UI de expirado) cuando el countdown llega a 0 sin transición terminal. | `src/components/booking/PaymentStatusWatcher.tsx` | M |
| 46 | Reserva pendiente (ventana 15 min) | 🐛 Bug funcional | 🟡 HIGH ✅ | Si el polling de estado falla de forma persistente (5xx repetido, o booking que queda colgado en pending_payment tras expirar sin que el background job lo marque expired), el usuario ve el spinner 'Confirmando tu pago…' indefinidamente sin ningun feedback de error ni CTA para reintentar/salir. El catch {} silencioso y el `if (!res.ok) return` descartan cualquier 4xx/5xx sin mostrar nada. | `src/components/booking/PaymentStatusWatcher.tsx` | M |
| 47 | Staff | 🐛 Bug funcional | 🟡 HIGH ✅ | En inviteStaffAction, cuando Supabase devuelve 'already been registered', inviteData es null y el bloque updateUserById se saltea — el registro en staffUsers/tenantStaffMembers se crea/actualiza correctamente pero los app_metadata de auth (staff_user_id, tenant_id, role) nunca se sincronizan para ese usuario existente. | `src/app/(admin)/staff/actions.ts` | S |
| 48 | Transversal: Observabilidad/logging + Recuperacion ante fallos | 🐛 Bug funcional | 🟡 HIGH ✅ | PaymentStatusWatcher no tiene timeout client-side que transite a 'expired': si el backend expira la reserva pero el polling no recibe el estado antes de que el jugador deje la pestaña abierta, la UI queda en 'Confirmando tu pago...' indefinidamente. El ExpiryCountdown llega a cero pero el componente sigue en el branch pending_payment sin cambio de UI. | `src/components/booking/PaymentStatusWatcher.tsx` | S |
| 49 | Transversal: Observabilidad/logging + Recuperacion ante fallos | 🐛 Bug funcional | 🟡 HIGH ✅ | CloseDayButton.onConfirm no tiene try/catch: si closeDayAction lanza una excepción no controlada (cualquier error DB distinto de DayAlreadyCloseExistsError), la excepción no queda contenida en ConfirmDialog.handleConfirm (que tampoco tiene catch), dispara el error boundary de la página en lugar de mostrar un error inline, y el usuario pierde el contexto del dialog sin poder reintentar. | `src/app/(admin)/caja/components/CloseDayButton.tsx` | S |
| 50 | Transversal: Performance/stress + Accesibilidad global + Responsive/cross-browser | 🐛 Bug funcional | 🟡 HIGH ✅ | La tabla de movimientos en /caja tiene 6 columnas (Tipo, Categoría, Método, Descripción, Monto, Hora) sin wrapper overflow-x-auto ni min-width explícito, produciendo overflow horizontal en mobile (<640px). | `src/app/(admin)/caja/page.tsx` | S |
| 51 | Transversal: Performance/stress + Accesibilidad global + Responsive/cross-browser | 🐛 Bug funcional | 🟡 HIGH ✅ | BookingFormModal usa Dialog.Content con max-w-md pero sin max-h ni overflow-y-auto. En landscape mobile con Dynamic Island activo el modal supera el viewport y trunca los botones de acción. | `src/components/booking/BookingFormModal.tsx` | S |
| 52 | Transversal: Reservas/concurrencia + MercadoPago/webhooks + WebSockets/realtime | 🐛 Bug funcional | 🟡 HIGH ✅ | handleApproved confirma el booking sin comparar info.amount con booking.deposit_amount. Un pago aprobado por un monto menor al esperado transiciona la reserva a 'confirmed' de todas formas; la discrepancia solo queda registrada en el payment row pero no bloquea la confirmacion. | `src/modules/payments/payment.service.ts` | M |
| 53 | Transversal: Reservas/concurrencia + MercadoPago/webhooks + WebSockets/realtime | 🐛 Bug funcional | 🟡 HIGH ✅ | El calculo de refunds previos usa `description LIKE '%' + original.id + '%'` para linkear refunds al pago original. Si dos payment IDs comparten un prefijo UUID (improbable pero posible) o el description format cambia, la suma seria incorrecta y se permitiria sobre-reembolso. | `src/modules/payments/payment.service.ts` | M |
| 54 | Abonados (listado) | ⚠️ Riesgo | 🟡 HIGH ✅ | Missing filter UI: getAbonados acepta {status?} pero la page siempre llama con {} y AbonadosList no expone selectores de filtro. Gap de UX vs especificación (no hay forma de ver solo activos/pausados). | `src/app/(admin)/abonados/page.tsx` | M |
| 55 | Caja diaria | ⚠️ Riesgo | 🟡 HIGH ✅ | createCashFlow no tiene idempotency key ni constraint de unicidad: un doble submit o reintento de red puede crear movimientos de caja duplicados (registros financieros). El unico control es el boton disabled durante isPending en el cliente, que un retry de red burla. La tabla cash_flows no tiene unique index que lo prevenga. | `src/modules/cashflow/cashflow.service.ts` | M |
| 56 | Complejo suspendido | ⚠️ Riesgo | 🟡 HIGH ✅ | redirectIfTenantSuspended llama a isFeatureEnabled sin timeout: si la DB tarda, el admin layout bloquea indefinidamente antes de redirigir. El cache de 60s mitiga el steady-state pero no el cold path ni reinicios de proceso. | `src/shared/kill-switch.ts` | M |
| 57 | Eliminar cuenta (jugador) | ⚠️ Riesgo | 🟡 HIGH ✅ | No hay advertencia al jugador sobre reservas futuras confirmadas antes de anonimizar: el flujo permite eliminar la cuenta aunque existan bookings con status 'confirmed' en el futuro, y esos bookings quedan con player_id = NULL sin notificación al jugador ni al complejo. | `src/app/(player)/eliminar-cuenta/page.tsx` | M |
| 58 | Gestion de canchas | ⚠️ Riesgo | 🟡 HIGH | Cuando getCourtDeactivationImpactAction falla (error de red, rate limit, DB), CourtList asigna silenciosamente {futureBookings:0, activeAbonados:0} al estado impact y abre el ConfirmDialog sin ninguna advertencia. El admin puede desactivar una cancha creyendo que no tiene reservas futuras ni abonados activos cuando en realidad no se pudo obtener la información. | `src/app/(admin)/canchas/components/CourtList.tsx` | S |
| 59 | Home landing publica | ⚠️ Riesgo | 🟡 HIGH | Los catch de loadCities/loadFeatured/loadOpenMatches en page.tsx tragan silenciosamente las excepciones sin llamar a Sentry.captureException ni track de error. Si la DB falla de forma intermitente, la landing se renderiza vacía y el equipo no recibe ninguna alerta. | `src/app/page.tsx` | S |
| 60 | Mock MercadoPago (solo testing) | ⚠️ Riesgo | 🟡 HIGH | Fire-and-forget del webhook: mockPay/mockReject redirigen a /exito o /error independientemente de que el POST al webhook haya tenido exito. Si el webhook retorna 500/timeout, el booking queda atrapado en pending_payment indefinidamente sin ningun mecanismo de retry ni feedback al usuario. | `src/app/mock-mp/checkout/actions.ts` | M |
| 61 | Privacidad | ⚠️ Riesgo | 🟡 HIGH | El JSDoc de GET /api/player/data-export declara 'Logs the access in audit_logs (action=player.data_exported)' pero el cuerpo de la funcion NO escribe ningun audit log: el comentario inline en linea 117 lo reconoce explicitamente. Hay una discrepancia entre la documentacion interna del codigo y la implementacion real, y la trazabilidad de compliance ARCO (Art. 22 Ley 25.326) queda sin registro. | `src/app/api/player/data-export/route.ts` | M |
| 62 | Reportes mensuales | ⚠️ Riesgo | 🟡 HIGH | dateStr valida solo formato YYYY-MM-DD pero no validez calendario: fechas como '2026-02-31' o '2026-00-15' pasan la validación Zod del endpoint CSV, generando rangos inválidos silenciosos en new Date(). | `src/shared/validation/primitives.ts` | S |
| 63 | Reserva exitosa (post-pago) | ⚠️ Riesgo | 🟡 HIGH | El polling de estado no detecta fallo permanente: el catch del fetch es silencioso y reintenta cada 3s indefinidamente sin backoff exponencial ni UI de error tras N intentos. Si /status responde 5xx o la red cae, el jugador no recibe ninguna señal y el spinner gira para siempre. | `src/components/booking/PaymentStatusWatcher.tsx` | M |
| 64 | Settings (redirect) | ⚠️ Riesgo | 🟡 HIGH | El layout admin no verifica tenant.status para bloquear acceso al panel: tenants con status 'blocked', 'churned', 'canceled' o 'deleted' pueden acceder a todo el admin sin restricción de UI. | `src/app/(admin)/layout.tsx` | M |
| 65 | Terminos y condiciones | ⚠️ Riesgo | 🟡 HIGH | TERMS_VERSION esta hardcodeado como 'v1' en la Server Action. Si se actualiza el contenido de /terms sin cambiar esta constante, los jugadores existentes no seran re-promovidos a re-aceptar los nuevos terminos; el campo agreed_to_terms_at queda desactualizado silenciosamente. No hay ninguna comprobacion de version en getOrCreatePlayer ni en el middleware. | `src/app/(public)/[slug]/reservar/actions.ts` | M |
| 66 | Transversal: Reservas/concurrencia + MercadoPago/webhooks + WebSockets/realtime | ⚠️ Riesgo | 🟡 HIGH | La notificacion de late-payment al admin solo se encola cuando el booking esta en TERMINAL_BOOKING_STATUSES. Si el webhook aprobado llega mientras la booking aun esta en 'pending_payment' (race con el job de expiry) o si la fila no se encuentra, won=false no genera ninguna alerta y el cobro queda sin seguimiento. | `src/modules/payments/payment.service.ts` | M |
| 67 | Configuracion: PIN | 🏗️ Arquitectura | 🟡 HIGH | Brecha arquitectonica transversal en /settings/*: todas las paginas (pin, reservas, horarios, facturacion) usan el mismo anti-patron de castear Server Actions que retornan {success,error} a Promise<void>, sin infraestructura de UI para mostrar resultado. updateReservasPolicyAction, updateHorariosAction, addClosedDateAction y removeClosedDateAction quedan igualmente ciegas a errores. Conviene un wrapper cliente reutilizable (useActionState + toast) para toda la familia settings. | `src/app/(admin)/settings/reservas/page.tsx` | L |
| 68 | Transversal: Performance/stress + Accesibilidad global + Responsive/cross-browser | ♿ Accesibilidad | 🟡 HIGH | Los botones de toggle de duración (60/120 min) en BookingFormModal no tienen aria-pressed ni están agrupados con role=group + aria-labelledby. Un lector de pantalla no puede saber cuál opción está seleccionada. | `src/components/booking/BookingFormModal.tsx` | S |
| 69 | Transversal: Performance/stress + Accesibilidad global + Responsive/cross-browser | ♿ Accesibilidad | 🟡 HIGH | El banner de estado offline en BookingGrid no tiene aria-live ni role=status, por lo que el cambio de conexión (CONNECTED → OFFLINE) no se anuncia a lectores de pantalla. | `src/components/booking/BookingGrid.tsx` | S |
| 70 | Configuracion: Facturacion (SaaS + MP) | 🔒 Seguridad | 🟢 LOW | verifyPinAction aplica rate-limit (5 intentos/5min) pero no registra los intentos de PIN (exitosos ni fallidos) en audit_logs, pese a que el modulo de auditoria existe y se usa en billing. Si compliance/forensia lo requiere, falta trazabilidad de accesos a zonas protegidas. | `src/app/(admin)/actions/pin.ts` | S |
| 71 | Gestion de canchas | 🔒 Seguridad | 🟢 LOW | La sesión PIN en PinGate se verifica una única vez en el mount (useEffect con dependencia [pinRequired]). Si la cookie PIN expira mientras el usuario tiene la página abierta, el componente sigue mostrando verified=true y permite operaciones protegidas hasta el próximo mount o refresh manual. | `src/components/pin-gate.tsx` | M |
| 72 | Mis reservas (jugador) | 🔒 Seguridad | 🟢 LOW | La vista /mis-reservas no valida player_status ('banned'/'anonymized') ni tenant_player_bans: carga todas las reservas del jugador en ese complejo aunque haya sido baneado. El propio documento marca que 'puede ser intencional', y el aislamiento por player_id via RLS (player_own_bookings_select) sigue garantizado, por lo que no hay fuga entre jugadores; solo es una decision de producto a confirmar (mostrar/ocultar historial a un baneado). | `src/app/(player)/mis-reservas/page.tsx` | M |
| 73 | Mock MercadoPago (solo testing) | 🔒 Seguridad | 🟢 LOW | Si MP_MOCK_MODE='1' quedara activo accidentalmente en produccion, cualquier usuario anonimo podria invocar mockPay() con un bookingId ajeno y simular un pago aprobado sin autenticacion, ya que las actions no verifican que el tenantId resuelto corresponda a ninguna sesion autenticada (IDOR potencial). | `src/app/mock-mp/checkout/actions.ts` | S |
| 74 | Reserva pendiente (ventana 15 min) | 🔒 Seguridad | 🟢 LOW | La policy de rate limit 'bookingStatus' usa failMode='open': si Upstash/Redis se cae, el polling de estado queda sin limite, habilitando polling ilimitado durante un outage del rate limiter. Es un tradeoff aceptable para un endpoint de solo lectura, pero queda sin proteccion en degradacion. | `src/shared/rate-limit/policies.ts` | S |
| 75 | Transversal: Seguridad + Autenticacion/sesiones | 🔒 Seguridad | 🟢 LOW | La ruta `/mock-mp` no figura en el disallow de robots.txt y la pagina `/mock-mp/checkout/page.tsx` no exporta `metadata.robots = { index: false, follow: false }`, a diferencia de `/suspended` que si lo hace. Un crawler podria indexar la pagina de pago simulado. | `src/app/robots.ts` | S |
| 76 | Abonados (listado) | 🐛 Bug funcional | 🟢 LOW | El <tr> wrapper que monta AbonadoDialogs dentro del <tbody> es HTML inválido (tr con td colSpan que solo sirve de anchor para un Radix Portal). Puede causar warnings de hidratación en SSR. | `src/app/(admin)/abonados/AbonadosList.tsx` | S |
| 77 | Configuracion del jugador | 🐛 Bug funcional | 🟢 LOW | Si player.firstName es cadena vacia (permitida por la DB pese al notNull — puede insertarse via migration o admin directo), el saludo renderiza 'Hola , gestioná tus datos.' con coma flotante sin nombre, lo que es visiblemente incorrecto. | `src/app/(player)/configuracion/page.tsx` | S |
| 78 | Configuracion: Horarios | 🐛 Bug funcional | 🟢 LOW | La lista de Dias cerrados no filtra fechas pasadas: se hace [...closedDates].sort().map() sin filtrar d >= hoy. Si el admin no las elimina manualmente, las fechas ya vencidas se acumulan y ensucian la UI indefinidamente. Filtrar al renderizar (o limpiar via cron) las fechas anteriores a hoy. | `src/app/(admin)/settings/horarios/page.tsx` | S |
| 79 | Configuracion: Horarios | 🐛 Bug funcional | 🟢 LOW | addClosedDateAction valida fecha con regex /^\d{4}-\d{2}-\d{2}$/ pero no rechaza fechas calendario imposibles (p.ej. 2025-02-30 o 2025-13-01). Un cliente que haga bypass del input type=date puede persistir una fecha invalida en closedDates. Validar con un parseo real de fecha (Date / Temporal) ademas del regex. | `src/app/(admin)/settings/horarios/actions.ts` | S |
| 80 | Configuracion: Politicas de reserva | 🐛 Bug funcional | 🟢 LOW | No hay estado pending/disabled del boton 'Guardar cambios' durante el submit. La pagina es Server Component con <form action> plano y <Button type='submit'> estatico, sin useTransition/useFormStatus. Permite multiples clics rapidos en el cliente (aunque el server-side es idempotente y hay adminRateLimited como red de seguridad), y no da feedback de carga. Mitigado parcialmente por rate limit, por eso LOW. | `src/app/(admin)/settings/reservas/page.tsx` | S |
| 81 | Configuracion: Politicas de reserva | 🐛 Bug funcional | 🟢 LOW | Mensajes de validacion genericos y sin asociar a campo. La action devuelve parsed.error.issues[0]?.message (Zod por defecto, sin mensajes custom por campo) y, como el form descarta el resultado, ni siquiera se muestra. Caso concreto: depositPercentage vacio o no-numerico llega como '' -> Number('')=0 -> falla min(10) con mensaje generico que el usuario nunca ve. Mejora menor de UX, queda cubierta al implementar el renderizado de errores (finding HIGH). | `src/app/(admin)/settings/reservas/actions.ts` | S |
| 82 | Detalle de reserva | 🐛 Bug funcional | 🟢 LOW | cancelBookingAction/cancelByAdmin no revalida server-side el largo minimo del motivo de cancelacion (>= 3 chars). La unica validacion esta en BookingActions.tsx (cliente). Un cliente que evite el form puede persistir canceledReason vacio o basura, ensuciando el audit log y la seccion 'Motivo de cancelacion'. | `src/modules/bookings/booking.cancellation.ts` | S |
| 83 | Detalle de reserva | 🐛 Bug funcional | 🟢 LOW | Formato de montos inconsistente: page.tsx formatea ARS con maximumFractionDigits 0 (ej. '$100' en la tarjeta de detalle) pero BookingActions.tsx usa el default (2 decimales, ej. '$100,00') en el aviso de reembolso del dialog Cancelar. El mismo monto de sena se ve distinto en la pagina y en el dialog. | `src/app/(admin)/reservas/[id]/BookingActions.tsx` | S |
| 84 | Eliminar cuenta (jugador) | 🐛 Bug funcional | 🟢 LOW | PlayerAlreadyAnonymizedError se trata silenciosamente como success en requestDeleteAccountAction: el jugador es redirigido a /login?deleted=1 sin ningún mensaje explicativo, lo que puede confundir en el caso de doble tab o reintento. | `src/app/(player)/eliminar-cuenta/actions.ts` | S |
| 85 | Explorar complejos | 🐛 Bug funcional | 🟢 LOW | ExplorarFilters no valida que minPrice <= maxPrice al aplicar filtros. Si el usuario ingresa min=1000 y max=100, la URL se construye con ambos valores, el backend aplica ambas condiciones (fromPriceCents >= 100000 AND fromPriceCents <= 10000) y siempre devuelve 0 resultados sin feedback visual de que el rango es inválido. | `src/app/(public)/explorar/components/ExplorarFilters.tsx` | S |
| 86 | Explorar complejos | 🐛 Bug funcional | 🟢 LOW | FavoriteButton siempre inicializa con initialFavorited=false en TenantCard (prop no se pasa desde el servidor), por lo que tras un F5 el corazon vuelve a estado 'no favorito' aunque el jugador lo haya marcado previamente. El estado de favorito no es persistente entre recargas. | `src/app/(public)/explorar/components/TenantCard.tsx` | M |
| 87 | Home landing publica | 🐛 Bug funcional | 🟢 LOW | listPublicCities agrupa por (city, province) devolviendo filas únicas por par, pero HeroSearch renderiza las opciones con value={c.city} (sin province). Si dos provincias tienen una ciudad homónima (ej. 'Rivadavia' en Mendoza y San Juan), el <select> muestra dos opciones visualmente distintas ('{city}, {province}') con el mismo value, lo que puede confundir al usuario aunque el filtro resultante sea idéntico. | `src/components/site/HeroSearch.tsx` | S |
| 88 | Listado de reservas (admin) | 🐛 Bug funcional | 🟢 LOW | formatARS usa Intl.NumberFormat con maximumFractionDigits: 0 sobre cents/100, por lo que montos con centavos no enteros se redondean (half-expand, no truncamiento). Si llegara un price_snapshot que no sea multiplo de 100 (ej. 50¢ -> $1, 150¢ -> $2), el monto mostrado difiere del real. Bajo impacto porque los precios suelen ser pesos enteros, pero el redondeo oculta el valor exacto. | `src/app/(admin)/reservas/page.tsx` | S |
| 89 | Login (magic link) | 🐛 Bug funcional | 🟢 LOW | El mensaje de error de email invalido no coincide con la copy especificada. La accion devuelve hardcoded 'Email inválido.' ignorando el mensaje del schema Zod ('Ingresá un email válido'). El doc QA (y la vista register, que si surface issue.message) esperan 'Ingresá un email válido.'. Inconsistencia de copy entre login y register. | `src/app/(auth)/login/actions.ts` | S |
| 90 | Mis reservas (jugador) | 🐛 Bug funcional | 🟢 LOW | El badge de estado no tiene label para 'expired'. La query de la vista filtra solo por fecha (b.date >= today para Proximos), sin filtrar por status, asi que reservas que pasaron de pending_payment a 'expired' aparecen en Proximos. Como STATUS_LABELS y STATUS_CLASSES no incluyen la clave 'expired', el badge muestra el texto crudo 'expired' (fallback `?? b.status`) con estilo de 'completed', en ingles y sin sentido para el jugador. Conviene excluir 'expired' del listado o mapear un label en espanol. | `src/app/(player)/mis-reservas/page.tsx` | S |
| 91 | Onboarding (wizard 4 pasos) | 🐛 Bug funcional | 🟢 LOW | Race condition de slug: dos requests simultaneos con el mismo nombre pueden pasar generateUniqueSlug (que solo lee) y colisionar en el INSERT contra el UNIQUE de tenants.slug. createTenantAction no envuelve createTenantWithTrial en try/catch, asi que la violacion UNIQUE explota como excepcion no manejada y el usuario queda en 'Creando...' sin error claro. | `src/app/onboarding/actions.ts` | M |
| 92 | Para complejos (marketing) | 🐛 Bug funcional | 🟢 LOW | El mock de grilla en ShowcaseStrip muestra slots de 15 minutos (18:00, 18:15, 18:30, 18:45…) pero el producto solo soporta duraciones de 60 o 120 minutos. La página de marketing muestra una granularidad inexistente en el producto real, lo que puede generar expectativas incorrectas en potenciales clientes. | `src/app/(public)/para-complejos/page.tsx` | S |
| 93 | Perfil del jugador | 🐛 Bug funcional | 🟢 LOW | Avatar URL rota o 404 muestra imagen vacia sin fallback a iniciales: la logica usa if(player.avatarUrl) para decidir entre <Image> e iniciales, pero si el URL existe en DB y la imagen no carga, no hay onError handler para volver a las iniciales. | `src/app/(player)/perfil/page.tsx` | S |
| 94 | Perfil publico del complejo | 🐛 Bug funcional | 🟢 LOW | Validacion de fecha por regex /^\d{4}-\d{2}-\d{2}$/ no valida dias/meses calendario reales. Una fecha invalida como 2026-02-30 que caiga lexicograficamente dentro de la ventana todayStr..maxStr pasa la validacion y luego Date.UTC(2026,1,30) hace rollover a un dia valido distinto (ej. 2026-03-02), mostrando silenciosamente la disponibilidad de OTRO dia. Validar fecha real (ej. reconstruir y comparar) tanto en page.tsx como en el primitive dateStr de la API. | `src/app/(public)/[slug]/page.tsx` | S |
| 95 | Registro de nuevo admin | 🐛 Bug funcional | 🟢 LOW | Phone regex /^\+?54\s?9?\s?\d{2,4}\s?\d{4}-?\d{4}$/ tiene 9? (opcional) lo que acepta números sin prefijo móvil (ej: '+54 11 1234-5678') y rechaza números de interior legítimos con 3 dígitos de área más 7 dígitos locales (ej: '+54 9 226 123-4567'). | `src/app/(auth)/register/actions.ts` | S |
| 96 | Reserva con error de pago | 🐛 Bug funcional | 🟢 LOW | Al exceder el rate limit playerBooking (20/60s) en 'Reintentar pago', retryDepositPaymentAction hace redirect a la misma /reserva/${bookingId}/error sin query ni mensaje, y la pagina no tiene banner/toast para 429. El jugador hace clic y aparentemente no pasa nada (recarga muda), UX opaca: no sabe que debe esperar. | `src/app/(public)/[slug]/reservar/actions.ts` | M |
| 97 | Reserva exitosa (post-pago) | 🐛 Bug funcional | 🟢 LOW | El botón 'Cómo llegar' construye la URL de Google Maps con address+city sin validar que sean una ubicación resoluble; si el complejo tiene address vacío o ambiguo (y no hay lat/lng), el link puede no encontrar nada. Conviene deshabilitar/ocultar la acción cuando no hay geo ni dirección útil. | `src/components/booking/BookingSuccessExtras.tsx` | S |
| 98 | Reserva pendiente (ventana 15 min) | 🐛 Bug funcional | 🟢 LOW | PaymentStatusWatcher no tiene caso de render para status 'completed' ni 'no_show', pero ambos estan en TERMINAL_STATUSES (detienen el polling). Un booking en esos estados cae al render por defecto y muestra para siempre 'Confirmando tu pago…' con el countdown, sin volver a poll. Aunque en el flujo v1 es poco probable llegar a esos estados desde esta pantalla, es un render incorrecto latente. | `src/components/booking/PaymentStatusWatcher.tsx` | S |
| 99 | Staff | 🐛 Bug funcional | 🟢 LOW | El dialog de confirmación de desactivación compara el texto ingresado con el email de forma case-sensitive (typed.trim() === confirmationPhrase), pero no normaliza la entrada — si el usuario escribe el email con alguna mayúscula no puede confirmar aunque el email en DB esté en lowercase. | `src/components/ui/confirm-dialog.tsx` | S |
| 100 | Abonados (listado) | ⚠️ Riesgo | 🟢 LOW | El date input de cancelación solo tiene min=todayART() pero no tiene max vinculado a endsOn del abonado. El admin puede elegir una fecha posterior a endsOn, el backend la acepta y elimina reservas (que de todos modos no existirían), pero la UX es confusa. | `src/app/(admin)/abonados/AbonadoDialogs.tsx` | S |
| 101 | Abonados (listado) | ⚠️ Riesgo | 🟢 LOW | todayART() calcula la fecha con offset fijo UTC-3 hardcodeado. Frágil ante cualquier cambio de política horaria en Argentina (actualmente sin DST, pero sin garantía futura). | `src/app/(admin)/abonados/AbonadosList.tsx` | S |
| 102 | Caja diaria | ⚠️ Riesgo | 🟢 LOW | La conversion UTC->ART usa offset hardcodeado -3h en milisegundos (artDateOf: ts.getTime() - 3*3600_000) en page.tsx, cashflow.service.ts y RegisterMovementModal, mientras las queries SQL usan AT TIME ZONE 'America/Argentina/Buenos_Aires'. Hoy coinciden (Argentina sin DST), pero el offset manual es fragil ante cualquier cambio de zona/DST y puede desajustarse respecto a la fecha-ART calculada por Postgres. Conviene unificar con la IANA tz en ambos lados. | `src/app/(admin)/caja/page.tsx` | M |
| 103 | Caja diaria | ⚠️ Riesgo | 🟢 LOW | En RegisterMovementModal, si la action lanza una excepcion de DB (ej. constraint), Sentry captura pero el usuario solo ve 'No pudimos registrar el movimiento. Revisa tu conexion...' generico, atribuyendo a la red un fallo de servidor, sin un flujo de reintento estructurado ni distincion del tipo de error. | `src/app/(admin)/caja/components/RegisterMovementModal.tsx` | S |
| 104 | Complejo suspendido | ⚠️ Riesgo | 🟢 LOW | Botón 'Contactar a soporte' es mailto: puro sin fallback: en entornos donde no hay cliente de email configurado (webmail corporativo, dispositivos sin app de correo) el usuario queda sin salida visible para contactar soporte. | `src/app/(public)/suspended/page.tsx` | S |
| 105 | Complejo suspendido | ⚠️ Riesgo | 🟢 LOW | La página /suspended no emite ningún evento de telemetría (Sentry, log estructurado, analytics) cuando es renderizada. Sin este dato no es posible medir cuántos tenants están suspendidos activamente ni por cuánto tiempo, información operacional relevante para el negocio. | `src/app/(public)/suspended/page.tsx` | M |
| 106 | Configuracion del jugador | ⚠️ Riesgo | 🟢 LOW | El endpoint /api/player/data-export no registra el acceso en audit_logs pese a que el JSDoc de la funcion afirma que si lo hace (linea 25-27). La trazabilidad de descargas ARCO (Art. 22 Ley 25.326) depende solo de logs de Vercel/Sentry, no de un registro estructurado en base de datos. | `src/app/api/player/data-export/route.ts` | M |
| 107 | Configuracion: Facturacion (SaaS + MP) | ⚠️ Riesgo | 🟢 LOW | Cuando getSubscriptionState falla (timeout DB, tenant inconsistente, etc.) el catch asigna sub=null y la pagina muestra el mismo texto 'Todavia no tenes una suscripcion activa' que para un tenant realmente sin plan. El admin con suscripcion no distingue un error transitorio de un estado real; UX silenciosa sin toast ni hint. Diferenciar el caso de error del caso 'sin suscripcion'. | `src/app/(admin)/settings/facturacion/page.tsx` | S |
| 108 | Configuracion: Facturacion (SaaS + MP) | ⚠️ Riesgo | 🟢 LOW | getSubscriptionState se ejecuta dentro del render SSR sin timeout explicito; no hay statement_timeout configurado en el cliente DB. Un query colgado bloquea el render de la pagina (el try-catch no protege contra un await que nunca resuelve). Agregar un timeout explicito o statement_timeout. | `src/app/(admin)/settings/facturacion/page.tsx` | M |
| 109 | Configuracion: Facturacion (SaaS + MP) | ⚠️ Riesgo | 🟢 LOW | Tras conectar MercadoPago el callback redirige a /dashboard, no a /settings/facturacion, y la pagina no ofrece refresh manual ni auto-refresh del estado de suscripcion/MP. El admin que vuelve a Facturacion ve el estado nuevo solo por SSR en la proxima carga, sin affordance explicito de actualizar. | `src/app/api/mp/callback/route.ts` | S |
| 110 | Configuracion: Horarios | ⚠️ Riesgo | 🟢 LOW | Ningun form tiene loading/disabled state durante el submit: son forms HTML puros sin useFormStatus ni boton disabled mientras la accion corre. El usuario puede hacer doble click. En updateHorarios el UPDATE es idempotente (mismo tenant.id), pero genera queries redundantes y mala percepcion de UX (sin spinner). Agregar useFormStatus para deshabilitar el boton y mostrar estado de carga. | `src/app/(admin)/settings/horarios/page.tsx` | M |
| 111 | Configuracion: Horarios | ⚠️ Riesgo | 🟢 LOW | Race condition leve en addClosedDateAction: lee existing = tenant.closedDates, evalua !existing.includes(date) y luego escribe [...existing, date]. Dos requests simultaneos pueden leer el mismo array base y producir duplicados o pisar uno al otro (last-write-wins sobre el JSONB completo). Resolver con dedupe en el UPDATE (array a nivel DB) o lectura/escritura dentro de la misma transaccion con lock. | `src/app/(admin)/settings/horarios/actions.ts` | M |
| 112 | Configuracion: PIN | ⚠️ Riesgo | 🟢 LOW | setPinAction no tiene cobertura de tests. El test pin.test.ts solo cubre checkPinSessionAction y verifyPinAction. La logica de cambio (verificacion de PIN actual, refine de coincidencia, persistencia de hash, gating por sesion PIN) no esta testeada. | `tests/unit/pin.test.ts` | M |
| 113 | Configuracion: PIN | ⚠️ Riesgo | 🟢 LOW | No hay validacion de que el nuevo PIN sea distinto del actual: changePinSchema solo valida formato y que newPin==confirmPin. Un admin puede 'cambiar' el PIN al mismo valor sin aviso. El propio doc QA lo marca como aceptable por UX, por lo que es solo una mejora menor opcional. | `src/app/(admin)/settings/pin/actions.ts` | S |
| 114 | Confirmacion de reserva (checkout jugador) | ⚠️ Riesgo | 🟢 LOW | Si la fecha es hoy y la hora ya paso, el slot se marca status='past' y la pagina muestra el mensaje generico 'Ese turno ya no está disponible' en vez de 'Ese horario ya pasó'. No es bloqueante (no se crea booking) pero el mensaje no orienta al jugador sobre la causa real. | `src/app/(public)/[slug]/reservar/page.tsx` | S |
| 115 | Detalle de reserva | ⚠️ Riesgo | 🟢 LOW | BookingActions devuelve null cuando status != 'confirmed' sin ningun mensaje. Si la query retornara un estado inesperado/invalido, el admin veria la tarjeta de detalle pero sin botones ni explicacion del por que faltan las acciones (el estado igual se muestra como label en la tarjeta, mitigando parcialmente). | `src/app/(admin)/reservas/[id]/BookingActions.tsx` | S |
| 116 | Disponibilidad semanal | ⚠️ Riesgo | 🟢 LOW | AvailabilityGrid (grilla con navegacion Prev/Next, usada en la home del complejo) silencia el error de fetch en loadDate() manteniendo datos stale sin avisar al usuario que la carga fallo. El usuario cree estar viendo la fecha nueva (setDate ya cambio la fecha mostrada) pero la grilla sigue mostrando la disponibilidad anterior. Nota: la page /disponibilidad NO usa este componente (usa WeeklyAvailability con tabs locales sin fetch). | `src/app/(public)/[slug]/components/AvailabilityGrid.tsx` | S |
| 117 | Disponibilidad semanal | ⚠️ Riesgo | 🟢 LOW | AvailabilityGrid.loadDate() castea la respuesta como AvailabilityResponse sin validar la forma del JSON antes de setAvailability(). Si la API devolviera un 200 con shape inesperado, buildTimeRows(availability.courts) iteraria sobre undefined y crashearia el render. Mitigado parcialmente porque las respuestas de error usan status no-2xx (res.ok=false dispara throw), pero no hay validacion runtime de la forma. | `src/app/(public)/[slug]/components/AvailabilityGrid.tsx` | S |
| 118 | Disponibilidad semanal | ⚠️ Riesgo | 🟢 LOW | WeeklyAvailability accede a week.days[active] con non-null assertion (day = week.days[active]!) y luego lee day.courts.length. Si days[] llegara vacio, day seria undefined y day.courts crashearia el render con TypeError. Actualmente no es triggerable porque getPublicWeeklyAvailability siempre construye exactamente 7 dias, pero la assertion no defensiva deja la vista fragil ante un cambio de contrato del servidor. | `src/app/(public)/[slug]/disponibilidad/components/WeeklyAvailability.tsx` | S |
| 119 | Explorar complejos | ⚠️ Riesgo | 🟢 LOW | Vista mapa descarta silenciosamente complejos sin latitude/longitude: aparecen en la lista pero no en el mapa, sin ningun aviso al usuario de cuantos fueron omitidos. Solo muestra mensaje cuando TODOS carecen de coordenadas. | `src/app/(public)/explorar/components/ExplorarMap.tsx` | S |
| 120 | Explorar complejos | ⚠️ Riesgo | 🟢 LOW | Offset arbitrariamente grande en URL (ej ?offset=999999) no tiene validacion ni redirect a pagina 1: el backend aplica el offset, devuelve 0 resultados y la UI muestra estado vacio identico a 'sin resultados por filtros', sin indicar que se trata de un offset fuera de rango. | `src/app/(public)/explorar/page.tsx` | S |
| 121 | Gestion de canchas | ⚠️ Riesgo | 🟢 LOW | El mensaje de error de gaps de pricing muestra solo los primeros 3 slots sin cubrir (slice(0,3)) y no hay forma en la UI de ver el listado completo. Si hay muchos gaps el admin no puede saber el alcance real del problema. | `src/app/(admin)/canchas/actions.ts` | S |
| 122 | Gestion de canchas | ⚠️ Riesgo | 🟢 LOW | No hay indicador visual del límite de canchas del plan antes de que el usuario intente crear. El límite solo se descubre al hacer submit y recibir el error 409. Un banner o label 'X/3 canchas usadas' evitaría el intento fallido. | `src/app/(admin)/canchas/components/CourtList.tsx` | M |
| 123 | Gestion de canchas | ⚠️ Riesgo | 🟢 LOW | No hay validación (ni client-side ni server-side) que detecte reglas de pricing con días/horarios superpuestos. Dos reglas que se solapan no generan error; calculatePrice usa la primera que matchea (orden del array), lo que puede producir precios inconsistentes e impredecibles sin que el admin sea advertido. | `src/app/(admin)/canchas/components/CourtForm.tsx` | M |
| 124 | Listado de reservas (admin) | ⚠️ Riesgo | 🟢 LOW | FILTERS solo expone 5 estados (Todas, Confirmadas, Pago pendiente, Completadas, Ausentes). No hay filtro directo para reservas canceladas (canceled_refunded / canceled_no_refund) ni expiradas, aunque la query si las soporta. El admin debe revisar 'Todas' y escanear visualmente para encontrar canceladas. Gap de funcionalidad menor. | `src/app/(admin)/reservas/page.tsx` | S |
| 125 | Listado de reservas (admin) | ⚠️ Riesgo | 🟢 LOW | LIMIT 200 fijo sin paginacion ni indicador de 'hay mas'. Un tenant con >200 reservas no puede ver ni filtrar las restantes desde esta vista; el corte es silencioso (no hay aviso al usuario). Documentado como 'por diseno' pero queda como limitacion conocida sin feedback visual. | `src/app/(admin)/reservas/queries.ts` | M |
| 126 | Login (magic link) | ⚠️ Riesgo | 🟢 LOW | No hay timeout visual en conexion lenta (Riesgo 5). Si signInWithOtp tarda mucho, el boton solo queda disabled via useFormStatus sin skeleton, mensaje de timeout ni feedback adicional; el usuario no sabe si sigue procesando o se colgo. | `src/app/(auth)/login/page.tsx` | M |
| 127 | Mis reservas (jugador) | ⚠️ Riesgo | 🟢 LOW | artToday() calcula 'hoy ART' como `new Date(Date.now() - 3*3600_000).toISOString().slice(0,10)`, lo cual solo es correcto si el proceso de Node corre en UTC. Si el servidor corriera en otra zona, el calculo de 'hoy' se desplaza y reservas del dia limite caen en la pestana equivocada (Proximos vs Historial). Es un off-by-one-day en el borde del dia. Mismo patron replicado en otros modulos; conviene centralizar un util que use Intl/timeZone explicito. | `src/app/(player)/mis-reservas/page.tsx` | M |
| 128 | Mis reservas (jugador) | ⚠️ Riesgo | 🟢 LOW | No hay timeout explicito en cancelMyBookingAction ni en el llamado a createRefund (gateway MP). Si MercadoPago cuelga durante el reembolso (deposit_status='paid', inPolicy), el ConfirmDialog queda en 'Procesando...' con el boton 'Volver' deshabilitado (handleOpenChange retorna temprano si isPending), sin opcion de reintento ni cancelacion para el usuario. Conviene un timeout/abort en el gateway de pagos. | `src/modules/bookings/booking.cancellation.ts` | L |
| 129 | Mock MercadoPago (solo testing) | ⚠️ Riesgo | 🟢 LOW | Sin debounce ni disabled en los botones de submit: doble-click rapido envia dos POSTs simultaneos al webhook. Aunque processed_webhooks deduplica por mpEventId, puede causar doble navegacion o flash en el cliente. | `src/app/mock-mp/checkout/page.tsx` | S |
| 130 | Nuevo abonado | ⚠️ Riesgo | 🟢 LOW | Los montos se convierten con Math.round(valor * 100) sobre un float proveniente de input type=number step=0.01. Para valores con artefactos de punto flotante (p.ej. 10.005) el redondeo depende de la representacion binaria y puede dar 1000 o 1001 centavos de forma no determinista. Conviene parsear el string crudo del input y redondear a 2 decimales de forma controlada antes de multiplicar. | `src/app/(admin)/abonados/nuevo/actions.ts` | S |
| 131 | Nuevo abonado | ⚠️ Riesgo | 🟢 LOW | Posible desincronizacion (TOCTOU) entre el preview y la creacion: ambos releen closedDates del tenant de forma independiente, y el chequeo de overlap de bookings se repite en insertBookingsForSlots. Si entre preview y submit cambia closedDates o aparece una reserva, fechas mostradas como OK en preview pueden saltarse en la creacion. El sistema lo maneja (revalida overlap al insertar) pero el usuario no recibe aviso explicito de la discrepancia entre lo previsualizado y lo creado. | `src/modules/abonados/abonado.service.ts` | M |
| 132 | Onboarding (wizard 4 pasos) | ⚠️ Riesgo | 🟢 LOW | No hay validacion de 'al menos 1 dia abierto' al completar horarios. Un complejo puede terminar el onboarding con los 7 dias cerrados (closed=true), quedando invisible para reservas sin advertencia. | `src/app/onboarding/actions.ts` | S |
| 133 | Onboarding (wizard 4 pasos) | ⚠️ Riesgo | 🟢 LOW | El mensaje de rate-limit es generico y unico (ADMIN_RATE_LIMIT_MESSAGE) para los 4 pasos; no distingue crear tenant (Paso 1) de avanzar (Paso 2/3/4), lo que confunde al usuario sobre que accion fue rechazada. | `src/shared/rate-limit/server-action.ts` | S |
| 134 | Para complejos (marketing) | ⚠️ Riesgo | 🟢 LOW | FEATURE_BG (ShowcaseStrip) hardcodeado a URL externa de Unsplash sin fallback local. Si la CDN de Unsplash cae o el photo-id es removido (los términos de Unsplash permiten remociones), el fondo de la sección desaparece silenciosamente sin ningún fallback. | `src/app/(public)/para-complejos/page.tsx` | S |
| 135 | Perfil del jugador | ⚠️ Riesgo | 🟢 LOW | Solo se reporta el primer error de Zod al usuario: si nombre esta vacio Y telefono es muy corto, el usuario ve solo el primer error (nombre) y no sabe que telefono tambien falla, obligando multiples envios para descubrir todos los errores. | `src/app/(player)/perfil/actions.ts` | S |
| 136 | Perfil publico del complejo | ⚠️ Riesgo | 🟢 LOW | ReviewsSection 'Ver mas resenias': al fallar el fetch el catch es totalmente silencioso (sin toast). El boton sigue clickeable para reintentar (correcto), pero el usuario no recibe ninguna senal de que la carga fallo, dando sensacion de boton inerte. Agregar feedback minimo (toast no intrusivo) en el catch. | `src/app/(public)/[slug]/components/ReviewsSection.tsx` | S |
| 137 | Perfil publico del complejo | ⚠️ Riesgo | 🟢 LOW | ShareButton se usa en TenantHeader sin prop url, por lo que resolveUrl() cae a window.location.href. Si el usuario navego con query params transitorios (ej. ?date=2026-05-25 via deep link), el enlace copiado/WhatsApp incluye ese parametro efimero en vez de la URL canonica limpia /{slug}. Pasar url={absoluteUrl('/'+tenant.slug)} para compartir la canonica. | `src/app/(public)/[slug]/components/TenantHeader.tsx` | S |
| 138 | Privacidad | ⚠️ Riesgo | 🟢 LOW | La fecha 'Ultima actualizacion: 25 de mayo de 2026' en /privacy esta hardcoded en el JSX. Si la politica se modifica sin actualizar esta fecha, los usuarios son notificados de una version desactualizada, incumpliendo la promesa de la seccion 9 ('publicaremos la fecha de actualizacion'). | `src/app/(public)/privacy/page.tsx` | S |
| 139 | Privacidad | ⚠️ Riesgo | 🟢 LOW | La pagina /privacy no renderiza la version de la politica (no hay numero de version ni referencia a players.terms_version). El schema tiene terms_version en players y el doc18 define versionado, pero la pagina publica no expone que version esta vigente, dificultando que usuarios sepan ante cual version consintieron. | `src/app/(public)/privacy/page.tsx` | S |
| 140 | Registro de nuevo admin | ⚠️ Riesgo | 🟢 LOW | Sin CAPTCHA ni rate-limiting propio en el endpoint de registro. Un bot puede generar miles de magic links y saturar la cuota de emails de Supabase/Resend. | `src/app/(auth)/register/actions.ts` | M |
| 141 | Reportes mensuales | ⚠️ Riesgo | 🟢 LOW | calcOccupancyPct no limita a 100%: si booked_minutes supera available_minutes por inconsistencia de datos, renderiza valores como '125%' en la tabla Por cancha sin cap. | `src/modules/reports/report.utils.ts` | S |
| 142 | Reportes mensuales | ⚠️ Riesgo | 🟢 LOW | CSV exportado sin BOM UTF-8: Content-Type es 'text/csv; charset=utf-8' sin BOM ﻿, lo que causa que Excel en Windows abra el archivo con encoding incorrecto y los caracteres con tilde/ñ se vean corruptos. | `src/app/api/reports/revenue/route.ts` | S |
| 143 | Reportes mensuales | ⚠️ Riesgo | 🟢 LOW | GET /reportes no tiene rate-limiting: cualquier admin autenticado puede recargar la página indefinidamente ejecutando 5 queries pesadas por carga (typeRows, courtIncome, courtMinutes, bookingCount, courtCount) sin throttle. | `src/app/(admin)/reportes/page.tsx` | M |
| 144 | Reserva con error de pago | ⚠️ Riesgo | 🟢 LOW | Cada 'Reintentar pago' reinvoca createDepositPayment, que hace INSERT de un nuevo payment pending tipo deposit. No existe UNIQUE en (booking_id, type) ni mp_payment_id en ese momento (se setea recien en el webhook), por lo que se acumulan filas payment huerfanas pending por booking; bookings.payment_id apunta solo a la ultima. Distorsiona reportes/conciliacion aunque no afecta el cobro real. | `src/shared/db/schema/payments.ts` | M |
| 145 | Reserva con error de pago | ⚠️ Riesgo | 🟢 LOW | Cuando booking.tenantSlug es null (tenant borrado o JOIN sin match) el Link 'Reservar de nuevo' cae silenciosamente a href '/'. Es navegacion segura pero deja al jugador en el home generico en lugar de volverlo al complejo original; perdida de contexto y UX pobre. | `src/app/reserva/[bookingId]/error/page.tsx` | S |
| 146 | Reserva exitosa (post-pago) | ⚠️ Riesgo | 🟢 LOW | BookingMiniMap se carga con dynamic import ssr:false y solo define 'loading' (skeleton); no hay manejo de error si falla la carga del chunk de Leaflet o de los tiles de OpenStreetMap. Ante fallo de red en el bundle de Leaflet, el usuario queda con el skeleton animado permanente sin fallback de error. | `src/components/booking/BookingSuccessExtras.tsx` | S |
| 147 | Reserva pendiente (ventana 15 min) | ⚠️ Riesgo | 🟢 LOW | Si el status queda rezagado en pending_payment despues de los 15 min (lag del background job que marca expired), ExpiryCountdown clampa el remaining a '0:00' (Math.max(0,...)) y el UI sigue mostrando el spinner con countdown en cero, lo que puede confundir al usuario que cree estar dentro de la ventana. No hay transicion local a estado 'expirado' por tiempo. | `src/components/booking/PaymentStatusWatcher.tsx` | M |
| 148 | Staff | ⚠️ Riesgo | 🟢 LOW | getStaffMembers no tiene cláusula LIMIT ni paginación — en producción con muchos miembros históricos la query trae todas las filas; improbable en v1 dado el dominio (complejos pequeños), pero es una deuda técnica. | `src/app/(admin)/staff/page.tsx` | M |
| 149 | Terminos y condiciones | ⚠️ Riesgo | 🟢 LOW | El email privacidad@turnogol.com.ar aparece en dos lugares del componente /terms sin ser leido de una variable de entorno ni de una constante centralizada. Si el email cambia, requiere editar el codigo y redeployar, con riesgo de quedar desincronizado con privacy/page.tsx u otros componentes. | `src/app/(public)/terms/page.tsx` | S |
| 150 | Terminos y condiciones | ⚠️ Riesgo | 🟢 LOW | La fecha 'Ultima actualizacion: 25 de mayo de 2026' esta hardcodeada en el JSX. Si se modifica el contenido de T&C en una PR futura, es facil olvidarse de actualizar la fecha, publicando terminos con fecha incorrecta (incumplimiento de la clausula 10 del propio documento). | `src/app/(public)/terms/page.tsx` | S |
| 151 | Transversal: Performance/stress + Accesibilidad global + Responsive/cross-browser | ⚠️ Riesgo | 🟢 LOW | El tamaño del bundle gzipped post-remoción de Sentry-Replay nunca fue verificado con pnpm analyze. El gate de performance CI podría estar fallando silenciosamente si el shared bundle supera 140KB. _(no verificable)_ | `src/components/perf/WebVitalsReporter.tsx` | S |
| 152 | Transversal: Reservas/concurrencia + MercadoPago/webhooks + WebSockets/realtime | ⚠️ Riesgo | 🟢 LOW | El webhook handler no valida que el tenant_id del query param exista en la tabla tenants antes de encolar el job. Un payload con un tenant_id fantasma encola el job, que luego falla con TenantMpNotConnectedError y se reintenta 5 veces innecesariamente. | `src/app/api/webhooks/mercadopago/route.ts` | S |
| 153 | Verificacion de magic link | ⚠️ Riesgo | 🟢 LOW | /verify es un Server Component estatico sin timeout ni fallback en el cliente. Si el intercambio en el callback tarda (>30s, red lenta) o el usuario llega directo a /verify (deep link, back tras limpiar error), ve el spinner 'Verificando tu enlace…' indefinidamente sin via de recuperacion salvo el logo hacia home. Lo confirma tambien el test [Refresh timeout] (linea 540). | `src/app/(auth)/verify/page.tsx` | S |
| 154 | Verificacion de magic link | ⚠️ Riesgo | 🟢 LOW | LoadingState no ofrece retry ni indicacion de que hacer si el intercambio falla silenciosamente o el usuario cierra la pestaña. Como el codigo del magic link es de un solo uso, no hay forma de retomar el flujo: el usuario debe pedir un nuevo enlace, pero la UI no lo guia hacia eso en el estado de carga. | `src/app/(auth)/verify/page.tsx` | S |
| 155 | Caja diaria | 🏗️ Arquitectura | 🟢 LOW | El gating de tenant_status (suspended=read-only, blocked=403) vive en with-tenant.ts, que solo envuelve Route Handlers. /caja es Server Component y sus Server Actions (createCashFlowAction, closeDayAction) usan withTenantContext directo sin ese gate; solo el kill-switch por feature-flag 'suspended' del admin layout bloquea acceso. Un tenant con tenant_status='suspended' (que deberia ser solo-lectura) podria seguir escribiendo movimientos via Server Action. past_due si carga /caja, pero eso es por diseno (doc4). | `src/app/(admin)/caja/actions.ts` | M |
| 156 | Complejo suspendido | 🏗️ Arquitectura | 🟢 LOW | No existen tests unitarios para la página /suspended: ni snapshot, ni test de metadata (robots/title), ni test del markup accesible. El documento QA lista ~50 checks sin cobertura automatizada. | `src/app/(public)/suspended/page.tsx` | M |
| 157 | Dashboard del admin | 🏗️ Arquitectura | 🟢 LOW | Divergencia spec vs implementacion: el checklist/spec sugiere que clic en un KPI navega a su seccion, pero MetricCard es un div estatico sin onClick ni href (solo hover visual). No es bug funcional, pero amerita aclarar la especificacion o quitar la expectativa de interactividad. | `src/components/dashboard/metric-card.tsx` | S |
| 158 | Home landing publica | 🏗️ Arquitectura | 🟢 LOW | No hay validación server-side de los parámetros de query (city, date, time) que el buscador construye con URLSearchParams y pasa via router.push. La página /explorar recibe valores sin sanitizar desde la URL. | `src/components/site/HeroSearch.tsx` | M |
| 159 | Login (magic link) | 🏗️ Arquitectura | 🟢 LOW | No hay redireccion si el usuario ya esta autenticado (Riesgo 1). Un admin o jugador con JWT valido que navega a /login ve el formulario igual: el (auth)/layout.tsx no valida sesion y el matcher del middleware no incluye /login. Confunde al usuario logueado (deberia ir a su dashboard/mis-reservas). | `src/app/(auth)/layout.tsx` | S |
| 160 | Reserva exitosa (post-pago) | 🏗️ Arquitectura | 🟢 LOW | loadBooking confía exclusivamente en RLS (withPlayerContext) y no filtra por player_id en el WHERE (solo por b.id). Es defensa-en-profundidad ausente: si el contexto RLS no se setea o falla silenciosamente, la query devolvería la reserva de otro jugador. Mitigado en práctica por RLS dual y tests, pero un filtro explícito redundante reduciría el blast radius. | `src/app/reserva/[bookingId]/exito/page.tsx` | S |
| 161 | Caja diaria | ♿ Accesibilidad | 🟢 LOW | Cuando la caja esta cerrada CajaActions retorna null (botones desaparecen sin estado deshabilitado visible). Existe el badge 'Cerrada por X' junto al titulo, pero los botones de accion solo se esfuman; falta un mensaje o tooltip que explique por que no se puede agregar movimiento ni cerrar (los ajustes posteriores van por otra via). Mejora menor de claridad UX. | `src/app/(admin)/caja/components/CajaActions.tsx` | S |
| 162 | Configuracion: Horarios | ♿ Accesibilidad | 🟢 LOW | Los inputs de Apertura/Cierre (14 inputs type=time) no tienen asociacion explicita de label: el <Label> de cada dia no usa htmlFor y los inputs no tienen id ni aria-label. Un lector de pantalla no anuncia inequivocamente 'Lunes apertura' / 'Lunes cierre'. Agregar aria-label (o id+htmlFor) por input combinando dia + apertura/cierre. | `src/app/(admin)/settings/horarios/page.tsx` | S |
| 163 | Confirmacion de reserva (checkout jugador) | ♿ Accesibilidad | 🟢 LOW | Cuando aparece el alert de error (slot_taken / banned) no hay un CTA explicito para volver a elegir otro turno; es solo un parrafo role='alert'. El jugador debe usar el back del navegador. Agregar un link 'Elegir otro turno' junto al alert mejora la recuperacion del error. | `src/app/(public)/[slug]/reservar/page.tsx` | S |
| 164 | Confirmacion de reserva (checkout jugador) | ♿ Accesibilidad | 🟢 LOW | En LoginGate el bloque de error usa role='alert' pero sin aria-live='polite'; ademas la transicion a estado 'sent' (card 'Revisá tu email') reemplaza el form sin region aria-live, por lo que el lector de pantalla no anuncia el cambio de estado tras enviar el magic link. | `src/app/(public)/[slug]/reservar/components/LoginGate.tsx` | S |
| 165 | Dashboard del admin | ♿ Accesibilidad | 🟢 LOW | Los botones del checklist ('Copiar link' / 'Configurar') no tienen estado disabled ni loading durante la transicion. 'Copiar link' usa useTransition pero el boton no se deshabilita mientras isPending, permitiendo doble clic antes de que revalidatePath complete. El impacto real es bajo porque la action es idempotente (merge jsonb a true) y esta rate-limited. | `src/components/dashboard/onboarding-checklist.tsx` | S |
| 166 | Explorar complejos | ♿ Accesibilidad | 🟢 LOW | RatingStars con count=0 genera aria-label '0.0 de 5 estrellas' sin aclarar 'sin resenas', lo que puede confundir a lectores de pantalla. El componente solo dice 'X de 5 estrellas' cuando count es 0 o undefined. | `src/components/public/RatingStars.tsx` | S |
| 167 | Gestion de canchas | ♿ Accesibilidad | 🟢 LOW | No hay validación client-side que verifique cobertura de franjas horarias antes del submit; el error de cobertura (ej. 'Precios sin cubrir: L 09:00...') llega recién desde el servidor, lo cual genera un round-trip innecesario y una UX confusa donde el usuario no sabe qué franjas corregir hasta recibir el error. | `src/app/(admin)/canchas/components/CourtForm.tsx` | M |
| 168 | Home landing publica | ♿ Accesibilidad | 🟢 LOW | Las etiquetas de texto de amenities en FeaturedComplexCard usan sr-only sm:not-sr-only: en viewport mobile (<640px) el texto queda visualmente oculto y solo el ícono es visible para usuarios videntes, sin tooltip ni alternativa textual accesible en pantalla. | `src/components/site/FeaturedComplexCard.tsx` | S |
| 169 | Nuevo abonado | ♿ Accesibilidad | 🟢 LOW | Los estados de carga 'Cargando…' (preview) y 'Guardando…' (confirmar) solo cambian el texto y deshabilitan el boton, sin spinner visual ni aria-busy. En conexiones lentas el feedback es debil. Agregar un spinner/icono y aria-busy al boton durante la transition. | `src/app/(admin)/abonados/nuevo/AbonadoForm.tsx` | S |
| 170 | Onboarding (wizard 4 pasos) | ♿ Accesibilidad | 🟢 LOW | El mensaje de error del Paso 2 (StepCourts) no tiene role='alert', por lo que los lectores de pantalla no lo anuncian. Nota: el Paso 3 (StepSchedule) ya fue corregido y SI usa role='alert'. | `src/app/onboarding/components/StepCourts.tsx` | S |
| 171 | Onboarding (wizard 4 pasos) | ♿ Accesibilidad | 🟢 LOW | Los inputs requeridos del Paso 1 marcan obligatoriedad solo con un asterisco rojo visual; no exponen aria-required explicito junto al label, por lo que la marca de requerido no se comunica de forma semantica clara a tecnologias de asistencia. | `src/app/onboarding/components/StepIdentity.tsx` | S |
| 172 | Onboarding (wizard 4 pasos) | ♿ Accesibilidad | 🟢 LOW | Requests lentos dejan el boton en estado 'Creando...'/'Guardando...' indefinidamente, gobernado solo por isPending, sin timeout ni mensaje. Si la conexion se corta antes de la respuesta el usuario queda sin feedback. | `src/app/onboarding/components/StepIdentity.tsx` | M |
| 173 | Perfil del jugador | ♿ Accesibilidad | 🟢 LOW | El label 'Email' del campo display-only no tiene htmlFor: el <label> en ProfileForm no referencia ningun elemento con id, por lo que un screen reader no asocia el label con el contenido del email. | `src/app/(player)/perfil/ProfileForm.tsx` | S |
| 174 | Registro de nuevo admin | ♿ Accesibilidad | 🟢 LOW | El helper text del campo phone ('Formato argentino con prefijo +54 9') se renderiza como <p> sin aria-describedby en el input, por lo que screenreaders no lo anuncian automáticamente al enfocar el campo. | `src/app/(auth)/register/page.tsx` | S |
| 175 | Reserva con error de pago | ♿ Accesibilidad | 🟢 LOW | El boton 'Reintentar pago' y el Link 'Reservar de nuevo' no tienen estilo focus-visible explicito (sin focus:ring ni outline-offset). Dependen solo del outline nativo del navegador; sobre elementos custom emerald el indicador de foco por teclado puede quedar poco visible. Mejora menor de navegacion por teclado. | `src/app/reserva/[bookingId]/error/page.tsx` | S |
| 176 | Reserva pendiente (ventana 15 min) | ♿ Accesibilidad | 🟢 LOW | PaymentStatusWatcher no redirige automaticamente a /exito ni /error: usa solo <Link> de navegacion manual. El checklist QA esperaba redirect automatico. No deja al usuario atrapado (los Links funcionan y /mis-reservas, / existen), pero al confirmarse el pago el foco queda en el spinner viejo y el usuario debe clickear manualmente; solo el aria-live anuncia el cambio. | `src/components/booking/PaymentStatusWatcher.tsx` | M |

## Orden de ejecucion sugerido

1. **Sprint 0 — BLOCKERS** (8): impiden el lanzamiento o rompen la experiencia. Atacar primero.
2. **Sprint 1 — HIGH** (61): degradan UX; abordar antes del lanzamiento si el tiempo lo permite.
3. **Sprint 2 — LOW** (107): cosmeticos/mejoras; post-lanzamiento.

## Hallazgos ya resueltos / no vigentes (verificados en codigo)

Estos hallazgos del documento QA ya estan manejados en el codigo actual. Se listan para evitar re-trabajo.

| Vista | Descripcion (del QA) | Evidencia en codigo |
|---|---|---|
| Login (magic link) | XSS via mensaje de error de Supabase (Riesgo 7): no aplica. La accion nunca renderiza el error crudo de Supabase; en fallo devuelve copy hardcoded y React escapa el JSX. No hay sink de string no confiable en el <p role='alert'>. | actions.ts L40-42 ante result.ok=false retorna 'No pudimos enviar el email...' (literal), nunca result.error; page.tsx L126-130 renderiza solo state.message (siempre literal) escapado por React. |
| Login (magic link) | Sin test e2e del magic link de jugador sin autenticar desde /login (Riesgo 9): no aplica. Ya existe cobertura e2e del flujo de jugador anonimo en /login (envio de magic link y caso de email vacio). | player-magic-link.spec.ts usa browser.newContext() sin storageState, va a /login, completa E2E_PLAYER_EMAIL y verifica SentState 'Revisá tu email' + caso de email vacio que queda en /login. |
| Disponibilidad semanal | El riesgo (6) del documento QA sobre timezone mismatch en formatDayTab por uso de Date.UTC NO es vigente: el codigo construye la fecha con concatenacion 'T12:00:00Z' y formatea con timeZone:'UTC', por lo que el dia mostrado siempre coincide con el dateStr recibido. No hay corrimiento de zona horaria. Lo marco como ya-arreglado/no-bug. | formatDayTab L7-12: 'new Date(dateStr + "T12:00:00Z")' y ambos Intl.DateTimeFormat usan timeZone:'UTC'; no se usa Date.UTC con componentes locales, no hay mismatch. |
| Mis reservas (jugador) | Riesgos 1 y 8 del documento (la Server Action de cancelacion falla y el error no llega al dialog) ya estan resueltos en el codigo actual. CancelBookingButton.handleConfirm retorna {success:false, error} cuando la action falla, y ConfirmDialog.handleConfirm hace setError(res.error) para mantener el dialog abierto y mostrar el error en <p role='alert'>. Se reporta como no-accionable (ya-arreglado) para cerrar el hallazgo del documento. | CancelBookingButton.tsx:21-24 retorna {success:false, error:result.error}; confirm-dialog.tsx:55-57 hace setError(res.error ?? ...) cuando res.success===false y retorna sin cerrar el dialog. |
| Configuracion: Facturacion (SaaS + MP) | Riesgo del doc de crash al parsear staff_pin_hash corrupto: verificado que NO se materializa. verifyPin valida que existan ambas partes del split(':') y Buffer.from(x,'base64') no lanza ante base64 invalido (decodifica lo que puede), y timingSafeEqual esta protegido por chequeo de longitud, retornando false en vez de crashear. Residual menor: verifyPinAction no envuelve verifyPin en try-catch, pero no hay ruta de excepcion conocida. | pin.ts lineas 30-37: guarda saltB64/hashB64, Buffer.from base64 invalido no throw, y if(derived.length!==expected.length) return false antes de timingSafeEqual. |
| Transversal: Performance/stress + Accesibilidad global + Responsive/cross-browser | Las celdas vacías de la grilla (BookingCard sin booking) tenían el bug documentado de falta de role=button/tabIndex. Ya está resuelto en el código actual. | Líneas 21-33: los slots interactivos ya incluyen role='button', tabIndex=0, onKeyDown (Enter/Space), y aria-label='Reservar turno {timeStart}'. |
| Transversal: Performance/stress + Accesibilidad global + Responsive/cross-browser | Spinners con animate-spin no respetan prefers-reduced-motion de forma explícita en cada componente, pero el riesgo está cubierto globalmente. | Líneas 104-117: @media (prefers-reduced-motion: reduce) aplica animation-duration:0.01ms !important a * y neutraliza animate-spin en todos los componentes. |
