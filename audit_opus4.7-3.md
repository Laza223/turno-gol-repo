[effort: high]

He hecho un barrido exhaustivo de las 12 correcciones aplicadas, y verifico que **todas se propagaron correctamente al schema crítico (doc13)**. Sin embargo, encontré un puñado de inconsistencias que quedaron rezagadas en docs satélite y dos defectos puntuales en el SQL que romperían la migration. Reporto solo los defectos pendientes:

| Archivo(s) | Error Lógico / Fuga Detectada | Solución Arquitectónica Propuesta |
|---|---|---|
| doc13 §3.11 final del bloque (`COMMENT ON INDEX uq_tenant_player_active_ban`) | **BLOQUEANTE — el SQL de migration FALLARÁ**. El Fix #10 reemplazó correctamente el índice parcial por el trigger `enforce_single_active_ban`, pero la última línea del bloque sigue siendo `COMMENT ON INDEX uq_tenant_player_active_ban IS '...';` apuntando a un índice que YA NO EXISTE. PostgreSQL devolverá: `ERROR: relation "uq_tenant_player_active_ban" does not exist`. La migration entera queda en rollback. | Eliminar el `COMMENT ON INDEX uq_tenant_player_active_ban`. Reemplazarlo por: `COMMENT ON TRIGGER enforce_single_active_ban ON tenant_player_bans IS 'Previene insertar un ban activo si ya hay otro vigente para el mismo (tenant_id, player_id). Permite re-banear cuando el ban anterior expiró. Reemplaza al índice parcial uq_tenant_player_active_ban (eliminado por incompatibilidad de NOW() con índices parciales — Fix #10).';`. Adicionalmente agregar comentario al índice de performance: `COMMENT ON INDEX idx_tenant_player_bans_active IS 'Lookup rápido de bans por (tenant_id, player_id, banned_until). Sin predicado NOW() para cumplir IMMUTABLE.';`. |
| doc13 §10 (Migration Order) | **BLOQUEANTE — la tabla `system_admins` no está en el orden de migration**. El §10 lista 8 pasos (extensiones, ENUMs, globales, aisladas, triggers, seed, RLS) con sub-índices explícitos por tabla. `system_admins` está creada en §3.14 pero **no aparece en el orden 3.x ni en ningún paso**. Un dev que ejecute las migrations por bloques siguiendo esta guía simplemente no la creará. | Insertar en §10 paso 3 (Tablas globales sin FK): `3.7 system_admins (sin FK)`. Actualizar también el conteo "8 tablas" a "9 tablas globales/sistema". Agregar al final del §10 un seed bloque opcional: `7.2 system_admins (bootstrap manual del primer super-admin via service role o seed.sql separado, NUNCA via la app)`. |
| doc15 §5.2 (Canchas) `rol mínimo: readonly` | **Rol fantasma en API contracts**. La tabla de endpoints de Canchas declara `GET /api/courts` y `GET /api/courts/:id` con `rol mínimo: readonly`. El ENUM `staff_role` tiene SOLO `'admin'`. Cualquier middleware de autorización que valide `role >= 'readonly'` fallará porque `readonly` no es un valor válido del ENUM. Confunde al dev sobre cómo implementar el guard. | Reemplazar las dos celdas `readonly` por `admin` (todos los endpoints del panel admin requieren admin en v1). Agregar nota al inicio de §5: "v1 tiene un único rol `admin`. La columna 'Rol mínimo' está mantenida para extensión futura cuando se agreguen roles. Las zonas sensibles (precios, suscripción, configuración) se protegen con `tenant.settings.admin_pin` (ver doc6 Tenant settings) en lugar de roles separados". |
| doc8 US-ADM-001 (Gestionar Canchas) | **Stories con vocabulario obsoleto del modelo de roles y estados**. Tres aserciones contradicen las decisiones tomadas: (1) "Court se crea con status=`active`" → debería ser `'online'`; (2) "puedo editar: nombre, superficie, precio por franja, estado (active/maintenance/inactive)" → es `online/offline`; (3) "Dado que pongo una cancha en `maintenance`, entonces los slots futuros aparecen como bloqueados" → ya no existe `maintenance`, ahora es `offline`. Si Claude Code implementa esta story literal, hardcodea ENUM viejo. | Reescribir las 3 aserciones a: (1) "Court se crea con status=`online`"; (2) "puedo editar: nombre, superficie, precio por franja, estado (`online`/`offline`)"; (3) "Dado que pongo una cancha en `offline`, entonces los slots futuros desaparecen de la grilla y las reservas existentes se mantienen". Actualizar también el copy de los warnings: "Si pongo una cancha `offline` CON abonados activos → warning..." en lugar de "desactivo". |
| doc8 US-ADM-003 (Gestionar Staff) | **Story descalibrada con la decisión de rol único**. El narrative dice "cuando contrato un nuevo recepcionista" y "darle acceso al sistema con permisos limitados, para que pueda gestionar reservas y caja sin acceder a la configuración ni los reportes". Esto es exactamente lo opuesto a la decisión P2.1 (rol único `admin` con PIN para zonas sensibles). Una vez más, el dev implementaría granularidad de permisos que no existe. | Reescribir el narrative: *"Como Marcelo, cuando incorporo un nuevo empleado al complejo, quiero invitarlo como `admin` con su propio email, para que pueda operar el sistema completo. Las zonas sensibles (precios, suscripción, configuración) están protegidas con un PIN compartido que solo yo conozco."*. Eliminar "permisos limitados" y "sin acceder a la configuración ni los reportes" — el PIN es la única defensa. Actualizar el criterio "Dado que quiero cambiar el rol de un staff" porque NO hay rol que cambiar; reemplazar por "Dado que quiero revocar el PIN de zonas sensibles, cuando lo cambio en Settings, entonces todos los empleados deben recibir el nuevo PIN del dueño". |
| doc7 Flujo 2 (Reserva Online) precondiciones | **Stale enum reference en precondición crítica**. La precondición de Flujo 2 dice literal: "La cancha tiene status `active`". Flujo 3 (Reserva Manual) dice correctamente "status `online`". Asimetría peligrosa: el dev que implemente el guard de Flujo 2 podría escribir `WHERE status = 'active'` y la query siempre devolverá vacío (el ENUM no tiene `'active'` para `court_status`). | Cambiar la precondición del Flujo 2 a: "La cancha tiene status `online`". Hacer un grep global de `status 'active'` en doc7 (Flujo 5 tiene la misma referencia obsoleta para precondición de Abonado: "La cancha tiene status `active`" → debe ser `online`). |
| doc12 §2 (Tablas SIN tenant_id) ↔ doc13 §3.14 (`system_admins`) | **`system_admins` está implementada pero no aparece en la clasificación canónica de doc12**. La tabla §2 lista 7 globales pero NO incluye `system_admins`. Tampoco hay mención del **3er context variable `app.current_system_admin_id`** en doc12 §4 (middleware) ni en §4.3 (orden del middleware stack). Como doc12 es la fuente de verdad de aislamiento citada por toda la app, un dev no sabrá cómo construir el middleware de `/internal`. | (1) Agregar fila a la tabla §2: `8 \| system_admins \| SystemAdmin \| Equipo interno de TurnoGol. RLS basada en self-id (app.current_system_admin_id). Acceso solo via panel /internal con MFA + IP whitelist`. Actualizar conteo a "7 globales + 1 sistema (`system_admins`) + 1 con RLS dual". (2) Agregar §4.4 nuevo: "Middleware del panel interno (`/internal/*`)". Documentar: "El middleware del panel interno verifica el JWT de tipo `system_admin` y ejecuta `SET LOCAL app.current_system_admin_id = $id`. NO setea `app.current_tenant_id` (las queries del panel usan el service role para acceder cross-tenant). Steps 4 y 5 del middleware stack se saltean." (3) Actualizar §4.3 enumerando tres tipos de request: staff / player / system_admin. |
| doc13 §3.10 (`audit_logs`) y §4.2 (`enforce_booking_invariants_fn`) — typos en SQL | **Espacios accidentales dentro de palabras**. `audit_logs` COMMENT dice `'Registro de auditoría. INSERT ONLY. Reten ción 12 meses.'` (espacio en "Retención"). El comentario del trigger `enforce_booking_invariants_fn` dice `-- (1) price_snapshot es siempre inm utable` (espacio en "inmutable"). PostgreSQL no falla con esto, pero al copy-paste a Drizzle quedan literales raros en producción. | Buscar y reemplazar: `'Reten ción'` → `'Retención'` y `inm utable` → `inmutable`. Hacer un linter simple `grep -nE '[a-z] +[a-z]{2,}' docs/doc13_database_schema.md` para detectar otros casos similares. |
| doc13 §3.13 (`daily_cash_closes`) `diff_amount` ↔ §3.13 comment | **Comentario semánticamente desincronizado con la nueva fórmula**. Después del Fix #4 el balance es `total_income + total_adjustments` (sin `expense`). Pero el COMMENT de `diff_amount` sigue diciendo `'Diferencia entre efectivo declarado y balance esperado. Positivo = sobrante, negativo = faltante.'` sin precisar QUÉ es `balance_esperado_efectivo` ni de qué se compone (es solo el subset cash de income/adjustments). Sin esto, el dev que implemente el cálculo de `diff_amount` en el cierre podría incluir transferencias y MP, dando un diff falso. | Expandir el comentario: `COMMENT ON COLUMN daily_cash_closes.diff_amount IS 'Diferencia entre declared_cash y la suma de cash_flows del día filtrados por method=''cash'' (NO incluye transfer ni mercadopago, que no están en el cajón físico). Positivo = sobrante (más efectivo del esperado). Negativo = faltante. Calculado en el momento del cierre, NUNCA recalculado después.';`. Documentar el mismo cálculo en doc7 Flujo 6 §4 paso 3 ("Verificación y cierre"). |
| doc13 §4.4 (Guard de transición atómica) referencia residual | **Referencia a un trigger que ya no existe**. El comentario de §4.4 dice: *"Los estados finales (expired, completed, no_show, canceled_*) están protegidos adicionalmente por el trigger `enforce_booking_immutability` (§4.2)"*. Pero §4.2 ya no se llama así — el trigger consolidado es `enforce_booking_invariants` y el viejo `enforce_booking_immutability` quedó deprecado con instrucción de DROP. Confunde al dev sobre cuál es el trigger autoritativo. | Reemplazar la última línea del comentario por: *"Los estados finales (expired, completed, no_show, canceled_refunded, canceled_no_refund) están protegidos adicionalmente por el trigger `enforce_booking_invariants` (§4.2), que rechaza cualquier UPDATE que modifique campos distintos de `notes_internal` cuando OLD.status es terminal."*. |

---

## Veredicto

**No estás listo todavía.** De los 10 hallazgos, **2 son bloqueantes para correr la migration** (#1 `COMMENT ON INDEX` a índice inexistente, #2 `system_admins` ausente del Migration Order). Si Claude Code intenta aplicar el schema tal cual está, el primer `psql -f migrations.sql` aborta. **Otros 5 son altos** porque inducen al dev a implementar contra ENUMs viejos (`active`, `readonly`, `maintenance`) o features que ya no existen (rol recepcionista, permisos granulares).

Lo bueno: **la columna vertebral del schema (doc13) está sólida**. Las 12 correcciones críticas de la auditoría anterior están bien aplicadas: `staff_role DEFAULT 'admin'`, `system_admins` table existente con MFA/RLS/3ª variable de contexto, trigger consolidado `enforce_booking_invariants`, `chk_cashflow_type_category`, `chk_booking_payment_consistency`, `total_adjustments` renombrado, `REVOKE UPDATE,DELETE` en `daily_cash_closes`, trigger `enforce_single_active_ban` reemplazando el `NOW()` en índice parcial, y todas las RLS policies dual para player/staff/realtime. La auditoría de Fase 2 fue exitosa en su scope estricto.

Lo que queda es una capa de "limpieza fina" — 2 fixes de SQL puntual (15 minutos) + 5 sincronizaciones cross-doc (45 minutos). **Cuando atiendas estos 10, estás técnicamente listo para empezar a desarrollar.**

Mi recomendación táctica: aplicá #1 y #2 inmediatamente (bloqueantes), luego corré una pasada manual con grep sobre `'active'`, `'maintenance'`, `'recepcionista'`, `'readonly'` en `docs/` para cazar cualquier residuo que se me haya escapado. Si todo eso queda limpio, el próximo paso natural es generar el archivo `migrations/0001_initial.sql` ejecutando el orden de §10 y verificar que `psql --dry-run` (o `drizzle-kit push --dry-run`) lo acepta sin errores. Ese es el verdadero test final antes de abrir Claude Code.