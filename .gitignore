node_modules/
dist/
.next/
.env
.env.production
.env.local
.env.test
.env.staging
.lazaro-os.json
*.log
*.resolved
*.tsbuildinfo
.DS_Store
Thumbs.db
.worktrees/
.claude/

# Playwright artifacts
test-results/
playwright-report/
.playwright-mcp/
tests/e2e/.auth/
_pw-push.json
.ig-session/

# Lighthouse CI artifacts (HTML reports — bulky, keep only config + summary)
.lighthouseci/
docs/audit/reports/fase-f00-raw/lhci/*.html
docs/audit/reports/fase-f00-raw/lhci/*.json
docs/audit/reports/fase-f03-raw/lhci/*.html
docs/audit/reports/fase-f03-raw/lhci/*.json
docs/audit/reports/fase-f06-raw/lhci/*.html
docs/audit/reports/fase-f06-raw/lhci/*.json

# dependency-cruiser — grafo generado a demanda (pnpm depcruise:graph), no versionado.
docs/architecture-graph.mmd

# Storybook — build estático y evidencia de QA visual (screenshots, snapshots,
# logs de consola). A git van solo los reportes resumidos, los manifiestos y los
# scripts reproducibles (scripts/storybook-qa.mjs), no los miles de PNG.
storybook-static/
artifacts/

# design-sync (claude.ai/design) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.claude/handoff.md

.vercel
.env*

# Baselines visuales: SOLO se versionan las de linux (generadas en CI).
# Ver docs/testing/VISUAL_REGRESSION.md.
tests/e2e/visual/__screenshots__/*/win32/
tests/e2e/visual/__screenshots__/*/darwin/
